---
name: searxng
description: 使用自托管的 SearXNG 元搜索引擎在网络上进行搜索。该引擎可以聚合 Google、Brave、DuckDuckGo 等多个搜索引擎的数据，且无需使用 API 密钥。
homepage: https://docs.searxng.org
command-dispatch: cli
command: python3 {baseDir}/scripts/searxng_search.py
metadata: {"moltbot":{"emoji":"🔍","requires":{"bins":["python3"],"env":["SEARXNG_URL"]}}}
---

# SearXNG 网页搜索

通过您自己托管的 SearXNG 实例，提供尊重用户隐私的元搜索服务。

## 使用场景（触发短语）

当用户提出以下请求时，可以使用此功能：
- “在网页上搜索...”  
- “查找...” / “查询关于...的信息...”  
- “...是什么？”（当需要获取当前信息时）  
- “进行研究...” / “搜索...”  
- “用谷歌搜索...”（此时会重定向到尊重用户隐私的搜索服务）

## 快速入门

```bash
python3 {baseDir}/scripts/searxng_search.py "your query"
python3 {baseDir}/scripts/searxng_search.py "query" --count 10
python3 {baseDir}/scripts/searxng_search.py "query" --lang de
```

## 设置

配置 `SEARXNG_URL` 环境变量：
```bash
export SEARXNG_URL="http://your-searxng-host:8888"
```

## 参数说明

| 参数 | 默认值 | 说明 |
|------|---------|-------------|
| `-n`, `--count` | 5 | 返回的结果数量（1-20条） |
| `-l`, `--lang` | auto | 语言代码（en, de, fr, es 等） |

## 输出格式

返回 JSON 格式的数据：
```json
{
  "query": "search terms",
  "count": 5,
  "results": [
    {"title": "...", "url": "...", "description": "...", "engines": ["google", "brave"], "score": 1.5}
  ]
}
```

## 注意事项

- 无需使用 API 密钥——SearXNG 会汇总来自上游搜索引擎的结果 |
- 结果中会显示来源搜索引擎的信息，以确保透明度 |
- 分数表示结果的相关性（分数越高，相关性越强） |
- 如需搜索新闻，请在查询中添加 “news”；或使用 `--lang` 参数来获取特定地区的内容 |