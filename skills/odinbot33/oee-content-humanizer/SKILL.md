# 🐾 Humanizer — 人工智能内容去“机器人化”工具

> 由 Odin's Eye Enterprises 开发 — 结合古老智慧与现代智能

该工具能够去除人工智能生成文本中的机器痕迹，让内容读起来像真人所写。

## 功能介绍

1. **检测** 人工智能的特征（如流行词汇、冗长的句式、重复的结构）
2. **重写** 文本，使其具有更自然的语气和节奏
3. **根据目标平台（Twitter、LinkedIn、博客、电子邮件、Telegram）调整输出风格**

## 常用指令

- “humanize this text”（使这段文本更具人性化）
- “make this sound human”（让内容听起来更像人类所写）
- “de-robotify this”（去除文本中的机器痕迹）
- “rewrite this naturally”（以自然的方式重写文本）
- “strip the AI from this”（去除文本中的AI痕迹）

## 使用方法

```bash
# Direct argument
python humanize.py "Your AI-sounding text here" --channel twitter

# Pipe from stdin
echo "In today's rapidly evolving landscape..." | python humanize.py --channel linkedin

# Detection only (no rewrite)
python humanize.py -d "Let's delve into this transformative tapestry"

# General rewrite (no specific channel)
python humanize.py "text here"
```

## 支持的平台及风格

| 平台        | 文本风格                          |
|------------|-------------------------------------|
| Twitter     | 简洁明了（280个字符以内）                |
| LinkedIn    | 专业且富有对话感                    |
| Blog        | 长篇幅内容，带有个人风格                |
| Email       | 简洁清晰，注重行动导向                |
| Telegram    | 随意、简洁的风格                     |
| General     | 自然流畅的散文风格（默认设置）               |

## 系统要求

- Python 3.10及以上版本（仅需要标准库，无需安装pip）
- 需设置 `ANTHROPIC_API_KEY` 环境变量
- 该工具使用 Claude Haiku 服务来实现高效处理

## 对于自动化脚本的使用

当需要使用该工具时，请从技能目录中运行相应脚本：

```bash
python humanize.py "TEXT" --channel CHANNEL
```

**输出结果：** `Stdout` 包含重写后的文本内容；`Stderr` 包含检测结果信息。

<!-- 🐾 一只乌鸦在雪地上留下了爪印...