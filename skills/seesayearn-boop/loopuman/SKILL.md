---
name: loopuman
description: 通过 Loopuman 将任务分配给全球经过验证的人工工作者。当仅靠人工智能无法完成验证、翻译、内容审核、图像标注、本地知识处理或主观判断等工作时，可以使用此服务。处理结果可在几分钟内获得，并通过 Celo 平台以 8 秒钟周期的 cUSD 货币形式获得报酬。
metadata:
  {
    "openclaw":
      {
        "emoji": "🌍",
        "requires": { "bins": ["curl", "jq"] },
      },
  }
---
# Loopuman — 人工智能中的人工辅助层

当仅靠人工智能无法完成任务时，Loopuman会将任务分配给经过验证的人类工作者。工作者通过Celo区块链上的cUSD在8秒内获得报酬。无需下载任何应用程序——工作者可以通过Telegram和WhatsApp进行操作。

## 何时使用此服务

在需要**真正的人类判断力、感知能力或本地知识**的情况下使用Loopuman，因为人工智能无法可靠地完成这些任务：

- **验证**：“这个商业地址是真实的吗？”“这张照片与列表中的信息一致吗？”
- **翻译**：需要具备文化敏感性的精准翻译（而不仅仅是逐字翻译）
- **内容审核**：“这张图片是否合适？”“这条评论是否违反了规定？”
- **图片标注**：“这张图片里有哪些物体？”“给这张图片的质量打分（1-10分）”
- **本地知识**：“内罗毕的番茄当前价格是多少？”
- **调查与意见收集**：“哪种Logo设计更好？”“请对这条产品描述进行评价”
- **数据收集**：“查找这家餐厅的电话号码”
- **质量保证**：“这段由人工智能生成的文本听起来自然吗？”

**请勿用于**人工智能可以处理的任务：数学计算、代码生成、摘要生成、网络搜索、文件操作等。

## 设置

使用您的Loopuman API密钥创建配置文件：

```bash
mkdir -p ~/.openclaw/skills/loopuman
cat > ~/.openclaw/skills/loopuman/config.json << 'EOF'
{
  "apiKey": "YOUR_API_KEY",
  "apiUrl": "https://api.loopuman.com"
}
EOF
```

获取您的API密钥（只需一个命令，无需身份验证）：

```bash
curl -X POST https://api.loopuman.com/api/v1/register \
  -H "Content-Type: application/json" \
  -d '{"email": "you@example.com", "company_name": "Your Name", "promo_code": "LOBSTER"}'
```

系统会返回您的`api_key`（以`lpm_`开头）。请立即保存它——之后无法重新获取。

**免费信用额的促销代码：**
- `CLAW500`：前10位OpenClaw测试者可获500 VAE（5.00美元）
- `LOBSTER`：早期访问用户可获100 VAE（1.00美元，限50个名额）
- 无代码优惠：25 VAE（0.25美元）欢迎奖励

如需增加资金，请通过Telegram联系[@LoopumanBot](https://t.me/LoopumanBot)并附上您的账户链接。

## API身份验证

所有请求都需要使用`x-api-key`头部：

```
x-api-key: YOUR_API_KEY
```

## 创建任务

```bash
scripts/loopuman.sh create \
  --title "Verify business address" \
  --description "Check if this address exists on Google Maps: 123 Main St, Nairobi, Kenya. Reply with YES/NO and a screenshot." \
  --category other \
  --budget 50 \
  --estimated-seconds 120
```

**参数：**
- `--title`：任务简短标题（必填）
- `--description`：给人类工作者的详细说明（必填，务必具体！）
- `--category`：可选值：`survey`（调查）、`labeling`（标注）、`translation`（翻译）、`writing`（写作）、`research`（研究）、`content_creation`（内容创作）、`ai_training`（人工智能训练）、`micro`（微任务）、`other`（其他）。注意：验证任务使用`other`，审核任务使用`other`，数据收集任务使用`research`。
- `--budget`：以VAE代币支付的预算。100 VAE = 1美元（默认值：100）
- `--estimated-seconds`：预计工作者完成任务所需的时间（用于公平计算报酬，默认值：120秒）
- `--max-workers`：允许使用的工作者数量（默认值：1，最大值：100）
- `--priority`：`normal`（普通）或`high`（高优先级，会立即通知工作者）
- `--webhook`：任务完成后发送推送通知的URL

**各类别的最低预算：**
- `survey`、`labeling`、`ai_training`、`micro`：25 VAE（0.25美元）
- `research`、`content_creation`：75 VAE（0.75美元）
- `writing`、`translation`：100 VAE（1.00美元）

**公平报酬机制：**Loopuman确保最低有效报酬为每小时6美元。如果您的预算除以预计完成时间低于此标准，API会建议提高预算。

**编写良好的任务描述：**
- 明确说明您的需求（例如“回复‘是’或‘否’”，而不是“验证这个”）
- 提供工作者所需的所有背景信息
- 指定预期的回复格式
- 设定明确的成功标准

## 查看任务状态及获取结果

```bash
scripts/loopuman.sh status --task-id <TASK_ID>
```

返回任务的全部详细信息，包括：
- `status`：`active`（活动中）、`completed`（已完成）、`expired`（已过期）、`cancelled`（已取消）
- `progress`：已批准、待审核、进行中的提交数量
- `submissions`：包含工作者提交内容的已批准结果数组
- `pending_submissions`：等待您审核的结果

## 等待任务完成

对于需要先获得结果才能继续的任务：

```bash
# Poll every 30 seconds, timeout after 10 minutes
scripts/loopuman.sh wait --task-id <TASK_ID> --interval 30 --timeout 600
```

一旦有已批准的提交结果，系统会立即返回结果。

## 列出任务

```bash
scripts/loopuman.sh list
```

## 取消任务

```bash
scripts/loopuman.sh cancel --task-id <TASK_ID>
```

如果没有任何工作者开始处理任务，系统会退还您的余额。

## 任务类型及定价

| 类别 | 描述 | 最低预算（VAE） | 完成时间 |
|----------|-------------|-----------------|-------------------|
| `survey` | 快速响应、收集意见 | 25（0.25美元） | 1-5分钟 |
| `labeling` | 标注图片、分类内容 | 25（0.25美元） | 1-5分钟 |
| `micro` | 5秒内的微任务 | 25（0.25美元） | 小于1分钟 |
| `ai_training` | 机器学习训练、偏好排序 | 25（0.25美元） | 1-5分钟 |
| `research` | 查找信息、进行调查 | 75（0.75美元） | 5-20分钟 |
| `content_creation` | 创意内容创作 | 75（0.75美元） | 5-20分钟 |
| `writing` | 文章撰写、描述编写 | 100（1.00美元） | 10-30分钟 |
| `translation` | 语言翻译 | 100（1.00美元） | 5-15分钟 |
| `other` | 自定义任务 | 25（0.25美元） | 根据任务难度而定 |

## 示例

**验证地址：**
“创建一个Loopuman任务，验证‘456 Kenyatta Ave, Nairobi’是否是真实的商业地址”
→ `create --title "Verify address" --description "在Google Maps中查看456 Kenyatta Ave, Nairobi。回复‘是/否’并附上截图。” --category other --budget 30 --estimated-seconds 120`

**带有文化背景的翻译：**
“使用Loopuman将‘We're excited to launch’翻译成斯瓦希里语，并保持自然的语气”
→ `create --title "Translate to Swahili" --description "将‘We're excited to launch’翻译成斯瓦希里语，确保翻译自然，不要显得机械。” --category translation --budget 100 --estimated-seconds 180`

**获取人类意见：**
“询问哪款Logo更专业”
→ `create --title "Logo comparison" --description "哪款Logo更专业？A还是B？请用两句话解释原因。” --category survey --budget 25 --estimated-seconds 60`

**检查AI生成的内容：**
“请人类审核这段由AI生成的产品描述”
→ `create --title "QA check on AI text" --description "这段文本听起来自然且准确吗？如有问题请标记：[粘贴文本]" --category other --budget 30 --estimated-seconds 90`

## 错误处理

| HTTP代码 | 含义 | 解决方法 |
|-----------|---------|-----|
| 401 | API密钥无效 | 请检查config.json中的`x-api-key` |
| 400 | 缺少字段或类别无效 | 请检查必填参数 |
| 402 | VAE余额不足 | 请通过@LoopumanBot充值 |
| 404 | 任务未找到 | 请确认任务ID |
| 429 | 超过请求频率限制 | 请稍后重试 |

## 链接

- 官网：https://loopuman.com
- Telegram机器人：https://t.me/LoopumanBot
- ERC-8004代理：https://www.8004scan.io/agents/celo/17
- MCP：https://api.loopuman.com/.well-known/mcp.json
- A2A：https://api.loopuman.com/.well-known/agent-card.json

⚠️ **预算安全：**任务完成后才会向工作者支付报酬。测试时建议从较低的预算（25-50 VAE）开始。Loopuman会在您设定的预算基础上收取20%的佣金。