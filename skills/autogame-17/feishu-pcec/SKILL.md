# PCEC Feishu 扩展

这是一个用于 `capability-evolver` 的内部封装层，它强制通过 Feishu 交互式卡片（Feishu Interactive Cards）来报告任务执行结果。  
适用于内部调度任务（如 `openclaw.json` 中定义的任务）。

## 使用方法  
```bash
node skills/pcec-feishu/index.js
```