---
name: apprentice
version: "1.0.0"
description: “Watch-me-once”工作流学习机制：只需说出“Watch me”，然后执行某个任务——学徒会观察你的每一个操作步骤，理解你的意图，并将这些步骤转化为一个可重复使用的技能，供你的代理程序永久执行。这是一种通过演示进行编程的方法：无需编写代码，也无需编写详细规格说明。只需执行一次任务即可。该机制可通过以下命令触发：`watch me`、`learn this`、`remember how I do this`、`apprentice mode`、`teach you something`、`run [workflow-name]`、`what have you learned` 或 `replay [workflow-name]`。
homepage: https://github.com/Taha2053/apprentice
metadata:
  clawdbot:
    emoji: "🎓"
    requires:
      env: []
    files:
      - "scripts/*"
      - "workflows/*"
---
# Apprentice — 观看我一次，终身受用。

> 计算技术的整个发展历程都是这样的：你描述你想要什么，然后计算机执行你的指令。
> 但Apprentice颠覆了这一模式：你直接去做你想要做的事情，代理会观察你的行为，然后这些行为就会变成你的一项永久性技能。

这就是**通过示范进行编程**（Programming by Demonstration）——这是人机交互研究领域30年来一直追求的圣杯。以往的所有尝试都失败了，因为这需要受限的环境或严格的正式规范。而大型语言模型（LLM）代理的出现，首次使得以下成为可能：
- 观察你实际的操作（而不仅仅是你说你做了什么）
- 理解你的意图，而不仅仅是你的行为
- 在不同情境中泛化这些行为——区分什么是变量，什么是常量
- 将这些行为转化为可重复的工作流程，代理可以不断运行、优化并串联起来。

---

## 外部端点

| 端点 | 用途 | 发送的数据 |
|---|---|---|
| 无 | 完全本地化 | 没有任何数据会离开你的机器 |

Apprentice的所有记录都保存在本地。所有的合成操作都是通过你已运行的LLM会话完成的，不需要任何外部API。

---

## 安全性与隐私

- **零外部调用**。观察日志、工作流程文件以及所有合成结果都保存在本地。
- **不访问任何凭证**。Apprentice只会观察你告诉它的内容，不会读取系统文件或在操作系统层面拦截你的输入。
- **保存前需审核**。观察完成后，你可以查看合成的工作流程，并在保存之前进行审核。
- **沙箱式执行**。生成的工作流程脚本都是使用标准的bash命令，并带有完整的安全权限信息。

> **信任声明：**Apprentice会将观察日志和工作流程的SKILL.md文件保存在本地。它发送给LLM的唯一信息就是你描述的观察内容以及合成请求，这些数据都来自你现有的会话。没有任何数据会离开你的机器。

---

## 模型调用说明

Apprentice的操作始终由用户发起。当你说出“观看我”或类似的指令时，它才会启动；在你说“完成”或“停止观察”之前，它会保持暂停状态。每次观察结束后，Apprentice会生成一个工作流程，并在你审核后才会保存。

---

## 工作原理

### 第一步 — 开始观察
```
"Watch me"
"Apprentice mode on"
"Learn this"
"I'm going to show you how I do X"
```
代理进入观察模式，开始监听并记录你的操作。

### 第二步 — 自然地完成任务
只需像平常一样去做事情。如果需要的话，可以大声说出来——代理会听到你说的每一句话：
- 你运行的命令
- 你创建、编辑或删除的文件
- 你的决策及其原因
- 操作的顺序
- 不同运行之间的变化（变量）和保持不变的部分（常量）

你不需要使用正式的语言。例如：“现在我要用项目名称更新README文件”就足够了。

### 第三步 — 停止观察
```
"Done"
"Stop watching"
"That's it"
"End observation"
```

### 第四步 — 审查合成结果
Apprentice会向你展示：
- 它理解的工作流程名称和用途
- 它识别出的操作步骤（按顺序排列）
- 它检测到的变量（每次运行都会变化的部分）
- 常量（每次运行都保持不变的部分）
- 生成的工作流程（以可读、可编辑的形式呈现）

你可以批准、修改或拒绝这个工作流程。

### 第五步 — 成为永久性技能
被批准的工作流程会被保存在`apprentice/workflows/`目录下，文件名为SKILL.md。你的代理可以使用这些工作流程来完成相应的任务：
```
"Run new-project-setup"
"Do the weekly-report workflow with topic=AI agents"
"What workflows have you learned?"
"Replay morning-routine"
```

---

## 示例：学习一个工作流程

**用户：**
```
"Watch me set up a new project"
```

**[用户按照步骤操作，并大声说明每一步：】
```
mkdir projects/client-dashboard
cd projects/client-dashboard
cp -r ~/templates/react-starter .
sed -i 's/TEMPLATE_NAME/client-dashboard/g' README.md
git init
git remote add origin git@github.com:myorg/client-dashboard.git
echo "Done — this is how I start every new project"
```

**用户：**
```
"Done watching"
```

**Apprentice的回应：**
```
🎓 OBSERVATION COMPLETE — New Project Setup

What I understood:
  Purpose: Initialize a new project from template
  Name: "new-project-setup"

Steps I identified:
  1. Create project directory with name [PROJECT_NAME]
  2. Copy React starter template
  3. Replace template placeholder with [PROJECT_NAME] in README
  4. Initialize git repository
  5. Add remote origin at [GIT_ORG]/[PROJECT_NAME]

Variables (change each run):
  • PROJECT_NAME — the new project's name
  • GIT_ORG — your GitHub organization (default: myorg)

Constants (same every time):
  • Template source: ~/templates/react-starter
  • README placeholder: TEMPLATE_NAME

Does this look right? [Accept] [Edit] [Reject]
```

**下次操作：**
```
"New project setup for analytics-redesign"
→ Agent runs all 5 steps automatically with PROJECT_NAME=analytics-redesign
```

---

## 触发指令

**开始观察：**
```
"Watch me"
"Apprentice mode"
"Learn this"
"Remember how I do this"
"I'll show you"
"Teaching you something"
```

**停止观察：**
```
"Done"
"Stop watching"
"That's it"
"Got it?"
"End lesson"
```

**运行工作流程：**
```
"Run [workflow-name]"
"Do the [workflow-name] workflow"
"[workflow-name] with [variable=value]"
"Replay [workflow-name]"
```

**管理工作流程：**
```
"What have you learned?"
"Show me my workflows"
"Delete [workflow-name]"
"Edit [workflow-name]"
"When did you learn [workflow-name]?"
```

---

## 如何进行有效的观察

你提供的上下文信息越多，Apprentice的理解就越准确：
✅ **大声说出来**——例如：“现在我要用新的API密钥更新配置文件”
✅ **解释原因**——例如：“我在部署之前总是这样做，因为测试环境需要不同的变量”
✅ **为变量命名**——例如：“这里的PROJECT_NAME每次都会变化”
✅ **标记操作的结束**——例如：“这就是整个工作流程”

❌ **无声的操作**——Apprentice只能通过你的对话来学习
❌ **仅依赖GUI的任务**——Apprentice只能根据你的描述来执行操作；它无法观察你的屏幕

---

## 工作流程文件

每个学习到的工作流程都会保存在`apprentice/workflows/<名称>/`目录下：

```
apprentice/workflows/new-project-setup/
├── SKILL.md          ← The learned workflow (OpenClaw-compatible)
├── run.sh            ← Generated execution script
└── observation.json  ← Raw observation log (editable)
```

生成的SKILL.md文件是一个完整的、有效的OpenClaw技能。这意味着：
- 其他技能可以调用这个工作流程
- 你可以手动编辑它以进行优化
- 你可以将其发布到ClawHub与他人分享

---

## 连接多个工作流程

当你拥有多个工作流程后，Apprentice可以将其串联起来使用：

```
"After running new-project-setup, also run notify-team"
→ Agent chains both workflows in sequence

"If the deploy workflow fails, run rollback-staging"
→ Conditional chaining with error handling
```

---

## 工作流程库

使用一段时间后，你的工作流程库就会变成你的“个人操作系统”——一个记录了你日常操作习惯的库。例如：
- `morning-routine`：你每天早晨的例行事务
- `new-project-setup`：你启动新项目的方式
- `weekly-report`：你编写和发送周五总结报告的流程
- `client-onboarding`：新客户加入时你执行的步骤
- `deploy-staging`：你的部署流程
- `code-review-prep`：你在审阅代码提交（PR）之前的准备工作

没有两个用户的流程库是完全相同的。这个代理完全是由你的行为塑造的。

---

## 文件结构

```
apprentice/
├── SKILL.md                         ← You are here
├── README.md                        ← Install guide
├── scripts/
│   ├── observe.py                   ← Observation session manager
│   ├── synthesize.py                ← Turns observation into workflow SKILL.md
│   └── run.py                       ← Executes a named workflow
└── workflows/                       ← Your learned workflow library
    └── (empty on install, grows with you)
```

---

## 设计理念

所有工具都会问：“你想要什么？”
Apprentice则会问：“我可以观察你的操作吗？”

这两者的区别至关重要。当你只是描述你的需求时，很多细节就会丢失——操作的顺序、边缘情况以及“我总是先做这个”的习惯都很重要。而当Apprentice观察你的实际操作时，它会准确地记录下所有这些细节。

你的代理并不会因为训练了更多的数据而变得更聪明。它之所以变得更聪明，是因为它通过观察你而学习到了这些知识。