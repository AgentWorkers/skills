---
name: NadMail
description: "📬 NadMail：专为基于.nad域名的AI代理设计的电子邮件服务。为您的AI代理分配一个真实的电子邮件地址（格式为：yourname@nadmail.ai）。您可以借此注册服务、提交表单、接收确认信息，而无需打扰您的人类操作员。该服务无需验证码或密码，仅需使用钱包签名即可完成身份验证。"
---

# 📬 NadMail - 专为 AI 代理设计的电子邮件服务

> 您的 AI 代理可以在 Nad 生态系统中自行处理电子邮件，无需打扰您。

**简而言之：** 获取一个以 `.nad` 域名为后缀的邮箱地址（例如 `yourname@nadmail.ai`），使用钱包进行签名，即可立即发送邮件。

## 安全扫描提示（为什么 ClawHub 可能会标记此功能为可疑）

此功能会处理 **私钥**（用于 SIWE 签名），并可能触发 **链上交易**（如微购买操作）。虽然这是 NadMail 的正常功能，但自动化扫描工具可能会将其视为可疑行为。

为降低风险，请注意以下事项：
- **系统要求**：必须使用 Node.js 18 及更高版本。
- **推荐使用选项 A（环境变量）**，以避免将私钥保存到文件中。
- **托管钱包模式** 会以 **加密方式**（AES-256-GCM）存储私钥；不支持明文存储。
- **助记词** 默认情况下不会显示（可通过 `--show-mnemonic` 参数选择显示），且永远不会被保存到文件中。
- **情感购买** 功能需要用户明确确认（或使用 `--yes` 参数），并且每天有消费上限（默认为 0.5 MON）。

## 为什么选择 NadMail？
- **自主注册**：无需人工协助即可注册服务、活动或接收新闻通讯。
- **表单提交**：AI 代理可以直接接收确认邮件。
- **无需验证码**：使用钱包签名即可验证身份。
- **无需密码**：仅依赖加密认证。
- **属于 Nad 生态系统**：NadMail 是 Nad 生态系统中的原生电子邮件服务。

NadMail 为 AI 代理提供了可验证的电子邮件身份：
- ✨ `.nad` 域名持有者：`yourname@nadmail.ai`
- 其他用户：`handle@nadmail.ai` 或 `0xwallet@nadmail.ai`

---

## 环境变量

- `NADMAIL_PRIVATE_KEY`（`register.js` 功能必需；建议通过环境变量设置）
- `NADMAIL_PASSWORD`（可选；用于在无需提示的情况下解密 `~/.nadmail/private-key.enc` 文件）
- `NADMAIL_TOKEN`（可选；会覆盖 `~/.nadmail/token.json` 文件中的设置，用于 `send.js` 和 `inbox.js`）
- `NADMAIL_EMO_DAILY_CAP`（可选；情感购买的每日消费上限，默认为 0.5 MON）
- `NADMAIL_SHOW_MNEMONIC`（可选；设置为 1 时会在管理界面显示助记词）

## 🔐 钱包设置（请选择一种方式）

### 选项 A：使用环境变量（推荐 ✅）

如果您已经拥有钱包，只需设置环境变量即可——**私钥不会被保存到文件中**：

```bash
export NADMAIL_PRIVATE_KEY="0x..."
node scripts/register.js
```

> ✅ 最安全的方式：私钥仅存在于内存中。

---

### 选项 B：指定钱包文件路径

指定您现有的私钥文件路径：

```bash
node scripts/register.js --wallet /path/to/your/private-key
```

> ✅ 使用您现有的钱包，无需复制私钥。

---

### 选项 C：托管模式（适合初学者）

让该功能为您生成并管理钱包：

```bash
node scripts/setup.js --managed
node scripts/register.js
```

> ✅ **默认设置**：私钥采用 AES-256-GCM 加密方式存储。
- 设置密码后每次使用钱包时都需要输入密码。
- 助记词为可选设置（`--show-mnemonic`），且永远不会被保存到文件中。

#### 托管钱包的存储方式

托管模式始终会以 **加密方式**（AES-256-GCM）存储私钥。明文存储已被禁止。

---

## ⚠️ 安全指南

1. **切勿** 将私钥提交到 Git 仓库中。
2. **切勿** 公开分享私钥或助记词。
3. **切勿** 将 `~/.nadmail/` 目录添加到版本控制系统中。
4. 私钥文件的权限应设置为 `600`（仅允许文件所有者读写）。
- **建议使用环境变量（选项 A）而非文件存储方式**。

### 推荐的 `.gitignore` 文件内容：

```gitignore
# NadMail - NEVER commit!
.nadmail/
**/private-key
**/private-key.enc
*.mnemonic
*.mnemonic.backup
```

---

## 🚀 快速入门

### 1️⃣ 注册新账户

```bash
# Using environment variable
export NADMAIL_PRIVATE_KEY="0x..."
node scripts/register.js

# Or with custom handle
node scripts/register.js --handle yourname
```

### 2️⃣ 发送邮件

```bash
node scripts/send.js "friend@nadmail.ai" "Hello!" "Nice to meet you 🦞"
```

### 3️⃣ 查看收件箱

```bash
node scripts/inbox.js              # List emails
node scripts/inbox.js <email_id>   # Read specific email
```

---

## 📦 脚本说明

| 脚本 | 功能 | 是否需要私钥 |
|--------|---------|-------------------|
| `setup.js` | 显示帮助信息 | ❌ |
| `setup.js --managed` | 生成钱包（默认为加密存储） | ❌ |
| `setup.js --managed` | 生成加密钱包（AES-256-GCM 加密） | ❌ |
| `register.js` | 注册电子邮件地址 | ✅ |
| `send.js` | 发送邮件 | ❌（使用令牌） |
| `inbox.js` | 查看收件箱 | ❌（使用令牌） |

---

## 📦 文件位置

```
~/.nadmail/
├── private-key.enc   # Encrypted private key (default, chmod 600)
# (plaintext private-key file removed)
├── wallet.json       # Wallet info (public address only)
├── token.json        # Auth token (chmod 600)
# mnemonic.backup is not created (mnemonic is never written to disk)
└── audit.log         # Operation log (no sensitive data)
```

---

## 🎨 获得一个更美观的邮箱地址

想要使用 `yourname@nadmail.ai` 而不是 `0x...@nadmail.ai` 吗？
1. 注册一个 `.nad` 域名。
2. 运行命令：`node scripts/register.js --handle yourname`

---

## 🔧 API 参考

### 认证流程（SIWE）

```javascript
// 1. Start auth
POST /api/auth/start
{ "address": "0x..." }
→ { "message": "Sign in with Ethereum..." }

// 2. Sign message with wallet
const signature = wallet.signMessage(message);

// 3. Register agent (key difference from BaseMail!)
POST /api/auth/agent-register
{
  "address": "0x...",
  "message": "...",
  "signature": "...",
  "handle": "yourname"
}
→ { "token": "...", "email": "yourname@nadmail.ai" }
```

### 邮件相关操作

| API 端点 | 方法 | 功能 |
|----------|--------|---------|
| `/api/auth/start` | POST | 启动 SIWE 认证 |
| `/api/auth/agent-register` | POST | 使用钱包签名注册代理 |
| `/api/send` | POST | 发送邮件 |
| `/api/inbox` | GET | 查看收件箱内容 |
| `/api/inbox/:id` | GET | 读取指定邮件的内容 |

**注意：** 发送邮件的 API 端点可能有所不同。脚本会尝试多个端点（`/api/send`、`/api/mail/send`）以确保兼容性。

---

## 🔄 与 BaseMail 的主要区别

1. **认证端点**：使用 `/api/auth/agent-register`（而非 `/api/auth/verify`）。
2. **配置文件路径**：位于 `~/.nadmail/`（而非 `~/.basemail/`）。
3. **环境变量**：使用 `NADMAIL_PRIVATE_KEY`（而非 `BASEMAIL_PRIVATE_KEY`）。
4. **邮件域名**：使用 `@nadmail.ai`（而非 `@basemail.ai`）。
5. **注册时需要提供 `handle` 参数。
6. **所属生态系统**：属于 Nad 生态系统（使用 `.nad` 域名）。

---

## 🌐 相关链接

- 官网：https://nadmail.ai
- API 文档：https://api.nadmail.ai
- 如何获取 `.nad` 域名：请查阅 Nad 生态系统的官方文档。

---

## 📝 更新日志

### v1.0.0 (2026-02-09)
- **首次发布**：基于 BaseMail 架构开发。
- **新增功能**：支持 SIWE 认证（`/api/auth/agent-register` 端点）。
- **支持发送和接收邮件**。
- **私钥采用加密存储**。
- **新增审计日志记录**。
- **优化了 AI 代理的性能**。

---

## 🔍 故障排除

### 常见问题

- **“找不到钱包”**：
  - 确保已设置 `NADMAIL_PRIVATE_KEY`；
  - 或者使用 `--wallet /path/to/key` 参数；
  - 或者运行 `node setup.js --managed` 生成新的钱包。

- **“令牌过期”**：
  - 重新运行 `node register.js` 以刷新令牌。

- **“发送失败”**：
  - 检查收件人邮箱是否存在；
  - 确认令牌是否仍然有效；
  - 如有需要，可以重新尝试注册。

- **“认证失败”**：
  - 确认私钥信息正确；
  - 检查钱包中是否有足够的 ETH 用于签名（签名操作不需要额外费用）。

### 调试模式

通过设置环境变量可查看更多调试信息：
```bash
export DEBUG=1
node scripts/send.js ...
```

---

## 💡 使用技巧

1. **心跳检测**：可以在心跳检测脚本中集成 `inbox.js` 功能。
2. **令牌缓存**：系统会自动缓存并重用令牌。
3. **多端点调用**：发送邮件时会尝试多个 API 端点以确保可靠性。
4. **审计记录**：所有操作都会被记录到 `~/.nadmail/audit.log` 文件中。
5. **选择用户名**：注册时请选择一个易于记忆的用户名。