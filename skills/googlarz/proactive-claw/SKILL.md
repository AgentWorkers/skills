---
name: proactive-claw
version: 1.2.0
description: >
  将AI代理转变为受管控的执行伙伴，它们能够理解你的工作内容、监控你的工作环境，并根据你的需求进行预测性操作——所有行动都在你的控制之下。🧠⚙️🦞
  **本地优先自动化（LOCAL-FIRST AUTOMATION）**：该功能仅能读取你的日历（仅限读取权限），并将数据写入自身的“Proactive Claw — Actions”日历中。它会以15分钟的周期运行一个用户级别的后台守护进程，所有决策都会被记录在本地。**安全默认设置：大部分功能默认处于关闭状态。**
  **两种运行模式：**  
  1. **守护进程模式（Daemon mode）**：在后台运行，每15分钟执行一次计划（PLAN→EXECUTE）操作。该模式会收集用户事件、检测事件删除情况、自动重新链接被移动的事件、规划提醒事项并触发相应的操作。该模式会读取所有用户的日历（仅限读取权限），并将操作结果写入日历及SQLite数据库中。  
  2. **对话模式（Conversation mode）**（可选，需要单独启用相应的LLM）：在与你进行对话时，Claude Code可以选择性地调用proactive-claw脚本来读取你的日历信息、提议日程变更或记录对话结果——但这一切都需要你在每次对话中明确授权。  
  **管控层（Governance layer）**：统一的主动行为引擎整合了能量管理、通知机制、策略设置以及用户关系信息。系统支持不同的优先级等级（P0–P5），并设有静默时段、操作冷却时间限制以及每日操作次数的上限。用户还可以调整系统的整体活跃程度（低/平衡/高级），并设定操作自主性的上限（建议/确认/完全自主）。该系统会记录每一项决策的详细过程，以确保操作的透明性和可解释性。  
  **内存与学习机制（Memory & Learning）**：采用衰减加权评分算法（最近的数据权重更高），通过SQLite数据库将用户事件与计划中的操作关联起来。系统还能检测策略冲突，并定期进行系统健康检查。  
  **产品化功能（Productization features）**：提供配置向导、数据导出/备份功能、每月的数据变化监控机制以及模拟模式。此外，系统还支持软取消（soft-cancel）操作。  
  **系统要求（System requirements）**：需要安装python3，并使用Google OAuth凭证（或Nextcloud应用密码）进行登录。  
  **可选功能（All optional, default disabled）：**  
  - gh CLI（用于跨技能功能交互，feature_crossSkill）  
  - NOTION_API_KEY（用于访问Notion API，feature_cross_skill）  
  - Telegram机器人令牌（用于与Telegram集成，feature_telegram）  
  - LLM_RATER_API_KEY（用于评估LLM模型的性能，feature_llm_rater）  
  - Apple Notes osascript（仅适用于macOS系统，用于将操作结果保存到Apple Notes中，notes_destination=apple-notes）  
  - Notion结果数据库（用于存储对话记录，notes_destination=notion）  
  **安全默认设置（Safe defaults）：**  
  - feature_cross_skill=false  
  - feature_voice=false  
  - feature_team_awareness=false  
  - feature_llm_rater=false  
  - max_autonomy_level=confirm （即操作完全由用户确认后执行，非自主模式）
requires:
  bins:
    - python3
  env_vars: []
  credentials:
    - Google OAuth credentials (via setup.sh) OR Nextcloud app password (via setup.sh)

install:
  - kind: script
    label: "One-time setup — creates calendar, installs dependencies, configures daemon"
    command: "bash scripts/setup.sh"

side_effects:
  - Installs a user-level background daemon (launchd on macOS, systemd user timer on Linux) via install_daemon.sh. Runs every 15 min. Does NOT run as root. Uninstall instructions in SKILL.md.
  - Writes local files under ~/.openclaw/workspace/skills/proactive-agent/ only. No files written outside this directory.
  - Creates a "Proactive Claw — Actions" calendar in Google/Nextcloud. Never modifies your existing calendars — reads them only.
  - Maintains a SQLite link graph (proactive_links.db) tracking connections between your events and planned actions.
  - Outbound HTTPS to Google Calendar API only by default. Notion, Telegram, GitHub, clawhub.ai, LLM rating API are all opt-in via feature_* flags in config.json.
  - pip installs google-api-python-client, google-auth-oauthlib, google-auth-httplib2 (Google backend) or caldav, icalendar (Nextcloud backend) during setup.sh.
---
# 🦞 Proactive Claw v1.2.0

> 将AI代理转变为受管控的执行伙伴，它们理解您的工作，监控您的日程安排，并在您的控制下提前采取行动——以预测性方式执行。

---

## 🏗️ 架构 — 2个日历 + 链接图

**您的日历** 是只读的，永远不会被修改。所有操作都会被记录到一个名为“Proactive Claw — Actions”的日历中，您可以在日历应用程序中看到这个日历。事件通过SQLite图谱进行关联，因此当源事件移动或被删除时，操作仍然保持同步。

### 守护进程周期：计划 → 执行

每15分钟：

1. **计划**：获取用户事件，检测删除事件，自动重新关联移动的事件，计划提醒/准备/缓冲/总结操作
2. **执行**：以幂等方式执行到期的操作（在发送前检查`sent_actions`表）
3. **清理**：每天一次：将暂停/取消的事件重命名为`🦞 [Paused] ...`，删除超过`action_cleanup_days`天的已取消条目

---

## 🔒 安全性与隐私

### ⚠️ 重要提示：在安装之前

1. **首先查看设置脚本：**
   两个脚本都是普通的shell脚本。确认它们只写入`~/.openclaw/`目录，并创建用户级别的定时器（而不是root服务）。

2. **从安全默认设置开始：**
   - **不要**直接将`config.example.json`复制到`config.json`
   - 运行`python3 config_wizard.py`进行引导式设置
   - 或者手动创建`config.json`，并将`max_autonomy_level`设置为`confirm`（而不是`autonomous`
   - 确保所有`feature_*`都设置为`false`，除非您明确需要它们

3. **关于凭证：**
   - Google：使用标准的OAuth桌面流程（setup.sh负责处理）
   - **除非您信任clawhub.ai**，否则不要使用`clawhub_token`
   - Nextcloud：生成应用程序特定的密码（永远不要使用您的账户密码）
   - 所有外部API（Telegram、Notion、GitHub、LLM）：仅在启用相应功能时提供令牌

4. **在模拟模式下测试：**
   ...

### 安全保障

| 内容 | 详情 |
|------|--------|
| **凭证保持本地** | `credentials.json`、`token.json`、`config.json`仅存储在`~/.openclaw/workspace/skills/proactive-agent/`目录中。永远不会上传到其他地方。 |
| **仅使用用户级别的守护进程** | `install_daemon.sh`创建用户级别的定时器（macOS上使用launchd，Linux上使用systemd用户服务）。仅以用户身份运行——永远不会以root权限运行。卸载：`launchctl unload ~/Library/LaunchAgents/ai.openclaw.proactive-agent.plist && rm ~/Library/LaunchAgents/ai.openclaw.proactive-agent.plist`（macOS）或`systemctl --user disable --now openclaw-proactive-agent.timer && rm ~/.config/systemd/user/openclaw-proactive-agent.*`（Linux） |
| **日历写入是隔离的** | 仅写入它创建的“Proactive Claw — Actions”日历。读取您的其他日历，但永远不会修改它们。 |
| **网络调用受到限制** | 所有外部网络调用都需要明确的配置或功能标志。默认只使用Google Calendar API（核心功能）。Notion、Telegram、GitHub、LLM服务都是可选的。 |
| **Nextcloud密码** | 仅使用应用程序特定的密码。在`your-nextcloud.com/settings/personal/security`生成。永远不要存储您的账户密码。 |
| **默认配置是安全的** | `max_autonomy_level`默认设置为`confirm`（执行前会询问用户）。所有外部功能默认设置为`false`。 |
| **检查脚本** | `setup.sh`和`install_daemon.sh`都是普通的shell脚本——没有混淆的下载内容，没有root命令。在运行之前请阅读它们。 |
| **clawhub OAuth范围** | `clawhub_token`（可选）仅下载OAuth客户端定义（`credentials.json`）。您的个人Google令牌（`token.json`）在本地浏览器中生成，永远不会发送到clawhub.ai。 |
| **LLM评分器优先使用本地服务** | 默认使用`localhost`上的Ollama——不需要API密钥。 |

> **本地LLM = 无外部调用。** 使用`base_url: http://localhost:11434/v1`（Ollama）或`http://localhost:1234/v1`（LM Studio）时，不会有任何数据离开您的机器。

**没有其他数据**。没有分析数据，没有遥测信息，也不会将任何数据发送给技能的创建者。

---

## ✨ 特性概览

| 编号 | 特性 | 描述 |
|---|---------|-------------|
| 1 | 对话雷达 | 每次交流后默默地给出0-10分的评分 |
| 2 | 日历监控 | 扫描日历 + 检测冲突 + 识别可操作的事件 |
| 3 | 后台守护进程 | 每15分钟执行计划→执行→清理周期 |
| 4 | SQLite内存 | 使用TF-IDF进行语义搜索的事件历史记录 |
| 5 | 跨技能智能 | 支持GitHub和Notion的上下文（可选） |
| 6 | 自然语言规则 | 用户定义的规则引擎 |
| 7 | 事件后智能 | 提供跟进、每周摘要和季度洞察 |
| 8 | 日历策略引擎 | 自动准备/聚焦/缓冲/取消操作 |
| 9 | 多代理协调 | 完整的事前准备流程 |
| 10 | 能量预测 | 使用衰减权重进行预测性时间安排 |
| 11 | 日历编辑 | 移动日历事件、查找空闲时间、清除事件、用简单语言显示信息 |
| 12 | 关系记忆 | 来自参与者和事件结果的轻量级CRM |
| 13 | 以语音为主 | 集成Whisper功能 + 意图路由 |
| 14 | 自适应通知 | 自动调整通知渠道 + 根据时间进行优化 |
| 15 | 团队意识 | 可选的功能，支持跨日历协调 |
| 16 | LLM交互评分器 | 本地模型评估签到质量 |
| 17 | **主动性引擎** | 统一的评分机制：结合能量、通知、策略和关系信号 |
| 18 | **中断管理** | 分为P0-P5五个优先级，每天最多提醒次数，有冷却时间限制 |
| 19 | **可解释性模式** | 可追踪每个提醒/策略/能量决策的来源 |
| 20 | **主动性强度调节** | 可调节的评分机制 |
| 21 | **最大自主性限制** | 全局性的建议/确认/自主权限 |
| 22 | **记忆衰减** | 所有模块都采用指数衰减权重 |
| 23 | **系统健康检查** | 7项诊断检查：数据库、守护进程、配置、日历、标志、数据过期情况、磁盘状态 |
| 24 | **策略冲突检测** | 检测相互矛盾的策略 |
| 25 | **配置向导** | 提供交互式的CLI设置界面 |
| 26 | **模拟模式** | 在未来N天内模拟守护进程的运行 |
| 27 | **安静时段** | 在安静时段抑制非安全提醒 |
| 28 | **数据导出/导入** | 提供JSON/CSV格式的备份和恢复功能 |
| 29 | **趋势监控** | 提供每月的行为报告和差异警报 |
| 30 | **2个日历架构** | 仅读取用户的日历，所有操作都写入“Proactive Claw — Actions”日历 |
| 31 | **链接图谱** | 使用SQLite图谱连接用户事件和计划中的操作 |
| 32 | **操作规划器** | 在计划阶段：获取事件、检测缺失事件、自动重新关联事件 |
| 33 | **操作执行器** | 在执行阶段：以幂等方式执行到期的操作 |
| 34 | **删除检测** | 通过指纹匹配检测移动事件，并确认/取消操作 |
| 35 | **软取消策略** | 重命名已取消的事件，并在N天后清理记录 |

---

## 🛠️ 设置（只需运行一次）

### 选项A — clawhub OAuth（推荐，适用于移动设备）

1. 访问https://clawhub.ai/settings/integrations → 连接Google日历 → 复制您的token
2. 在`config.json`中设置`"clawhub_token": "your-token-here"`
3. 运行`setup.sh` — 凭证信息会自动下载，无需使用Google Cloud Console

### 选项B — 手动输入Google凭证

1. 访问https://console.cloud.google.com → 新项目 → 启用Google Calendar API
2. 创建OAuth 2.0凭证（适用于桌面应用） → 下载JSON文件
3. 将`credentials.json`文件移动到`~/Downloads/credentials.json`到`~/openclaw/workspace/skills/proactive-agent/`
4. 运行`setup.sh`

### 选项C — Nextcloud CalDAV

> ⚠️ 使用Nextcloud **应用程序特定的密码**，而不是您的账户密码。在`your-nextcloud.com/settings/personal/security`生成密码。该密码仅存储在您机器上的`config.json`文件中。

运行`setup.sh` — 连接日历，创建“Proactive Claw — Actions”日历，并保存URL。

### 安装后台守护进程

- **macOS**：安装launchd plist文件，每15分钟自动运行一次
- **Linux**：安装systemd用户定时器
- 日志文件：`~/.openclaw/workspace/skills/proactive-agent/daemon.log`

### 将现有结果数据迁移到SQLite

---

### 交互式配置向导（可选）

---

## ⚙️ 配置

`~/.openclaw/workspace/skills/proactive-agent/config.json`

直接编辑此文件以更改设置。仅修改右侧列中的值——不要更改键或结构。

| 关键 | 默认值 | 描述 |
|-----|---------|-------------|
| `calendar_backend` | `google` | `google`或`nextcloud` |
| `timezone` | `UTC` | IANA时区，例如`Europe/Berlin` |
| `daemon_interval_minutes` | `15` | 守护进程扫描的频率 |
| `proactivity_mode` | `balanced` | `low`、`balanced`、`executive` — 控制评分系数和每天最多提醒次数 |
| `max_autonomy_level` | `autonomous` | `advisory`（仅建议），`confirm`（询问用户后执行），`autonomous`（自动执行） |
| `quiet_hours` | `{"weekdays":"22:00-07:00","weekends":"21:00-09:00"}` | 在这些时间段内抑制非安全提醒 |
| `memory_decay_half_life_days` | `90` | 指数衰减权重的半衰期 |
| `max_nudges_per_day` | `12` | 每天的提醒次数上限 |
| `nudge_cooldown_minutes` | `30` | 被忽略的提醒之间的最小间隔时间 |
| `watched_calendars` | `[]` | 需要监控的日历ID（空表示除了行动日历之外的所有日历） |
| `ignored_calendars` | `[]` | 需要忽略的日历ID |
| `action_cleanup_days` | `30` | 被取消的事件在多少天后被删除 |
| `notification_channels` | `["openclaw","system"]` | `openclaw`、`system`、`telegram` |
| `telegram_bot_token` | `""` | Telegram机器人令牌 |
| `telegram.chat_id` | `""` | 您的Telegram聊天ID |
| `clawhub_token` | `""` | 来自clawhub.ai/settings/integrations的令牌 |
| `notes_destination` | `local` | `local`、`apple-notes`（macOS osascript）、`notion` |

### 特性开关

所有本地功能默认都是开启的。面向外部的功能默认是关闭的。

| 开关 | 默认值 | 描述 |
|------|---------|-------------|
| `feature_conversation` | `true` | 对话雷达评分 |
| `feature_calendar` | `true` | 日历扫描 |
| `feature_daemon` | `true` | 后台守护进程 |
| `feature_memory` | `true` | 使用SQLite内存 |
| `feature_conflicts` | `true` | 检测冲突 |
| `feature_rules` | `true` | 规则引擎 |
| `feature_intelligence_loop` | `true` | 提供跟进和摘要 |
| `feature_policy_engine` | `true` | 日历策略 |
| `feature_orchestrator` | `true` | 多代理协调 |
| `feature_energy` | `true` | 能量预测 |
| `feature_cal_editor` | `true` | 日历编辑 |
| `feature_relationship` | `true` | 关系管理功能 |
| `feature_adaptive_notifications` | `true` | 自适应通知 |
| `feature_proactivity_engine` | `true` | 统一的主动性评分 |
| `featureInterrupt_controller` | `true` | 中断管理 |
| `feature_explainability` | `true` | 提供决策追踪 |
| `feature_health_check` | `true` | 系统诊断 |
| `feature_simulation` | `true` | 模拟模式 |
| `feature_export` | `true` | 数据导出/导入 |
| `feature_behaviour_report` | `true` | 提供行为报告 |
| `feature_config_wizard` | `true` | 提供配置向导 |
| `feature_policy_conflict_detection` | `true` | 检测策略冲突 |
| `feature_cross_skill` | `false` | 支持GitHub/Notion的上下文（外部功能） |
| `feature_voice` | `false` | 需要Whisper技能才能使用语音功能 |
| `feature_team_awareness` | `false` | 支持跨日历的团队协调（外部功能） |
| `feature_llm_rater` | `false` | 如果使用云服务，则使用LLM评分器（外部功能） |

### LLM评分器配置

| 关键 | 默认值 | 描述 |
|-----|---------|-------------|
| `llm_rater.enabled` | `false` | 启用评分器 |
| `llm_rater.base_url` | `http://localhost:11434/v1` | LLM端点（Ollama默认值——本地，无需输入密钥） |
| `llm_rater.model` | `qwen2.5:3b` | 模型名称 |
| `llm_rater.api_key_env` | ```` | 用于存储API密钥的环境变量（空表示不需要密钥，适用于本地使用） |
| `llm_rater.timeout` | `30` | 请求超时时间（以秒为单位） |

---

## 特性1 — 对话雷达

每次交流后都会默默地给出0-10分的评分。在达到阈值时，会简短地询问用户。

| 分数 | 信号含义 |
|---------|--------|
| +3 | 明确的未来事件 |
| +3 | 正在进行的准备任务 |
| +2 | 重要/紧急的任务 |
| +2 | 截止日期临近 |
| +1 | 需要跟进的任务 |
| −2 | 假设性或历史性的任务 |

**在询问之前**，先检查守护进程是否有待处理的提醒：
如果存在提醒，会优先显示最紧急的任务，而不是再次询问用户。