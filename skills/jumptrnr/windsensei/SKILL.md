---
name: windsensei
description: 检查适合风帆运动（如风筝冲浪、翼装飞行、冲浪）的风力和天气状况。获取天气预报，查找附近的适合活动地点，查看过往的活动记录，并请求新的活动地点。
homepage: https://windsensei.com
source: https://github.com/jumptrnr/spotsensei
user-invocable: true
metadata: {"emoji": "🪁", "optionalEnv": "WINDSENSEI_API_KEY"}
---
# WindSensei - 风向预测助手

您可以查看您喜欢地点的风况，并根据天气情况采取相应行动。

## 何时使用此技能

当用户询问以下内容时，请激活此技能：
- 风况、风力预测或适合风运动的天气信息
- “风怎么样？”、“我应该去放风筝吗？”、“这个周末有适合的风吗？”
- “[地点名称] 的天气如何？”、“风力报告”
- 放风筝、风筝冲浪、翼装冲浪或冲浪的天气条件
- 根据风力安排时间或向日历中添加活动
- 查找附近的地点或探索新地点
- 活动历史记录、统计数据或活动日志
- 了解哪些朋友正在活动（实时情况）

## 配置

### 凭据

- `WINDSENSEI_API_KEY`：一个以 `ss_` 开头的 API 密钥。**可选——即使没有这个密钥，该技能也能正常使用。** 没有密钥时，您仍然可以查询任何公共地点的名称、搜索地点以及查找附近的地点。拥有密钥后，您可以获得个性化预测、仪表盘概览、活动历史记录和社交功能。
- **不需要其他凭据。** 该技能仅向 WindSensei API 发送 HTTPS 请求。日历功能（如果用户请求）会使用主机代理自己的日历工具——该技能不会请求或存储任何日历凭据。

### 获取 API 密钥

如果用户需要个性化预测，请指导他们：
1. 在 **windsensei.com** 注册
2. 添加他们喜欢的地点并设置活动偏好
3. 访问他们的 **个人资料页面**（地址：`windsensei.com/dashboard/profile`）——API 密钥管理器位于该页面的底部
4. 点击 “创建 API 密钥”，为其命名（例如 “Claude” 或 “OpenClaw”，然后复制密钥
5. 密钥以 `ss_` 开头——将其设置为代理的环境配置中的 `WINDSENSEI_API_KEY`

**直接链接：** `https://windsensei.com/dashboard/profile`（滚动到 “API 密钥” 部分）

## 两种模式

### 公共模式（无需 API 密钥）
- 使用 `spot` 参数查询任何公共地点的名称
- 按名称搜索地点
- 根据纬度/经度查找附近的地点
- 使用默认的风筝放飞偏好（12-40 公里/小时，上午 8 点至晚上 8 点）
- 响应中包含一个提示，建议用户注册以获得个性化结果

### 认证模式（需要 API 密钥）
- 公共模式的所有功能，此外还包括：
- 一次调用即可获取所有喜欢地点的仪表盘概览
- 根据用户的活动偏好、风速范围和时间窗口提供个性化预测
- 活动历史记录和统计数据
- 跟随用户的实时活动信息
- 喜好地点列表
- 响应中不包含注册提示

## 认证

对于所有需要认证的端点，需将 API 密钥作为 Bearer token 包含在请求中：
```
Authorization: Bearer {WINDSENSEI_API_KEY}
```

API 还接受 `X-API-Key: {key}` 请求头或 `?api_key={key}` 查询参数。

---

## API 端点

### 1. 风力报告（快速概要）

这是查看天气状况的最简单方式。返回自然语言概要和结构化数据。

```
GET https://windsensei.com/api/v1/wind-report?when={when}
```

| 参数 | 值 | 默认值 | 描述 |
|-------|--------|---------|-------------|
| `when` | `today`, `tomorrow`, `weekend`, `week` | `today` | 时间窗口 |
| `spot` | 字符串（例如：`Cherry Beach`） | — | 通过名称搜索公共地点。使用 API 密钥时不需要。 |
| `locationId` | 字符串 | — | 通过 ID 查找特定地点 |
| `format` | `text`, `full` | `full` | `text` 仅返回概要字符串 |

**认证：** 可选。使用 API 密钥时，会自动返回用户所有喜欢地点的预测。

**响应：**
```json
{
  "success": true,
  "text": "Cherry Beach: Great conditions 2-6pm — 18-22kt SW, 24°C, water 19°C. Good for Kitefoil, Twintip.",
  "when": "today",
  "authenticated": false,
  "hint": "Personalize your forecast with your own spots and activity preferences at windsensei.com",
  "locations": [
    {
      "locationId": "abc123",
      "name": "Cherry Beach",
      "timezone": "America/Toronto",
      "blocks": [
        {
          "date": "2026-02-21",
          "dateLabel": "Saturday, Feb 21",
          "start": "14:00",
          "end": "18:00",
          "quality": "good",
          "windSpeed": { "avg": 20, "gust": 25 },
          "windDirection": "SW",
          "windQuality": "good",
          "temperature": 24,
          "waterTemp": 19,
          "activities": ["Kitefoil", "Twintip"]
        }
      ]
    }
  ]
}
```

**当未指定地点且没有 API 密钥时：**
```json
{
  "success": true,
  "text": "Provide a spot name to check conditions. Example: ?spot=Cherry Beach",
  "locations": [],
  "hint": "Personalize your forecast with your own spots and activity preferences at windsensei.com"
}
```

---

### 2. 仪表盘（所有地点概览）

**认证：** 必需。一次调用即可获取用户所有喜欢地点的当前天气状况，按最佳条件排序。这是认证用户回答 “风怎么样？” 的最有效方式。

**响应：**
```
GET https://windsensei.com/api/v1/dashboard
```

**关键字段：**
- `current`：基于预测的当前天气状况
- `observed`：实际气象站数据（如果该地点有数据）——比预测更准确
- `nextGoodWindow`：根据用户活动偏好确定的下一个适合活动的时间窗口。时间以地点的本地时区为准。
- `modelConsensus`：预测模型对天气状况的共识
- 地点按质量（从最佳到最差排序），然后按风速排序

**何时使用仪表盘与风力报告：**
- 当用户拥有 API 密钥并询问一般性的 “风怎么样？” 时，使用 **仪表盘**——它一次调用即可覆盖所有地点
- 当需要查询特定地点的名称，或者没有 API 密钥时，使用 **风力报告**

---

### 3. 最佳地点（排名地点）

查找当前最适合的地点。无论是否认证均可使用。

```
GET https://windsensei.com/api/v1/best-conditions
```

| 参数 | 值 | 默认值 | 描述 |
|-------|--------|---------|-------------|
| `when` | `now`, `today`, `tomorrow`, `weekend` | `now` | 时间窗口。`now` 表示当前小时开始。 |
| `lat` | 数字 | — | 纬度（如果没有 API 密钥则必需） |
| `lng` | 数字 | — | 经度（如果没有 API 密钥则必需） |
| `radius` | 数字 | 100 | 搜索半径（最大 500 公里）。仅与纬度/经度一起使用。 |
| `limit` | 数字 | 5 | 最大结果数量（最多 20 个） |

**认证：** 可选。
- **使用 API 密钥时：** 根据用户的活动偏好评估他们喜欢的地点。不需要提供纬度/经度。
- **没有 API 密钥时：** 需要提供纬度/经度。查找附近的公共地点，并使用默认的风筝放飞偏好进行评估。
- 如果已认证但没有偏好地点，将回退到基于纬度/经度的搜索。

**响应：**
```json
{
  "success": true,
  "when": "now",
  "spots": [
    {
      "locationId": "abc123",
      "name": "Cherry Beach",
      "timezone": "America/Toronto",
      "distance": 4.2,
      "current": {
        "windSpeed": 20,
        "windGust": 26,
        "windDirection": "SW",
        "windQuality": "good",
        "temperature": 22,
        "quality": "good"
      },
      "waterTemp": 18.5,
      "bestBlock": {
        "date": "2026-02-21",
        "dateLabel": "Saturday, Feb 21",
        "start": "14:00",
        "end": "18:00",
        "quality": "good",
        "windSpeed": { "avg": 20, "gust": 26 },
        "windDirection": "SW",
        "activities": ["Kitefoil", "Twintip"]
      },
      "activities": ["Kitefoil", "Twintip"]
    }
  ],
  "total": 1,
  "authenticated": false,
  "hint": "Get personalized results with your own spots and preferences at windsensei.com",
  "generatedAt": "2026-02-21T12:00:00Z"
}
```

**关键字段：**
- `distance`：从提供的纬度/经度算起的距离（仅适用于附近搜索）
- `current`：当前天气状况（或时间窗口内的第一个小时）
- `bestBlock`：时间范围内最适合活动的连续时间段
- 仅返回条件较好的地点——条件较差的地点将被排除
- 地点按质量（从最佳到最差排序），然后按风速排序

**何时使用：** “现在哪个地点最适合？”、“我在多伦多附近哪里适合放风筝？”、“这个周末哪个地点条件最好？”

---

### 4. 搜索地点

按名称查找地点。无需认证即可使用。

```
GET https://windsensei.com/api/v1/locations/search?q={query}
```

| 参数 | 值 | 默认值 | 描述 |
|-------|--------|---------|-------------|
| `q` | 字符串 | — | 搜索查询（必需） |
| `limit` | 数字 | 10 | 最大结果数量（最多 50 个） |

**认证：** 已认证的用户还可以查看他们的私人地点。

**响应：**
```json
{
  "success": true,
  "query": "cherry",
  "locations": [
    {
      "_id": "abc123",
      "name": "Cherry Beach",
      "latitude": 43.63,
      "longitude": -79.34,
      "activityTypes": ["kite"],
      "windDirections": ["SW", "S", "W"],
      "isFavorite": false
    }
  ],
  "total": 1
}
```

**何时使用：** 当用户需要将地点名称转换为 `locationId` 以供其他端点使用，或者用户想要浏览可用地点时。**

---

### 5. 附近地点

查找特定地点附近的地点。无需认证即可使用。

```
GET https://windsensei.com/api/v1/locations/nearby?lat={lat}&lng={lng}
```

| 参数 | 值 | 默认值 | 描述 |
|-------|--------|---------|-------------|
| `lat` | 数字 | — | 纬度（必需） |
| `lng` | 数字 | — | 经度（必需） |
| `radius` | 数字 | 50 | 搜索半径（最大 500 公里） |
| `limit` | 数字 | 10 | 最大结果数量（最多 50 个） |

**认证：** 已认证的用户还可以查看他们的私人地点。

**响应：**
```json
{
  "success": true,
  "center": { "lat": 43.65, "lng": -79.38 },
  "radiusKm": 50,
  "locations": [
    {
      "id": "abc123",
      "name": "Cherry Beach",
      "latitude": 43.63,
      "longitude": -79.34,
      "distance": 4.2,
      "activityTypes": ["kite"],
      "windDirections": { "SW": "good", "S": "medium", "W": "good" },
      "isFavorite": false
    }
  ],
  "total": 3
}
```

**何时使用：** 当用户询问 “我附近有哪些地点？”、“多伦多附近有哪些适合放风筝的地点？” 或 “在 [城市] 有哪些风筝地点？” 时。您需要先使用代理的地理编码功能将城市/地点转换为纬度/经度。**

---

### 6. 详细预测

提供特定地点的完整每小时预测。比风力报告更详细。

**认证：** 必需。根据用户的活动偏好返回个性化评级。

**响应包括：**
- 每小时的风速、风向、阵风、温度、降水量
- 带有质量评级的时间段（优秀/良好/中等）
- 潮汐预测（高潮/低潮时间）
- 水温
- 最多 16 天的预测数据

**何时使用：** 当用户需要每小时的具体信息，或者在查看概要后询问 “最佳时间是什么时候？” 时。

---

### 7. 喜好地点列表

获取用户保存的喜欢地点列表。

```
GET https://windsensei.com/api/v1/favorites
```

**认证：** 必需。

**响应：**
```json
{
  "success": true,
  "locations": [
    {
      "_id": "abc123",
      "name": "Cherry Beach",
      "latitude": 43.63,
      "longitude": -79.34,
      "activityTypes": ["kite"],
      "windDirections": ["SW", "S", "W"],
      "isFavorite": true
    }
  ]
}
```

**何时使用：** 当用户询问 “我的地点有哪些？” 或 “列出我的喜欢地点？” 时。

---

### 8. 活动历史

获取用户记录的风力运动活动。

```
GET https://windsensei.com/api/v1/sessions
```

| 参数 | 值 | 默认值 | 描述 |
|-------|--------|---------|-------------|
| `limit` | 数字 | 20 | 最大结果数量（最多 100 个） |
| `offset` | 数字 | 0 | 分页偏移量 |
| `activityType` | 字符串 | — | 按活动类型过滤（例如：“Kitefoil”, “Wingfoil”） |
| `locationId` | 字符串 | — | 按地点过滤 |

**认证：** 必需。

**响应：**
```json
{
  "success": true,
  "sessions": [
    {
      "id": "sess123",
      "activityType": "Kitefoil",
      "locationName": "Cherry Beach",
      "startTime": "2026-02-15T14:00:00Z",
      "endTime": "2026-02-15T17:30:00Z",
      "duration": 12600,
      "rating": 4,
      "notes": "Great session, consistent SW wind",
      "stats": {
        "distance": 15.2,
        "maxSpeed": 32.5
      }
    }
  ],
  "pagination": {
    "total": 47,
    "limit": 20,
    "offset": 0,
    "hasMore": true
  }
}
```

**何时使用：** 当用户询问 “我进行了多少次活动？”、“显示我最近的活动”，“我上次的放风筝活动是什么？” 或 “我这个月共进行了多少次活动？” 时。

---

### 9. 实时活动

查看哪些跟随的用户目前正在活动。

```
GET https://windsensei.com/api/v1/live-sessions
```

**认证：** 必需。

**响应：**
```json
{
  "success": true,
  "data": {
    "sessions": [
      {
        "id": "live123",
        "user": {
          "name": "Mike",
          "nickname": "windmike"
        },
        "activityType": "Wingfoil",
        "spotName": "Cherry Beach",
        "startTime": "2026-02-21T14:00:00Z",
        "lastUpdateTime": "2026-02-21T14:30:00Z",
        "stats": {
          "elapsedTime": 1800,
          "distance": 3.2,
          "maxSpeed": 22.1
        },
        "conditions": {
          "windSpeed": 18,
          "windDirection": "SW"
        }
      }
    ]
  }
}
```

**何时使用：** 当用户询问 “现在有人在外面活动吗？”、“谁在放风筝？” 或 “我的朋友中有谁在活动？” 时。

---

### 10. 公共个人资料

查询任何 WindSensei 用户的公开信息。无需认证。

**响应：**
```
GET https://windsensei.com/api/public/profile/{nickname}
```

**认证：** 无需认证。

**响应包括：** 昵称、头像、总活动次数、总时长、总距离、最近的公开活动。

**何时使用：** 当用户询问特定用户的个人资料或统计信息时。**

---

### 11. 请求新地点

提交一个请求，添加系统中还不存在的地点。

```
POST https://windsensei.com/api/v1/spot-request
Content-Type: application/json

{
  "spotName": "Spot Name Here"
}
```

**认证：** 可选。如果包含 API 密钥，请求将归因于用户。

**响应：**
```json
{
  "success": true,
  "message": "Spot request for \"Spot Name\" submitted. We'll review it soon."
}
```

重复的请求会自动处理——仅返回成功信息。

---

## 解释数据

**重要提示：** 仅报告 API 数据提供的内容。请勿：**
- 如果 API 没有说明，不要自行判断天气状况是否良好
- 不要添加自己对风速、风向或质量的评估
- 不要对 API 返回的数据之外的信息进行推测（例如：“早上也可能适合”
- 不要对用户是否应该外出进行主观判断——数据和质量评级已经给出了答案
- 不要根据 `activities` 数组中列出的活动来推断其他活动

**如果某个地点的 `blocks` 为空，那么该地点没有适合活动的条件。** 请不要淡化这一点，也不要建议用户再去查看。

**如果 `blocks` 存在，它们代表 API 的最终评估**——质量、风速、风向和活动是根据用户的偏好和地点的特征计算得出的。请完全信任返回的数据。**

- **时区**：每个地点都包含一个 `timezone` 字段（例如：“America/Toronto”）。**`start`、`end` 和 `date` 字段中的所有时间都使用此时区**——不是 UTC，也不是用户的本地时间。在向用户展示时间时，如果时区与用户所在地点不同，请务必说明（例如：“下午 2-6 点是东部标准时间”）。
- **质量**：“excellent” > “good” > “medium”。只显示这些评级——排除条件较差的时间。
- **windQuality**：表示该地点的风向适合程度（“good”, “medium”, “bad”）。这是根据地点已知的最适合风向计算得出的——不要自行更改。
- **blocks**：每个 `blocks` 代表一个连续的适合活动的时间段。一个地点/一天可能有多个 `blocks`。如果没有 `blocks`，则表示没有适合活动的条件。
- **activities**：哪些风运动适合当前条件（例如：“Kitefoil”, “Twintip”, “Wingfoil”）。仅提及 `blocks` 中列出的活动。
- `observed` 与 `current`：`observed` 来自实际气象站的数据，更准确。如果有 `observed` 数据，则优先使用。
- **modelConsensus**：当 `agreement` 为 “high” 时，预测更可靠。
- **authenticated**：表示响应是否使用了用户的个性化偏好。如果 `false`，请告知用户可以通过设置 API 密钥获得更好的结果。
- **hint**：仅对未认证的查询显示。自然地向用户传达这一信息（例如，在第一次响应的结尾，而不是每次都显示）。

## 回答用户

1. **首先传达 `text` 字段**（使用风力报告时）——它已经是一个简洁的事实性总结。
2. 如果用户需要更多细节，可以使用结构化的 `blocks` 数据进行补充——但请始终遵循数据中的信息。
3. 对于 “没有适合的条件” 的结果（`blocks` 为空），要直接说明：“今天 Cherry Beach 没有适合活动的条件。” 不要添加关于可能适合活动的推测。
4. 如果响应中包含 `hint`，在第一次回答时随意提及：“顺便说一下，您可以在 windsensei.com 为您的地点获取个性化预测。”
5. 使用仪表盘数据时，先介绍最佳地点，然后总结其他信息。
6. 不要添加无关内容。像在海滩上的朋友一样直接沟通。
7. **永远不要与 API 的质量评估相矛盾。** 如果 API 显示为 “medium” 质量，就不要将其升级为 “great conditions”。如果 API 显示没有 `blocks`，也不要说 “可能仍然值得查看”。

## 选择合适的端点

| 用户意图 | 端点 | 是否需要认证？ |
|------------|----------|-------------|
| “风怎么样？”（拥有 API 密钥） | 仪表盘 | 是 |
| “Cherry Beach 的风怎么样？” | 使用 `spot=` 的风力报告 | 否 |
| “这个周末的风怎么样？” | 使用 `when=weekend` 的风力报告 | 否 |
| “现在哪个地点最适合？” | 最佳地点 | 否（需要纬度/经度）或需要 |
| “我在多伦多附近哪里适合放风筝？” | 使用纬度/经度的最佳地点 | 否 |
| “多伦多附近有哪些地点？” | 使用地理编码查找附近的地点 | 否 |
| “查找名为 ‘cherry’ 的地点” | 使用 `q=cherry` 搜索 | 否 |
| “提供 Cherry Beach 的每小时详细预报” | 使用完整预报 | 是 |
| “我的地点有哪些？” | 喜好地点 | 是 |
| “我进行了多少次活动？” | 活动记录 | 是 |
| “现在有人在外面活动吗？” | 实时活动 | 是 |
| “显示 @windmike 的个人资料” | 公共个人资料 | 否 |
| “添加 Wasaga Beach”（地点未找到） | 请求新地点 | 否 |

## 处理地点名称

当用户询问 “Cherry Beach 的风怎么样？” 时：
1. 提取地点名称（“Cherry Beach”）
2. 将其 URL 编码为 `?spot=Cherry+Beach` 并传递给风力报告 API
3. API 会进行模糊匹配——部分名称也可以匹配（例如，“cherry” 可以匹配 “Cherry Beach”）
4. 如果有多个匹配的地点，API 会返回最多 3 个地点。展示所有匹配的地点。

为了更好地控制，可以先使用搜索端点将地点名称转换为 `locationId`，然后再使用该 ID 调用其他端点。

## 处理缺失的地点

当找不到某个地点时：
1. 告诉用户：“WindSensei 尚未收集到该地点的数据。”
2. 询问：“您是否希望我提交一个添加该地点的请求？”
3. 如果用户同意，使用 `POST` 请求 `/api/v1/spot-request` 并提供地点名称。
4. 传达确认信息。

## 日历集成

此技能不提供或需要任何日历凭据。它依赖于主机代理现有的日历工具（例如 Google Calendar MCP、Apple Calendar 等）。如果代理没有日历工具，可以跳过日历相关操作，直接展示预测数据。

当用户请求 “安排时间”、“添加到日历” 或根据风力数据 “安排活动” 时：
1. 使用地点数据通过代理的日历工具创建日历事件：
   - **标题**：`Kite @ {location name}`（或块中的相关活动）
   - **开始时间**：块的 `date` 加 `start` 时间
   - **结束时间**：块的 `date` 加 `end` 时间
   - **描述**：`{windSpeed.avg}kt {windDirection}（阵风 {windSpeed.gust}kt），{temperature}°C{waterTemp ? ', water ' + waterTemp + '°C' : ''}. Quality: {quality}.`
   - **地点**：地点名称

2. 向用户确认已添加的内容。
3. 如果没有日历工具，可以提供事件详情，以便用户手动添加。

如果有多个适合活动的时间段，询问用户希望添加哪个或多个时间段，除非用户明确表示“全部添加”。

## 示例对话

**首次使用用户，没有 API 密钥：**
> 用户：**Cherry Beach 的风怎么样？**
> → 调用 GET /api/v1/wind-report?spot=Cherry+Beach&when=today
> → 传达 `text` 字段
> → 提及：“要获取您喜欢地点的个性化预测，请访问 windsensei.com”

**已认证用户，快速查询：**
> 用户：**风怎么样？**
> → 调用 GET /api/v1/dashboard（使用 Bearer token）
> → 总结：先介绍最佳地点，再介绍其他信息

**当前最适合的地点（未认证用户）：**
> 用户：**我在多伦多附近哪里适合放风筝？**
> → 将 Miami 地理编码为 lat=25.76, lng=-80.19
> → 调用 GET /api/v1/best-conditions?lat=25.76&lng=-80.19&when=now
> → “Crandon Park 现在条件最好——风速 18 公里/小时，风向适合，温度 28°C。最佳时间段是下午 2-6 点。”

**当前最适合的地点（已认证用户）：**
> 用户：**我今天最好的地点是哪里？**
> → 调用 GET /api/v1/best-conditions?when=today（使用 Bearer token）
> → “今天 Cherry Beach 的条件最好——下午 2-6 点风速 20 公里/小时，风向适合。Hanlan’s 的条件中等，时间是下午 1-4 点。”

**查找新地点：**
> 用户：**多伦多附近有哪些适合放风筝的地点？**
> → 将 Miami 地理编码为 lat=25.76, lng=-80.19
> → 调用 GET /api/v1/locations/nearby?lat=25.76&lng=-80.19&radius=100
> → 显示附近的地点及其距离

**活动统计：**
> 用户：**我今年进行了多少次活动？**
> → 调用 GET /api/v1/sessions?limit=100
> → 按活动类型统计数量

**实时查询：**
> 用户：**现在有人在外面活动吗？**
> → 调用 GET /api/v1/live-sessions
> → “您的朋友 Mike 正在 Cherry Beach 放翼装冲浪——已经活动了 30 分钟，风速 18 公里/小时”

**日历操作：**
> 用户：**安排这个周末的最佳时间**：
> → 调用 GET /api/v1/wind-report?when=weekend
> → 为每个时间段创建日历事件
> → “已在您的日历中添加 2 个活动：周六下午 2-6 点在 Cherry Beach 放风筝，周日上午 10-12 点在 Hanlan’s 放翼装冲浪”

**特定日期：**
> 用户：**周六的情况如何？**
> → 调用 GET /api/v1/wind-report?when=weekend
> → 传达响应中的相关信息

**没有适合的条件：**
> 用户：**明天适合放风筝吗？**
> → 调用 GET /api/v1/wind-report?when=tomorrow
> → 如果没有 `blocks`：**“明天 [地点名称] 没有适合活动的条件。”**

**地点未找到：**
> 用户：**Wasaga Beach 的风怎么样？**
> → 调用 GET /api/v1/wind-report?spot=Wasaga+Beach
> → 响应：**未找到**
> → 回复：**WindSensei 尚未收集到 Wasaga Beach 的数据。您是否希望我提交一个添加该地点的请求？**
> 用户：**是的，请添加。**
> → 调用 POST /api/v1/spot-request 并提供 `{"spotName": "Wasaga Beach"}`
> → 回复：**已提交添加 Wasaga Beach 的请求。WindSensei 团队将会审核。**