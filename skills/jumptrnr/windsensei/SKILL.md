---
name: windsensei
description: 检查适合风帆运动（如风筝冲浪、翼装飞行、冲浪）的风力和天气状况。获取天气预报，查找附近的适合运动的地点，查看过往的活动记录，并请求新的活动地点。
homepage: https://windsensei.com
source: https://github.com/jumptrnr/spotsensei
user-invocable: true
metadata: {"emoji": "🪁", "optionalEnv": "WINDSENSEI_API_KEY"}
---
# WindSensei - 风力预报助手

您可以查看您喜欢地点的风力情况，并根据预报采取相应行动。

## 何时使用此技能

当用户询问以下内容时，请激活此技能：
- 风力状况、风力预报或适合风运动的天气信息
- “风怎么样？”、“我应该去放风筝吗？”、“这个周末有适合的风吗？”
- “[地点名称] 的情况如何？”、“风力报告”
- 放风筝、风筝冲浪、翼装帆板、冲浪的天气条件
- 根据风力安排时间或向日历中添加活动
- 查找某个地点附近的适合地点
- 活动历史记录、统计数据或活动日志
- 了解哪些朋友正在活动（实时情况）

## 配置

### 认证信息

- `WINDSENSEI_API_KEY`：一个以 `ss_` 开头的 API 密钥。**可选——即使没有这个密钥，该技能也能正常使用。** 没有密钥时，您仍然可以按名称查询任何公开地点、搜索地点以及查找附近的地点。拥有密钥后，您可以获得个性化预报、仪表盘概览、活动历史记录和社交功能。
- **不需要其他认证信息。** 该技能仅向 WindSensei API 发送 HTTPS 请求。日历功能（如果用户请求）会使用主机代理自身的日历工具——该技能不会请求或存储任何日历凭证。

### 获取 API 密钥

如果用户需要个性化预报，请指导他们：
1. 在 **windsensei.com** 注册
2. 添加他们喜欢的地点并设置活动偏好
3. 访问他们的 **个人资料页面**：`windsensei.com/dashboard/profile`——API 密钥管理器位于该页面的底部
4. 点击 “创建 API 密钥”，为其命名（例如 “Claude” 或 “OpenClaw”，然后复制密钥
5. 密钥以 `ss_` 开头——将其设置为代理的环境配置中的 `WINDSENSEI_API_KEY`

**直接链接：** `https://windsensei.com/dashboard/profile`（滚动到 “API 密钥” 部分）

## 两种模式

### 公开模式（无需 API 密钥）
- 使用 `spot` 参数按名称查询任何公开地点
- 按名称搜索地点
- 查找特定纬度/经度附近的地点
- 使用默认的风筝运动偏好设置（风速 12-40 公里/小时，时间范围 早上 8 点到晚上 8 点）
- 响应中包含一个提示，建议用户注册以获得个性化结果

### 认证模式（需要 API 密钥）
- 公开模式的所有功能，此外还包括：
- 一次调用即可获取所有喜欢地点的仪表盘概览
- 根据用户的活动偏好、风速范围和时间窗口提供个性化预报
- 活动历史记录和统计数据
- 跟随用户的实时活动信息
- 喜欢地点列表
- 响应中不包含注册提示

## 认证

对于所有需要认证的接口，请将 API 密钥作为 Bearer 令牌包含在请求中：
```
Authorization: Bearer {WINDSENSEI_API_KEY}
```

API 还接受 `X-API-Key: {key}` 标头或 `?api_key={key}` 查询参数。

---

## API 接口

### 1. 风力报告（快速概要）

这是查看风力状况的最简单方式。返回自然语言概要以及结构化数据。

```
GET https://windsensei.com/api/v1/wind-report?when={when}
```

| 参数 | 值 | 默认值 | 描述 |
|-------|--------|---------|-------------|
| `when` | `today`, `tomorrow`, `weekend`, `week` | `today` | 时间范围 |
| `spot` | 字符串（例如，`Cherry Beach`） | — | 按名称搜索公开地点。使用 API 密钥时不需要此参数。 |
| `locationId` | 字符串 | — | 通过 ID 查找特定地点 |
| `format` | `text`, `full` | `full` | `text` 仅返回概要字符串 |

**认证：** 可选。使用 API 密钥时，会自动返回用户所有喜欢地点的预报。

**响应：**
```json
{
  "success": true,
  "text": "Cherry Beach: Great conditions 2-6pm — 18-22kt SW, 24°C, water 19°C. Good for Kitefoil, Twintip.",
  "when": "today",
  "authenticated": false,
  "hint": "Personalize your forecast with your own spots and activity preferences at windsensei.com",
  "locations": [
    {
      "locationId": "abc123",
      "name": "Cherry Beach",
      "timezone": "America/Toronto",
      "blocks": [
        {
          "date": "2026-02-21",
          "dateLabel": "Saturday, Feb 21",
          "start": "14:00",
          "end": "18:00",
          "quality": "good",
          "windSpeed": { "avg": 20, "gust": 25 },
          "windDirection": "SW",
          "windQuality": "good",
          "temperature": 24,
          "waterTemp": 19,
          "activities": ["Kitefoil", "Twintip"]
        }
      ]
    }
  ]
}
```

**当未指定地点且没有 API 密钥时：**
```json
{
  "success": true,
  "text": "Provide a spot name to check conditions. Example: ?spot=Cherry Beach",
  "locations": [],
  "hint": "Personalize your forecast with your own spots and activity preferences at windsensei.com"
}
```

---

### 2. 仪表盘（所有地点概览）

**认证：** 必需。一次调用即可获取用户所有喜欢地点的当前状况，结果按最佳顺序排序。这是认证用户回答 “风怎么样？” 的最有效方式。

```
GET https://windsensei.com/api/v1/dashboard
```

**响应：**
```json
{
  "success": true,
  "spots": [
    {
      "locationId": "abc123",
      "name": "Cherry Beach",
      "current": {
        "windSpeed": 18,
        "windGust": 24,
        "windDirection": 225,
        "temperature": 22,
        "quality": "good"
      },
      "observed": {
        "stationName": "Toronto Island",
        "observationTime": "2026-02-21T14:00:00Z",
        "windSpeed": 20,
        "windDirection": 210,
        "windGust": 26,
        "temperature": 21,
        "waterTemp": { "temperature": 18.5, "units": "C" }
      },
      "nextGoodWindow": {
        "start": "2026-02-21T14:00",
        "end": "2026-02-21T18:00",
        "quality": "good",
        "activities": ["Kitefoil", "Twintip"]
      },
      "modelConsensus": {
        "agreement": "high",
        "models": ["GFS", "ICON", "GEM"]
      }
    }
  ],
  "generatedAt": "2026-02-21T12:00:00Z"
}
```

**关键字段：**
- `current`：基于预报的当前状况
- `observed`：实际气象站数据（如果该地点有数据）——比预报更准确
- `nextGoodWindow`：根据用户活动偏好确定的下一个适合活动的时段。时间以地点的本地时区为准。
- `modelConsensus`：预报模型对状况的共识程度
- 地点按质量（从最佳到最差排序），然后按风速排序

**何时使用仪表盘与风力报告：**
- 当用户拥有 API 密钥并询问一般性的 “风怎么样？” 时，使用 **仪表盘**——它一次调用即可获取所有地点的信息
- 当需要按名称查询特定地点或没有 API 密钥时，使用 **风力报告**

---

### 3. 搜索地点

按名称查找地点。无需认证即可使用。

```
GET https://windsensei.com/api/v1/locations/search?q={query}
```

| 参数 | 值 | 默认值 | 描述 |
|-------|--------|---------|-------------|
| `q` | 字符串 | — | 搜索查询（必需） |
| `limit` | 数字 | 10 | 最多结果数量（最多 50 个） |

**认证：** 认证用户还可以查看他们的私人地点。

**响应：**
```json
{
  "success": true,
  "query": "cherry",
  "locations": [
    {
      "_id": "abc123",
      "name": "Cherry Beach",
      "latitude": 43.63,
      "longitude": -79.34,
      "activityTypes": ["kite"],
      "windDirections": ["SW", "S", "W"],
      "isFavorite": false
    }
  ],
  "total": 1
}
```

**使用场景：** 将地点名称解析为 `locationId` 以供其他接口使用，或当用户想要浏览可用地点时。

---

### 4. 附近地点

查找特定位置附近的地点。无需认证即可使用。

```
GET https://windsensei.com/api/v1/locations/nearby?lat={lat}&lng={lng}
```

| 参数 | 值 | 默认值 | 描述 |
|-------|--------|---------|-------------|
| `lat` | 数字 | — | 纬度（必需） |
| `lng` | 数字 | — | 经度（必需） |
| `radius` | 数字 | 50 | 半径（以公里为单位，最大 500 公里） |
| `limit` | 数字 | 10 | 最多结果数量（最多 50 个） |

**认证：** 认证用户还可以查看他们的私人地点。

**使用场景：** 当用户询问 “我附近有哪些地点？”、“多伦多附近有哪些适合放风筝的地点？” 或 “在 [城市] 找到适合放风筝的地点” 时。您需要先将该城市/地点进行地理编码（使用代理的地理编码功能或已知坐标）。

---

### 5. 详细预报

提供特定地点的完整每小时预报。比风力报告更详细。

**认证：** 必需。根据用户的活动偏好返回个性化质量评级。

**响应包括：**
- 每小时的风速、风向、阵风、温度、降水量
- 带有质量评级的时段（优秀/良好/中等）
- 潮汐预测（高潮/低潮时间）
- 水温
- 预报模型的共识数据
- 最多 16 天的预报

**使用场景：** 当用户需要每小时详细信息，或在查看概要后询问 “最佳时段是什么时候？” 时。

---

### 6. 喜欢地点列表

获取用户保存的喜欢地点列表。

**认证：** 必需。

**响应：**
```
GET https://windsensei.com/api/v1/favorites
```

**使用场景：** 当用户询问 “我的喜欢地点有哪些？” 或 “列出我的喜欢地点” 时。

---

### 7. 活动历史记录

获取用户记录的风力运动活动。

```
GET https://windsensei.com/api/v1/sessions
```

| 参数 | 值 | 默认值 | 描述 |
|-------|--------|---------|-------------|
| `limit` | 数字 | 20 | 最多结果数量（最多 100 个） |
| `offset` | 数字 | 0 | 分页偏移量 |
| `activityType` | 字符串 | — | 按活动类型过滤（例如，“Kitefoil”（风筝冲浪） |
| `locationId` | 字符串 | — | 按地点过滤 |

**认证：** 必需。

**使用场景：** 当用户询问 “我进行了多少次活动？”、“显示我最近的活动记录？”、“我上一次放风筝是什么时候？” 或 “这个月我共进行了多少次活动？” 时。

---

### 8. 实时活动（朋友的活动）

查看哪些跟随的用户正在活动。

**认证：** 必需。

**响应：**
```json
{
  "success": true,
  "data": {
    "sessions": [
      {
        "id": "live123",
        "user": {
          "name": "Mike",
          "nickname": "windmike"
        },
        "activityType": "Wingfoil",
        "spotName": "Cherry Beach",
        "startTime": "2026-02-21T14:00:00Z",
        "lastUpdateTime": "2026-02-21T14:30:00Z",
        "stats": {
          "elapsedTime": 1800,
          "distance": 3.2,
          "maxSpeed": 22.1
        },
        "conditions": {
          "windSpeed": 18,
          "windDirection": "SW"
        }
      }
    ]
  }
}
```

**使用场景：** 当用户询问 “现在有人在外面活动吗？”、“谁在放风筝？” 或 “我的朋友中有谁在水上吗？” 时。

---

### 9. 公开个人资料

查询任何 WindSensei 用户的公开信息。无需认证。

**认证：** 无需认证。

**响应包括：** 昵称、头像、总活动次数、总时长、总距离、最近的公开活动记录、活动类型。

**使用场景：** 当用户询问特定用户的个人资料或统计信息时。

---

### 10. 请求新增地点

提交请求以添加系统中尚不存在的地点。

**认证：** 可选。如果包含 API 密钥，请求将归因于用户。

**响应：**
```json
{
  "success": true,
  "message": "Spot request for \"Spot Name\" submitted. We'll review it soon."
}
```

重复请求会自动处理——仅返回成功信息。

---

## 解释数据

- **时区**：每个地点都包含一个 `timezone` 字段（例如，“America/Toronto”）。**`start`、`end` 和 `date` 字段中的所有时间均以此时区为准**——不是 UTC，也不是用户的本地时间。在向用户展示时间时，如果时区与用户所在地区不同，请务必说明（例如，“下午 2 点到 6 点是东部标准时间”）。
- **质量**：“优秀” > “良好” > “中等”。仅显示这些等级——较差的时间段会被排除。
- **windQuality**：该地点的风向适合度（“良好”、“中等”、“较差”）。
- **blocks**：每个时间段表示适合活动的连续时段。一个地点/一天可能有多个时间段。
- **activities**：适合该条件的风运动类型（例如，“Kitefoil”（风筝冲浪）、“Twintip”（双尖帆板）、“Wingfoil”（翼装帆板）。
- **observed vs current**：`observed` 来自实际气象站的数据，更准确。如果有 `observed` 数据，则优先使用。
- **modelConsensus**：当 `agreement` 为 “high” 时，预报更可靠。
- **authenticated**：响应是否使用了用户的个性化设置。如果为 `false`，请告知用户可以通过设置 API 密钥获得更好的结果。
- **hint**：仅对未认证的查询显示。自然地向用户传达这一信息（例如，在第一次响应的末尾，而不是每次都显示）。

## 回答用户

1. **首先传达 `text` 字段**（使用风力报告时）——它已经是一个简洁的自然语言概要。
2. 如果用户需要更多详细信息，可以使用结构化数据进行补充说明。
3. 对于 “没有适合的活动条件”的结果，直接说明：“今天 Cherry Beach 没有适合的活动条件。北风较弱。”
4. 如果响应中包含 `hint`，在第一次回复时随意提及：“顺便说一下，您可以在 windsensei.com 为您的地点获取个性化预报。”
5. 使用仪表盘数据时，先介绍最佳地点，再总结其他信息。
6. 不要冗长地添加无关内容。像在海滩上的朋友一样直接交流。

## 选择合适的接口

| 用户意图 | 接口 | 是否需要认证？ |
|------------|----------|-------------|
| “风怎么样？”（拥有 API 密钥） | 仪表盘 | 是 |
| “Cherry Beach 的风怎么样？” | 使用 `spot=` 的风力报告 | 否 |
| “这个周末风怎么样？” | 使用 `when=weekend` 的风力报告 | 否 |
| “多伦多附近有哪些地点？” | 使用附近地点搜索 | 否 |
| “查找名为 ‘cherry’ 的地点” | 使用 `q=cherry` 搜索 | 否 |
| “提供 Cherry Beach 的每小时预报” | 使用完整预报 | 是 |
| “我的喜欢地点有哪些？” | 喜欢地点列表 | 是 |
| “我进行了多少次活动？” | 活动记录 | 是 |
| “现在有人在外面活动吗？” | 实时活动 | 是 |
| “显示 @windmike 的个人资料” | 公开个人资料 | 否 |
| “添加 Wasaga Beach”（地点未找到） | 添加地点请求 | 否 |

## 处理地点名称

当用户询问 “Cherry Beach 的风怎么样？” 时：
1. 提取地点名称（“Cherry Beach”）
2. 将其 URL 编码为 `?spot=Cherry+Beach` 并传递给风力报告接口
3. API 支持模糊匹配——部分名称也可以匹配（例如，“cherry” 可以匹配 “Cherry Beach”）
4. 如果有多个匹配的地点，API 会返回最多 3 个地点。全部展示给用户。

为了更好地控制，可以先使用搜索接口将地点名称解析为 `locationId`，然后再使用该 ID 调用其他接口。

## 处理地点缺失的情况

当找不到某个地点时：
1. 告诉用户：“WindSensei 尚未收录该地点的数据。”
2. 询问用户：“您是否希望我提交添加该地点的请求？”
3. 如果用户同意，使用 `POST` 请求 `/api/v1/spot-request` 并传递地点名称。
4. 向用户传达确认信息。

## 日历集成

此技能不提供也不需要任何日历凭证。它依赖于主机代理现有的日历工具（例如 Google Calendar MCP、Apple Calendar 等）。如果代理没有日历工具，可以跳过日历相关操作，直接展示预报数据。

当用户请求 “安排时间”、“添加到日历” 或根据风力时段 “安排活动” 时：
1. 使用时段数据通过代理的日历工具创建日历事件：
   - **标题**：`Kite @ {location name}`（或时段中的相关活动）
   - **开始时间**：时段的 `date` 加 `start` 时间
   - **结束时间**：时段的 `date` 加 `end` 时间
   - **描述**：`{windSpeed.avg}公里/小时 {windDirection}（阵风 {windSpeed.gust}公里/小时），{temperature}°C{waterTemp ? ', water ' + waterTemp + '°C' : ''}. 质量：{quality}。`
   - **地点**：地点名称

2. 向用户确认已添加的内容。
3. 如果没有日历工具，提供事件详情以便用户手动添加。

如果有多个适合的时段，询问用户想要添加哪个（或多个）时段，除非用户明确表示“全部添加”。

## 示例对话

**首次使用，没有 API 密钥：**
> 用户：**Cherry Beach 的风怎么样？**
> → 调用 GET /api/v1/wind-report?spot=Cherry+Beach&when=today
> → 传达 `text` 字段
> → 提及：“如需查看您喜欢地点的个性化预报，请访问 windsensei.com”

**已认证用户，快速查询：**
> 用户：**风怎么样？**
> → 调用 GET /api/v1/dashboard（使用 Bearer 令牌）
> → 总结：先介绍最佳地点，再介绍其他信息

**查找新地点：**
> 用户：**迈阿密附近有哪些适合放风筝的地点？**
> → 将迈阿密地理编码为 lat=25.76, lng=-80.19
> → 调用 GET /api/v1/locations/nearby?lat=25.76&lng=-80.19&radius=100
> → 展示距离信息

**活动统计：**
> 用户：**我今年进行了多少次活动？**
> → 调用 GET /api/v1/sessions?limit=100
> → 按活动类型统计并总结结果

**实时查询：**
> 用户：**现在有人在外面活动吗？**
> → 调用 GET /api/v1/live-sessions
> → “您的朋友 Mike 正在 Cherry Beach 放翼装帆板——在 18 公里/小时的风速下活动了 30 分钟”

**日历操作：**
> 用户：**安排这个周末的适合时间**  
> → 调用 GET /api/v1/wind-report?when=weekend  
> → 为每个时段创建日历事件  
> → “已在您的日历中添加 2 个活动：周六下午 2 点到 6 点在 Cherry Beach 放风筝，周日上午 10 点到 1 点在 Hanlan’s 放翼装帆板”

**特定日期：**
> 用户：**周六的情况如何？**
> → 调用 GET /api/v1/wind-report?when=weekend  
> → 传达响应中的相关日期

**没有适合的条件：**
> 用户：**明天适合去放风筝吗？**
> → 调用 GET /api/v1/wind-report?when=tomorrow  
> → 如果没有适合的时段：**明天不适合活动——预报中没有合适的条件。**

**地点未找到：**
> 用户：**Wasaga Beach 的风怎么样？**
> → 调用 GET /api/v1/wind-report?spot=Wasaga+Beach  
> → 响应：未找到该地点  
> → 回复：**WindSensei 尚未收录 Wasaga Beach 的数据。您是否希望我提交添加该地点的请求？**  
> 用户：**是的，请添加。**  
> → 调用 POST /api/v1/spot-request 并传递 `{"spotName": "Wasaga Beach"}`  
> → 回复：**已提交添加 Wasaga Beach 的请求。WindSensei 团队会审核。**