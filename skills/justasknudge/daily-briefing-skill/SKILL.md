---
name: daily-briefing
description: 自动化的每日晨间简报生成器。该工具会获取天气信息、日历事件、新闻、人工智能/技术领域的最新动态以及OpenClaw的相关见解，并提供实用的建议。每日早上7点通过iMessage将生成的简报发送给用户。
homepage: https://github.com/nudge/openclaw-skills/tree/main/daily-briefing
metadata:
  {
    "openclaw":
      {
        "emoji": "📰",
        "requires": { "bins": ["curl", "python3"] },
        "schedule": "0 7 * * *",
        "delivery": "paulkingham@mac.com",
      },
  }
---
# 每日晨间简报

这是一个自动化的每日简报系统，它将天气信息、日历事件、新闻资讯、AI/科技动态以及OpenClaw的相关内容整合成一条简洁的晨间消息，通过iMessage发送给用户。

## 功能概述

每天早上7点，该简报会提供以下信息：

1. **天气**：伦敦莱顿地区的当前天气状况及预测。
2. **日历**：当天的日程安排及背景信息。
3. **热门新闻**：来自可靠新闻来源的头条新闻。
4. **AI/科技动态**：3-4条有趣的AI和科技领域的发展动态。
5. **OpenClaw精选**：最新的OpenClaw相关资讯、新技能、社区动态及使用建议。
6. **实用建议**：有助于提升工作效率的实用建议或改进措施。

## 快速入门

### 手动运行

```bash
cd /Users/nudge/openclaw/skills/daily-briefing
./scripts/generate-briefing.sh
```

### 通过iMessage发送

```bash
cd /Users/nudge/openclaw/skills/daily-briefing
./scripts/send-briefing.sh paulkingham@mac.com
```

## 配置

编辑 `/Users/nudge/openclaw/skills/daily-briefing/config/config.yaml` 文件以配置简报内容：

```yaml
location:
  city: "Leyton"
  region: "London"
  country: "UK"
  latitude: 51.5667
  longitude: -0.0167

delivery:
  recipient: "paulkingham@mac.com"
  channel: "imessage"
  time: "07:00"
  timezone: "Europe/London"

content:
  news_sources:
    - "bbc"
    - "guardian"
    - "techcrunch"
  ai_sources:
    - "openai"
    - "anthropic"
    - "hacker-news"
  max_news_items: 5
  max_ai_items: 4

preferences:
  temperature_unit: "celsius"
  include_weather_icon: true
  include_calendar_details: true
```

## 定时任务设置

该简报会通过OpenClaw的定时任务系统自动运行：

```bash
# Check current cron jobs
openclaw cron list

# The briefing is pre-configured. To update schedule:
openclaw cron update daily-briefing --schedule="0 7 * * *"
```

### 手动添加定时任务（如需）

将以下命令添加到系统的crontab文件中（在OpenClaw环境中执行）：

```cron
0 7 * * * cd /Users/nudge/openclaw/skills/daily-briefing && ./scripts/generate-and-send.sh
```

## 脚本说明

| 脚本          | 功能                        |
|----------------|---------------------------|
| `generate-briefing.sh` | 生成简报文本                   |
| `send-briefing.sh <recipient>` | 生成并通过iMessage发送简报           |
| `generate-and-send.sh` | 完整的简报生成与发送流程           |
| `lib/weather.py`    | 天气数据获取模块                   |
| `lib/news.py`    | 新闻聚合模块                   |
| `lib/calender.py`    | 日历信息处理模块                   |
| `lib/ai_pulse.py`    | AI/科技新闻处理模块                   |
| `lib/openclaw_dive.py` | OpenClaw相关资讯处理模块           |

## 自定义设置

### 更改发送地点

编辑 `config/config.yaml` 文件中的 `location` 配置项。

### 添加新闻来源

修改 `lib/news.py` 文件以添加新的RSS源或API接口。默认新闻来源包括：
- BBC News
- The Guardian
- TechCrunch
- Hacker News

### 更改发送时间

通过调整 `config/config.yaml` 文件中的定时任务设置来更改发送时间。

### 自定义内容显示

编辑 `scripts/generate-briefing.sh` 文件，将不需要的内容部分注释掉。

## 输出格式

简报会生成格式整齐的消息，例如：

```
📰 Good morning! Here's your briefing for Monday, February 16

🌤️ Weather in Leyton
   Partly cloudy, 8°C / 46°F
   High: 12°C · Low: 5°C
   💧 10% rain · 💨 12km/h wind

📅 Today
   09:00 - Team standup
   14:00 - Client call (Zoom)

📰 Top Stories
   • Headline one...
   • Headline two...
   • Headline three...
   • Headline four...
   • Headline five...

🤖 AI/Tech Pulse
   • OpenAI releases new feature...
   • Anthropic updates Claude...
   • Hacker News trending: ...

🦾 OpenClaw Deep Dive
   New this week: skill-name (description)
   Community highlight: ...
   Try this: ...

💡 Two Things to Try
   1. First suggestion...
   2. Second suggestion...
```

## 常见问题与解决方法

### 天气信息无法显示

- 检查网络连接是否正常。
- 确认 `wttr.in` 网站可访问：`curl wttr.in/London?format=3`
- 可以尝试使用备用API（如Open-Meteo）。

### 日历内容为空

- 确保终端具有访问日历的权限。
- 检查Calendar.app中是否包含当天的日程事件。
- 确保已安装 `icalBuddy`（可选，用于获取更详细的日历数据）。

### iMessage无法发送

- 确认Messages.app已登录。
- 检查收件人地址是否正确。
- 在系统设置中检查是否启用了自动化发送功能。
- 手动测试发送：`imsg send --to "recipient" --text "test"`。

### 新闻内容未显示

- 部分新闻来源可能需要API密钥才能正常显示。
- 默认实现使用网络搜索获取新闻标题。
- 如需正式使用，请将API密钥添加到 `config/secrets.yaml` 文件中。

## 所需依赖库

**必备依赖：**
- `curl`：用于网络请求。
- `python3`：用于数据处理。

**可选依赖（增强功能）：**
- `imsg`：用于通过iMessage发送消息（通过Homebrew安装：`brew install steipete/tap/imsg`）。
- `icalBuddy`：用于获取更详细的日历数据。
- `jq`：用于JSON数据解析。

## 系统架构

```
┌─────────────────┐
│  Cron (7am)     │
└────────┬────────┘
         │
┌────────▼────────┐
│ generate-and-   │
│ send.sh         │
└────────┬────────┘
         │
    ┌────┴────┬────────┬────────┬──────────┬──────────┐
    ▼         ▼        ▼        ▼          ▼          ▼
┌───────┐ ┌──────┐ ┌──────┐ ┌────────┐ ┌──────────┐ ┌─────────┐
│weather│ │calendar│ │news │ │ai_pulse│ │openclaw  │ │suggestions│
│  .py  │ │  .py   │ │ .py │ │  .py   │ │_dive.py  │ │  .py    │
└────┬──┘ └───┬────┘ └──┬───┘ └────┬───┘ └────┬─────┘ └────┬────┘
     └─────────┴─────────┴──────────┴──────────┴────────────┘
                             │
                    ┌────────▼────────┐
                    │  format-output  │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │  imsg send      │
                    └─────────────────┘
```

## 更新与维护

该技能会自动记录上次运行的时间。您可以通过以下命令查看运行状态：

```bash
cat /Users/nudge/openclaw/skills/daily-briefing/.last-run
cat /Users/nudge/openclaw/skills/daily-briefing/.last-status
```

如需更新该技能的功能，请按照文档中的说明进行操作。

## 扩展建议

- **周末版**：为周六和周日提供不同的内容。
- **会议前简报**：在日历事件前15分钟发送简报。
- **旅行模式**：显示目的地城市的天气信息。
- **股票/加密货币价格**：添加金融数据。
- **播客推荐**：根据用户的收听历史推荐相关播客。
- **健康统计**：显示前一天的活动数据。
- **智能家居状态**：简要显示连接的智能设备状态。