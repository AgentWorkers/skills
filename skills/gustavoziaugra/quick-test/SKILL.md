---
name: quick-test
description: 快速系统测试，用于验证 OpenClaw 环境是否正常。通过执行简单命令并验证其输出结果来进行测试。适用于检查 OpenClaw 是否正常运行、验证命令执行情况，或需要执行某个简单命令的场景。非常适合用于调试或确认系统状态。
---

# 快速测试

## 概述

这是一个简单的系统测试，用于验证 OpenClaw 环境。运行基本命令并验证输出结果。

## 快速入门

```bash
# Run all tests
python3 scripts/run_tests.py

# Run specific test
python3 scripts/run_tests.py --test date
python3 scripts/run_tests.py --test files

# Test with custom command
python3 scripts/run_tests.py --command "echo 'Hello World'"
```

## 安装

1. **克隆此仓库：**
```bash
git clone https://github.com/GustavoZiaugra/quick-test-skill.git
cd quick-test-skill
```

2. **无需依赖项**  
   - 仅使用 Python 标准库  
   - 不需要外部依赖

## 使用方法

### 运行所有测试

```bash
python3 scripts/run_tests.py
```

该测试将：
- 检查 Python 是否可用  
- 验证当前工作目录  
- 测试文件系统访问权限  
- 运行系统命令  
- 验证所有输出结果

### 运行特定测试

```bash
# Test date functionality
python3 scripts/run_tests.py --test date

# Test file system
python3 scripts/run_tests.py --test files

# Run custom command
python3 scripts/run_tests.py --command "pwd"
```

## 参数

| 参数 | 描述 | 默认值 |
|---------|---------|---------|
| `--test` | 要运行的特定测试 | `all` | `--test date` |
| `--command` | 要执行的自定义命令 | `none` | `--command "ls -la"` |
| `--quiet` | 抑制输出 | `false` | `--quiet` |

## 输出格式

```
✅ Quick Test Results

System Status: OK
Python: 3.12.7
Working Directory: /home/zig/.openclaw/workspace

Tests Run: 3/3 Passed
[✓] Python test
[✓] Date test
[✓] File system test

All tests passed successfully!
```

## 功能特性

- ✅ **Python 可用性检查** - 确认已安装 Python 3.x  
- ✅ **系统命令执行** - 运行并验证系统命令  
- ✅ **文件系统访问** - 验证目录访问权限  
- ✅ **自定义命令支持** - 支持执行任何命令并进行验证  
- ✅ **工作目录检查** - 确认当前工作目录位置  
- 📝 **详细日志记录** - 提供详细的调试日志

## 工作原理

1. **系统检查** - 验证 Python 和操作系统环境  
2. **目录检查** - 验证工作目录的权限  
3. **测试执行** - 运行请求的测试  
4. **结果验证** - 检查输出结果  
5. **报告** - 以清晰的方式显示成功/失败信息  

## 使用场景

### 系统验证

确认 OpenClaw 正在正确运行：

```bash
python3 scripts/run_tests.py --test date --test files --test command
```

### 调试命令

运行特定的系统命令：

```bash
python3 scripts/run_tests.py --command "pwd"
python3 scripts/run_tests.py --command "ls -la"
python3 scripts/run_tests.py --command "env | head -10"
```

### 测试前/后操作

使用快速测试来验证系统在测试前后的状态：

```bash
# Before installing a skill
python3 scripts/run_tests.py

# After installing a skill
python3 scripts/run_tests.py
```

## 示例

### 基本测试

```bash
# Check if everything works
python3 scripts/run_tests.py
```

输出结果：
```
✅ Quick Test - System Verification

Environment Check:
✓ Python 3.12.7
✓ Linux Ubuntu
✓ User: zig
✓ Working Directory: /home/zig/.openclaw/workspace

Tests:
[✓] Command: `pwd` → Passed
[✓] Command: `ls -la` → Passed
[✓] Command: `date` → Passed

All tests passed!
```

### 自定义命令

```bash
# Run a specific command
python3 scripts/run_tests.py --command "echo 'Custom test'"
```

输出结果：
```
Custom Command: echo 'Custom test'
Result: ✅ Passed
Output: Custom test
```

## 测试结果

### 成功指标

- ✅ **所有测试通过** - 系统运行正常  
- ⚠️ **部分测试失败** - 发现了小问题  
- ❌ **严重故障** - 需要关注的系统问题  

### 常见测试失败原因

- **权限拒绝**：`pwd` 或 `ls` 命令返回错误  
  - 请检查用户权限  
- **命令未找到**：自定义命令无法执行  
  - 请确认命令是否存在  
- **目录未找到**：工作目录检查失败  
  - 请检查路径和权限  

## 包含的脚本

### scripts/run_tests.py  
  - 主要的测试执行脚本  

### scripts/tests.py  
  - 包含各个测试功能的测试套件  

### scripts/system_check.py  
  - 系统验证功能  

## 设计理念

- **保持简单**  
  - 仅测试核心功能  
  - 避免复杂的依赖关系  
  - 输出清晰易读  

- **快速反馈**  
  - 立即显示测试结果  
  - 明确显示成功/失败状态  
- **无需等待或超时**  

- **易于调试**  
  - 提供详细的错误信息  
  - 包含系统相关信息  
  - 提供修复建议  

## 资源

### scripts/run_tests.py  
  - 执行所有测试并格式化结果的脚本  

### scripts/tests.py  
  - 用于系统验证的各个测试函数  

### scripts/system_check.py  
  - 系统验证和环境检查功能  

## 依赖项

**无依赖项！**  
  - 仅使用 Python 标准库（`sys`, `os`, `subprocess`, `datetime`, `json`, `pathlib`）  

## 限制

- **用户权限**：需要具有目录的读写权限  
- **系统命令**：必须位于系统路径（PATH）中  

- **系统差异**：不同操作系统（Linux, macOS, Windows）的行为可能有所不同  

## 故障排除

- **测试失败**：  
  - 检查用户权限  
  - 确认 Python 是否已安装（`python3 --version`）  
  - 检查命令是否存在  
  - 查看日志以获取具体错误信息  

- **命令无法执行**：  
  - 确认命令是否存在（使用 `which` 或 `whereis`）  
  - 在其他环境中直接测试该命令  
  - 检查系统路径（PATH）  

- **Python 未找到**：  
  - 确认 Python 版本（至少 3.7）  
  - 重新安装 Python（如需要）  

## 许可证

MIT 许可证 - 可用于个人和商业用途。  

## 致谢

由 **Gustavo (GustavoZiaugra)** 使用 OpenClaw 开发。  

- 简单的系统测试  
- Python 环境验证  
- 命令验证  
- 便于调试的输出结果  

---

**更多 OpenClaw 相关技能请访问 ClawHub.com**  
⭐ **如果您觉得这个仓库有用，请给它点赞！**  

✅ **简单、快速、无依赖** - 适用于所有环境，可测试所有功能。