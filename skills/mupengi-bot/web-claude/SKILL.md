---
name: web-claude
description: 统一的网络搜索功能。备用搜索顺序为：web_search(Brave) → duckduckgo → claude.ai。会自动缓存搜索结果（保存到内存或“research/”文件夹中）。
author: 무펭이 🐧
---
# 统一网络搜索 🐧

通过三层备用策略实现可靠的网络搜索：**Brave API → DuckDuckGo → claude.ai 浏览器**

## 搜索策略

### 第一层：web_search（Brave API）——⚡ 快速且可靠（推荐）

使用 OpenClaw 内置的 `web_search` 工具。

**优点：**
- 响应速度快（1-2 秒）
- 结果以结构化的 JSON 格式返回
- 支持设置结果更新频率（pd=24 小时，pw=1 周，pm=1 个月）
- 提供 `search_lang` 和 `country` 参数，支持韩语/地区搜索

**缺点：**
- 需要 Brave API 密钥
- 如果没有密钥，会显示 `missing_brave_api_key` 错误

### 第二层：duckduckgo-search — 🔒 注重隐私（备用方案）

当 Brave API 失效时，使用 DuckDuckGo API。

**优点：**
- 不需要 API 密钥
- 保护用户隐私
- 支持多种搜索类型（文本、新闻、图片、视频）

**缺点：**
- 搜索结果质量低于 Brave
- 有请求次数限制（连续请求过多会被阻止）

### 第三层：web-claude（浏览器）——🧠 分析+搜索（最后手段）

在 claude.ai 浏览器标签页中触发网络搜索。

**优点：**
- 不需要 API 密钥
- claude.ai 会自动进行网络搜索、分析并总结结果
- 非常适合复杂的研究任务

**缺点：**
- 搜索速度较慢（15-30 秒）
- 需要使用支持 port 18800 的浏览器，并登录 openclaw 账户
- 免费账户有每日请求次数限制
- 存在自动化检测风险

## 自动备用逻辑

所有搜索结果会自动保存到 `memory/research/` 文件夹中：

**文件命名规则：**

**保存的内容包括：**
- 搜索查询
- 时间戳
- 使用的搜索方式（Brave/DuckDuckGo/claude.ai）
- 搜索结果（链接 + 摘要）
- 提取的洞察信息

**示例：**

## 使用方法

### 通用搜索

系统会自动尝试使用第一层（Brave API），如果失败则尝试第二层（DuckDuckGo），最后使用第三层（claude.ai）。

### 强制使用特定搜索方式

### 使用结果更新频率参数

### 韩语搜索

### web-claude 浏览器使用细节

### 先决条件

- openclaw 浏览器已启动（端口 18800 已打开）
- 已登录 claude.ai 账户（使用 openclaw 账户信息）

### 自动化操作步骤

### 提示

- **明确搜索意图**：使用如 “最新数据”、“截至 2026 年” 等时间相关关键词
- **明确请求**：使用 “搜索并告诉我” 等指令
- **避免重复请求**：不要连续快速提出相同问题
- **注意自动化检测**：不要过于频繁地自动执行搜索操作

## 与其他技能的集成

### competitor-watch

在监控竞争对手时使用此统一搜索功能：
- 快速检查：优先使用 `web_search`
- 深入分析：结合使用 `web_search` 和 `duckduckgo`
- 复杂分析：使用 `web-claude`

### cardnews

使用搜索结果进行卡片新闻的研究：
- 选择主题后进行统一搜索
- 使用搜索结果来规划卡片新闻内容

### yt-digest

在 YouTube 摘要后使用该功能进行进一步搜索：
- 根据视频主题进行额外网络搜索
- 使用搜索结果获取补充信息

## 如何选择合适的搜索方式

| 情况 | 推荐的搜索方式 |
|-----------|-------------------|
| 需要快速搜索 | `web_search`（Brave API） |
| 无 Brave API 密钥 | `duckduckgo-search` |
- 注重隐私 | `duckduckgo-search` |
- 需要分析+总结 | `web-claude` |
- 复杂研究 | `web-claude` |
- 图片/视频搜索 | `duckduckgo-search` |
- 获取最新新闻（24 小时内） | `web_search`（设置 `freshness="pd"`）

## 注意事项

- **web_search**：需要 Brave API 密钥（若密钥缺失会自动切换到备用方案）
- **duckduckgo**：连续请求过多会被限制（请求之间需间隔 1 秒）
- **web-claude**：有每日请求次数限制，且需要使用浏览器
- 搜索结果会自动保存到 `memory/research/` 文件夹中

---
> 🐧 由 **무펭이** 开发 — [Mupengism](https://github.com/mupeng) 生态系统中的技能