---
name: appointment-scheduler
description: 自动化预约管理系统，适用于美容院、诊所、工作室和照相馆。该系统能够处理预订请求、日历同步、冲突检测、提醒通知、未到场的客户追踪以及候补名单管理等功能。
author: 무펭이 🐧
---
# 预约调度器 📅  
专为服务行业设计的自动化预约管理系统。  

## 主要功能  
1. **接收预约请求**：解析来自 DM/KakaoTalk/电话的预约请求（已转换为文本格式）  
2. **同步日历**：自动与 Google Calendar/Naver Calendar 同步  
3. **冲突检测**：防止重复预约  
4. **提醒通知**：预约前1天及预约前2小时自动发送提醒  
5. **处理爽约情况**：记录爽约行为，并标记频繁爽约的客户  
6. **等待名单管理**：当所有预约位置已满时自动加入等待名单，并在预约取消时发送通知  

## 目标应用场景  
- 美容院（Beauty Salons）  
- 医疗诊所（Clinics）  
- 摄影工作室（Studios）  
- 摄影亭（Photo Booths）  

## 快速入门  

### 1. 初始化配置  
```bash
node <skill-dir>/scripts/init-config.js
```  
此步骤会创建 `config/appointment-scheduler.json` 文件，其中包含：  
- 服务营业时间  
- 服务时长默认值  
- 日历连接设置  
- 提醒通知时间偏好  
- 爽约政策  

### 2. 处理收到的预约请求  
**通过短信接收的请求：**  
```bash
node <skill-dir>/scripts/parse-booking.js --text "내일 오후 3시에 컷 예약 가능할까요? - 김철수 010-1234-5678"
```  
**通过对话框接收的请求：**  
```
"고객이 예약 요청했어요: [message content]"
```  
系统会：  
1. 解析日期/时间、服务类型、客户名称及联系方式  
2. 检查日历是否存在冲突  
3. 如果有空位 → 进行预约并确认  
4. 如果存在冲突 → 提供替代时间或加入等待名单  

### 3. 手动预约  
```bash
node <skill-dir>/scripts/book.js \
  --date "2026-02-20" \
  --time "15:00" \
  --duration 60 \
  --service "컷" \
  --customer "김철수" \
  --phone "010-1234-5678"
```  

### 4. 查看预约安排  
```bash
# Today's appointments
node <skill-dir>/scripts/check-schedule.js --date today

# Specific date
node <skill-dir>/scripts/check-schedule.js --date 2026-02-20

# This week
node <skill-dir>/scripts/check-schedule.js --week
```  

### 5. 发送提醒通知  
**手动触发：**  
```bash
node <skill-dir>/scripts/send-reminders.js
```  
**通过 cron 任务自动触发：**  
```
# Daily at 9:00 AM - send 1-day-before reminders
0 9 * * * node /path/to/skills/appointment-scheduler/scripts/send-reminders.js --type day-before

# Hourly - send 2-hour-before reminders
0 * * * * node /path/to/skills/appointment-scheduler/scripts/send-reminders.js --type hour-before
```  

### 6. 处理爽约情况  
```bash
# Mark customer as no-show
node <skill-dir>/scripts/mark-noshow.js --booking-id abc123

# Check no-show stats
node <skill-dir>/scripts/noshow-report.js

# Flag repeat offenders (3+ no-shows)
node <skill-dir>/scripts/noshow-report.js --flag-repeat
```  

### 7. 等待名单管理  
```bash
# Add to waitlist
node <skill-dir>/scripts/waitlist.js add \
  --date "2026-02-20" \
  --time "15:00" \
  --customer "이영희" \
  --phone "010-9999-8888"

# Check waitlist
node <skill-dir>/scripts/waitlist.js list --date 2026-02-20

# Notify waitlist on cancellation
node <skill-dir>/scripts/waitlist.js notify --booking-id abc123
```  

## 数据存储  
数据存储路径：`workspace/data/appointments/`  

## 日历集成  
### Google Calendar  
1. 启用 Google Calendar API  
2. 下载 OAuth 凭据并保存至 `~/.secrets/google-calendar-credentials.json`  
3. 首次运行时需要浏览器授权  
4. 保存的令牌会自动更新至 `~/.secrets/google-calendar-token.json`  
**同步脚本：**  
```bash
node <skill-dir>/scripts/sync-google-calendar.js
```  

### Naver Calendar  
使用 Naver Calendar API（如可用）；若不可用则使用浏览器自动化方式。  
**配置方式**：在 `config/appointment-scheduler.json` 中添加 Naver 账户信息。  

## 预约请求解析  
该系统能理解以下自然语言输入：  
- “明天下午3点预约理发”  
- “2月20日15:00可以剪发吗？”  
- “下周周一上午预约理发+染发”  
- “3点预约摄影亭——客户：金哲洙，电话：010-1234-5678”  
**提取的字段包括：**  
- 日期（相对或绝对时间）  
- 时间  
- 服务类型  
- 客户名称  
- 联系方式（电话/电子邮件/用户名）  
- 特殊备注  

## 冲突检测  
在确认预约前：  
1. 检查指定日期/时间段的现有预约情况  
2. 计算服务时长是否重叠  
3. 如果存在冲突 → 提供最近的可用时间段  
4. 如果所有位置已满 → 将客户加入等待名单  

**缓冲时间**：可在 `config` 中配置（默认值为预约之间间隔10分钟）  

## 提醒通知系统  
**默认通知时间：**  
- 预约前1天上午9:00：**“您明天下午3点有预约”  
- 预约前2小时：**“2小时后有预约”  
**通知渠道**（根据客户联系方式自动选择）：  
- 电话 → SMS（通过短信工具）  
- Instagram → 私信  
- KakaoTalk → 消息通知  
**自定义设置**：可在 `config/appointment-scheduler.json` 中修改通知时间。  

## 爽约管理  
- 如果客户在15分钟宽限期內未出现，系统会将其标记为爽约  
- 爽约记录会保存至 `noshow/history.json` 文件  
- 系统会统计每位客户的爽约次数  

**自动标记规则**：  
- 累计3次爽约后，该客户将被标记为“频繁爽约者”  
- 被标记的客户下次预约需支付押金  

## 等待名单自动通知  
当预约被取消时：  
1. 查找同一日期/时间的等待名单中的客户  
2. 自动发送通知：“预约位置已空，请随时预约”  
- 采用先到先得的原则；客户确认后即可立即安排预约  

## 语音交互支持  
支持电话→短信→预约的交互流程：  
1. 通过语音转文字工具（STT）接收通话内容  
2. 解析预约请求  
3. 确认客户是否可用  
4. 发送短信确认  

## 钩子函数集成  
- **预预约钩子**：预约前检查营业时间和服务可用性  
- **预约后钩子**：预约完成后发送确认信息、更新日历并记录事件  
- **冲突钩子**：在检测到冲突时提供替代时间或建议加入等待名单  

## 事件总线集成  
数据存储路径：`events/appointment-YYYY-MM-DD.json`  

## Cron 任务设置  
**推荐的 cron 任务配置：**  
```cron
# Send day-before reminders at 9 AM
0 9 * * * node /path/to/skills/appointment-scheduler/scripts/send-reminders.js --type day-before

# Send 2-hour reminders every hour
0 * * * * node /path/to/skills/appointment-scheduler/scripts/send-reminders.js --type hour-before

# Sync calendar every 30 min
*/30 * * * * node /path/to/skills/appointment-scheduler/scripts/sync-google-calendar.js

# Daily no-show report at 8 PM
0 20 * * * node /path/to/skills/appointment-scheduler/scripts/noshow-report.js --daily
```  

## 配置模板  
配置文件路径：`config/appointment-scheduler.json`  

## 常见问题及解决方法  
| 问题 | 解决方案 |  
|------|---------|  
| 预约解析失败 | 检查 `parse-booking.js` 的输出结果，添加自定义解析规则 |  
| 日历同步失败 | 确认 OAuth 凭据是否有效，检查令牌是否过期 |  
| 提醒通知未发送 | 检查 cron 任务状态及短信工具配置 |  
| 发生重复预约 | 确保冲突检测功能已启用，并检查缓冲时间设置 |  
| 等待名单通知失败 | 检查客户联系方式及通知渠道设置 |  

## 高级功能  
- **自定义服务类型**：编辑 `config/appointment-scheduler.json` 文件以支持更多服务类型  
- **设置服务禁用时间**：  
  ```bash
# Mark time slot as unavailable
node <skill-dir>/scripts/block-time.js \
  --date "2026-02-20" \
  --start "12:00" \
  --end "13:00" \
  --reason "점심시간"
```  
- **批量导入预约信息**：  
  ```bash
# Import from CSV
node <skill-dir>/scripts/import-bookings.js --file bookings.csv
```  
- **导出报告**：  
  ```bash
# Monthly booking report
node <skill-dir>/scripts/export-report.js --month 2026-02 --format json

# No-show analysis
node <skill-dir>/scripts/noshow-report.js --month 2026-02 --export csv
```  

## 实施效果：  
- 韩文日期/时间表达式的自然语言解析准确率约为85%  
- 通过 Instagram 私信发送的提醒通知成功率超过95%，通过短信发送的成功率超过80%  
- 实施预约前1天提醒后，爽约率降低了40%  
- 等待名单自动通知功能使预约成功率提高了25%  

---

> 🐧 由 **무펭이** 开发 — [무펭이즘(Mupengism)] 生态系统中的技能