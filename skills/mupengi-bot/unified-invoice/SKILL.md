---
name: unified-invoice
description: 集成报价单/税务发票生成器：支持生成符合韩国标准的报价单（包含企业注册号、增值税信息），以及适用于自由职业者的多语言发票（含增值税信息）。具备交易方/商品信息数据库功能，支持PDF格式输出，并能自动进行费用计算。
version: 1.0.0
author: 무펭이 🐧
---
# 统一发票生成器 📄🐧

> **整合功能**：整合了原有的 `invoice-gen` 和 `korean-invoice` 两个工具

将韩国风格的报价单/税务发票功能与自由职业者发票功能整合到一个工具中。

## 主要功能

- 📋 **韩国风格报价单**：自动计算企业注册号、供应价格和增值税
- 🧾 **税务发票**：区分收据和账单，并生成审批编号
- 💼 **自由职业者发票**：支持 Markdown/PDF 格式，支持多种语言（KRW/USD/EUR）
- 🏢 **客户管理**：存储和管理企业信息及联系方式
- 📦 **商品管理**：保存常用商品以便重复使用
- 📄 **PDF 输出**：自动将 HTML 文件转换为 PDF 格式
- 🔢 **自动计算**：自动计算供应价格、增值税和总金额

## 快速入门

### 创建韩国风格报价单

```bash
node scripts/generate.js quote \
  --client "무펭이즘" \
  --items "포토부스 대여,2,500000" \
  --notes "부가세 별도"
```

### 创建税务发票

```bash
node scripts/generate.js tax \
  --client "무펭이즘" \
  --items "포토부스 대여,1,500000" \
  --type 영수
```

### 自由职业者发票（英文版）

```bash
node scripts/generate.js invoice \
  --client "Acme Corp" \
  --items "Web development,40h,50" \
  --currency USD \
  --lang en
```

## 文档类型

| 类型 | 说明 | 用途 |
|------|------|------|
| `quote` | 韩国风格报价单 | 向客户提供报价 |
| `tax` | 税务发票 | 用于增值税申报的正式税务文件 |
| `invoice` | 自由职业者发票 | 用于国际交易或自由职业者工作 |

## 报价单字段

| 字段 | 说明 | 是否必填 | 默认值 |
|------|------|------|--------|
| `--client` | 客户名称或 ID | ✅ | - |
| `--items` | 商品列表（商品名称、数量、单价） | ✅ | - |
| `--issue-date` | 创建日期 | ❌ | 当前日期 |
| `--valid-until` | 有效期 | ❌ | +30 天 |
| `--notes` | 备注 | ❌ | - |
| `--include-vat` | 是否包含增值税 | ❌ | true |
| `--currency` | 货币（KRW/USD/EUR） | ❌ | KRW |
| `--lang` | 语言（ko/en） | ❌ | ko |

## 税务发票字段

| 字段 | 说明 | 是否必填 | 默认值 |
|------|------|------|--------|
| `--client` | 收货方（客户 ID） | ✅ | - |
| `--items` | 商品列表 | ✅ | - |
| `--issue-date` | 创建日期 | ✅ | - |
| `--type` | 收据/账单 | ❌ | 收据 |
| `--notes` | 备注 | ❌ | - |

## 自动计算

### 韩国风格报价单（增值税 10%）

```
공급가액:     500,000 원
부가세(10%):   50,000 원
합계:         550,000 원
```

- 输入供应价格时自动添加 10% 的增值税
- 输入总金额时自动计算增值税（总金额 ÷ 1.1）
- 自动汇总各商品的小计

### 自由职业者发票（包含增值税）

```
Subtotal:      $2,000
VAT (10%):       $200
Withholding:    -$66  (3.3% for Korean freelance)
Total:        $2,134
```

- 可自定义各国税率
- 自动计算预扣税（韩国自由职业者为 3.3%）

## 客户管理

```bash
# 거래처 추가
node scripts/manage-clients.js add "무펭이즘" \
  --business-number "123-45-67890" \
  --ceo "김형님" \
  --address "서울시 강남구..." \
  --phone "010-1234-5678" \
  --email "contact@mufism.com"

# 거래처 목록
node scripts/manage-clients.js list

# 거래처 상세 조회
node scripts/manage-clients.js view "무펭이즘"

# 거래처 수정
node scripts/manage-clients.js edit "무펭이즘" --phone "010-9999-9999"

# 거래처 삭제
node scripts/manage-clients.js remove "무펭이즘"
```

客户数据存储路径：`data/clients.json`

## 商品管理

```bash
# 품목 추가
node scripts/manage-items.js add "포토부스 대여" \
  --price 500000 \
  --unit "일"

# 품목 목록
node scripts/manage-items.js list

# 품목 수정
node scripts/manage-items.js edit "포토부스 대여" --price 600000

# 품목 삭제
node scripts/manage-items.js remove "포토부스 대여"
```

商品数据存储路径：`data/items.json`

使用已保存的商品：
```bash
node scripts/generate.js quote \
  --client "무펭이즘" \
  --item-ids "포토부스 대여,출장비"
```

## 模板

### 韩国风格报价单（`templates/quote.html`）
- 供应商信息（企业注册号、公司名称、负责人、地址、联系方式）
- 收货方信息
- 创建日期、有效期
- 商品列表（商品名称、规格、数量、单价、供应价格）
- 总计（供应价格、增值税、总金额）
- 备注

### 税务发票（`templates/tax-invoice.html`）
- 生成的审批编号
- 供应商/收货方信息（企业注册号、公司名称、姓名、地址）
- 创建日期
- 商品列表
- 总计（供应价格、税额）
- 区分收据和账单

### 自由职业者发票（Markdown 格式）
- 专业的布局设计
- 发件人/收件人信息
- 带有小计的商品列表
- 税务计算信息
- 支付条款和银行信息
- 自动递增的发票编号（INV-YYYY-NNN）

### 创建自定义模板

```bash
cp templates/quote.html templates/my-custom.html
# 수정 후
node scripts/generate.js quote --template my-custom ...
```

## 输出格式

- **HTML**：直接在浏览器中查看
- **PDF**：将 HTML 文件转换为 PDF 格式（需要使用 OpenClaw 浏览器）
- **Markdown**：自由职业者发票（可编辑）

文件存储路径：`output/YYYY-MM-DD-{type}-{client}.{html|pdf|md}`

## 个人信息设置

`data/my-info.json`：

```json
{
  "businessNumber": "123-45-67890",
  "companyName": "무펭이즘",
  "ceo": "김무펭",
  "address": "서울시 강남구...",
  "phone": "010-1234-5678",
  "email": "contact@mufism.com",
  "bankAccount": "우리은행 1002-123-456789"
}
```

### 自由职业者信息（英文版）

```json
{
  "name": "John Doe",
  "email": "john@freelancer.com",
  "address": "123 Main St, New York, NY 10001",
  "phone": "+1-555-1234",
  "bankAccount": "Bank of America, Account #12345678"
}
```

## 使用示例

### 1. 向韩国客户发送报价单

```bash
# 거래처 미리 저장
node scripts/manage-clients.js add "무펭이즘" \
  --business-number "123-45-67890" \
  --ceo "김형님"

# 품목 미리 저장
node scripts/manage-items.js add "포토부스 대여" --price 500000 --unit "일"
node scripts/manage-items.js add "출장비" --price 100000 --unit "회"

# 견적서 생성
node scripts/generate.js quote \
  --client "무펭이즘" \
  --item-ids "포토부스 대여,출장비" \
  --notes "부가세 별도"
```

### 2. 发送税务发票

```bash
node scripts/generate.js tax \
  --client "무펭이즘" \
  --items "포토부스 대여,1,500000" \
  --type 영수 \
  --issue-date 2026-02-18
```

### 向海外客户发送发票

```bash
node scripts/generate.js invoice \
  --client "Acme Corp" \
  --items "Web development,40h,50" \
  --currency USD \
  --lang en \
  --notes "Payment due within 30 days"
```

## 集成功能

- **消息功能**：通过电子邮件/Discord 发送报价单
- **每日报告**：工作完成后自动将报价单转换为 PDF 格式
- **日历**：设置有效期/付款截止日期提醒
- **Trello/Notion**：发票生成后自动创建相关任务

## 安装方法

```bash
cd skills/unified-invoice
npm install
```

## 注意事项

- **企业注册号**：格式为 `123-45-67890`（包含连字符）
- **金额**：仅输入数字（不含逗号）
- **PDF 生成**：需要运行 OpenClaw 浏览器（端口 18800）
- **模板修改**：可以直接编辑 HTML 文件
- **多语言支持**：通过 `--lang en` 选项切换语言显示

## 常见问题解答

**PDF 生成失败**：
- 确保已运行 OpenClaw 浏览器（检查 `openclaw gateway status`）
- 确保端口 18800 可被访问

**客户/商品信息未加载**：
- 确保 `data/clients.json` 和 `data/items.json` 文件存在
- 检查 JSON 语法是否正确

**增值税计算错误**：
- 可通过 `--include-vat false` 选项排除增值税
- 如需手动计算，请输入商品信息（`--items "商品名称,数量,供应价格"`）

## 目录结构

```
unified-invoice/
├── SKILL.md                      # 이 파일
├── package.json
├── scripts/
│   ├── generate.js               # 견적서/세금계산서/인보이스 생성
│   ├── manage-clients.js         # 거래처 관리
│   └── manage-items.js           # 품목 관리
├── templates/
│   ├── quote.html                # 한국형 견적서
│   ├── tax-invoice.html          # 세금계산서
│   └── freelance-invoice.md      # 프리랜서 인보이스 (Markdown)
├── data/
│   ├── clients.json              # 거래처 DB
│   ├── items.json                # 품목 DB
│   └── my-info.json              # 내 사업자 정보
└── output/                       # 생성된 문서 저장
```

## 集成历史

| 原始工具 | 贡献内容 | 集成日期 |
|----------|----------|----------|
| korean-invoice | 韩国风格报价单/税务发票功能、企业注册号、HTML 模板 | 2026-02-18 |
| invoice-gen | 自由职业者发票功能、Markdown 模板、多语言支持 | 2026-02-18 |

---
> 🐧 由 **무펭이** 开发 — [무펭이즘(Mupengism)](https://github.com/mupeng) 生态系统的一部分