---
name: coworking-finder
description: 在巴西的城市中查找共享办公空间（coworking spaces）和传统办公室（office spaces）。当用户请求在巴西的特定城市或州内搜索共享办公空间、共享办公室或工作空间时，可以使用该功能；用户还可以根据社区（neighborhood）或地区（region）进行筛选。
---
# 合作空间查找器

使用 SerpAPI（结合 Google 搜索和 Google Local）在巴西城市中查找合作空间，若搜索失败则使用 Perplexity 作为备用方案。

## 输入

用户会输入如下类型的自然语言请求：
- “在米纳斯吉拉斯州的贝洛奥里藏特寻找合作空间”
- “在圣保罗州的圣保罗市，Vila Madalena 地区寻找合作空间”
- “在圣卡塔琳娜州的弗洛里亚诺波利斯市，Centro 区寻找合作空间”

从请求中提取以下信息：
- **城市**（必填）：城市名称
- **州**（必填）：巴西州的缩写（UF）
- **社区**（可选）：首选的社区或地区

## 工作流程

### 第 1 阶段：使用 SerpAPI 进行 Google 搜索

运行多次 SerpAPI 搜索，以获取合作空间的名称和相关信息：

```bash
# Primary searches (run all)
{serpapi}/scripts/search.sh "coworking {city} {state}" --country br --lang pt --num 10
{serpapi}/scripts/search.sh "espaço coworking {city}" --country br --lang pt --num 10
{serpapi}/scripts/search.sh "coworking space {city} Brazil" --country br --lang en --num 10
```

如果提供了 **社区** 参数，添加以下代码：
```bash
{serpapi}/scripts/search.sh "coworking {neighborhood} {city}" --country br --lang pt --num 10
```

其中：
- `{serpapi}` 的路径为 `/root/clawd/skills/serpapi-search`

从搜索结果中提取合作空间的名称及对应的 URL。

### 第 2 阶段：使用 Perplexity 进行深度搜索（如有需要）

如果第 1 阶段仅找到少于 3 个合作空间（或在没有提供社区参数时找到少于 5 个），则使用 Perplexity 进行深度搜索：

```bash
{perplexity}/scripts/search.sh --mode research --lang pt "lista completa de espaços de coworking em {city} {state} Brasil com endereço, telefone e site{neighborhood_clause}"
```

其中：
- `{perplexity}` 的路径为 `/root/clawd/skills/perplexity`
- `{neighborhood_clause}` 的值为 ``, na região de {neighborhood}`（如果提供了社区参数），否则为空

从 Perplexity 的搜索结果中提取合作空间的名称，以丰富信息列表。

### 第 3 阶段：通过 Google Local 获取详细信息

对于每个找到的合作空间，使用 Google Local 获取详细信息：

```bash
{serpapi}/scripts/search.sh "{coworking_name}" --engine google_local --country br --location "{city}, {state}, Brazil" --num 3
```

从 Google Local 的结果中提取以下信息：
- **名称**（标题）
- **Google 地图链接**（来自 `link` 字段，或根据 `place_id` 构建）
- **网站**（来自 `website` 字段）
- **地址**（来自 `address` 字段）
- **电话**（来自 `phone` 字段）
- **评分**（如果有的话）

### 第 4 阶段：进行网页抓取（针对未找到结果的合作空间）

如果 Google Local 无法返回特定合作空间的信息，直接从该合作空间的网站或 Instagram 页面中提取联系信息：

```bash
{serpapi}/scripts/search.sh "{coworking_name} {city} site telefone endereço" --country br --lang pt --num 5
```

使用 Jina Reader（路径为 `/root/clawd/skills/jina-reader`）来提取联系信息：

```bash
{jina}/scripts/read.sh "{website_url}"
```

其中：
- `{jina}` 的路径为 `/root/clawd/skills/jina-reader`

从网页内容中提取地址、电话和网站信息。

### 第 5 阶段：回退到传统办公空间

如果在所有阶段后，无论是否提供了社区参数，搜索结果都少于 3 个（提供社区参数时）或少于 5 个（仅搜索城市时），则将搜索范围扩大到传统办公空间：

```bash
{serpapi}/scripts/search.sh "escritório compartilhado {city} {state}" --country br --lang pt --num 10
{serpapi}/scripts/search.sh "sala comercial coworking {city}" --country br --lang pt --num 10
```

然后对新的搜索结果重复第 3 阶段（使用 Google Local）。

## 输出格式

将结果直接显示在聊天频道中：

```
🏢 Coworkings em {city}/{state}{neighborhood_line}:
{N} espaços encontrados

1. **{Name}**
   📍 {Address}
   🌐 {Website}
   📞 {Phone}
   ⭐ {Rating} (Google)
   🔗 {Google Maps/Local link}

2. **{Name}**
   ...

---
📚 Referências:
• {URL 1}
• {URL 2}
• {URL 3}
```

其中：
- `{neighborhood_line}` 的值为 ` — {neighborhood}`（如果提供了社区参数），否则为空

### 输出规则：
- 如果某个字段未找到，则省略该字段，不显示 “N/A”
- 包括搜索过程中使用的所有参考链接（Google 搜索结果页面、Perplexity 的搜索结果、访问的网站）
- 最小目标数量：提供社区参数时至少 3 个结果，仅搜索城市时至少 5 个结果
- 如果所有阶段后仍未满足目标数量，则显示实际找到的结果，并附上说明（表示搜索已穷尽）
- 可以根据评分（从高到低排序），如果没有评分信息，则按字母顺序排序

## 所需依赖的技能

此技能需要以下技能：
- **serpapi-search**：用于执行 Google 搜索和 Google Local 操作
- **perplexity**：用于进行深度搜索
- **jina-reader**：用于进行网页抓取