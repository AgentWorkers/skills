---
name: Ruby
slug: ruby
version: 1.0.1
description: 编写可靠的 Ruby 代码时，需要避免使用可变字符串（mutable strings）、块（blocks）中可能存在的陷阱以及元编程（metaprogramming）中的错误。
metadata: {"clawdbot":{"emoji":"💎","requires":{"bins":["ruby"]},"os":["linux","darwin","win32"]}}
---

## 快速参考

| 主题 | 文件 |
|-------|------|
| 可变字符串、对象相等性 | `objects.md` |
| 过程（Proc）与lambda表达式、返回行为 | `blocks.md` |
| 变量的可见性、`method_missing` 方法 | `methods.md` |
| 数组/哈希的修改机制 | `collections.md` |
`define_method` 方法与`eval` 的使用技巧 | `metaprogramming.md` |
| ActiveRecord 框架、N+1 问题、回调函数 | `rails.md` |

## 重要规则

- 字符串是可变的：`s = "hi"; s << "!"; t = s` 会导致 `t` 也包含 `"!"`  
- `==`、`equal?` 和 `eql?` 的区别：`==` 比较值，`equal?` 比较对象的身份，`eql?` 比较哈希值  
- 哈希的默认值是共享的：`Hash.new([])` 会创建一个共享的哈希对象，建议使用块形式来创建新的哈希  
- 在过程中使用 `return` 会从包含它的方法中返回；如果需要局部返回值，请使用 lambda 表达式  
- 块变量会“遮盖”外部作用域中的变量：`x = 1; [2].each { |x| }; x` 的结果仍然是 1（Ruby 3.0 及以后的版本）  
- 如果没有使用 `respond_to_missing?`，`method_missing` 方法会破坏 `respond_to?` 的正常工作  
- 在 Ruby 中，`private` 属性是针对每个对象的；`self.private_method` 无法被外部访问，但隐式的接收者（implicit receiver）仍然可以访问  
- `||=` 无法区分 `false` 和 `nil`，会用 `true` 替代 `false`  
- 冻结字符串字面量：`# frozen_stringLiteral: true` 可以使字符串不可变  
- `Symbol#to_proc` 方法：`&:method_name` 仅适用于无参数的方法  
- `rescue => e` 没有指定错误类型时，会捕获 `StandardError` 而不是其他类型的异常  
- `ensure` 语句总是会被执行，即使在函数返回之后也会执行，可用于清理操作