---
name: Bioinformatics
slug: bioinformatics
version: 1.0.0
homepage: https://clawic.com/skills/bioinformatics
description: 使用比对、变异检测和表达分析流程来分析 DNA、RNA 和蛋白质序列。
metadata: {"clawdbot":{"emoji":"🧬","requires":{"bins":["samtools","bcftools","bedtools","bwa","fastqc","fastp"],"config":["~/bioinformatics/"]},"os":["linux","darwin"]}}
---
## 设置

首次使用时，请阅读 `setup.md` 以获取集成指南。在用户同意的情况下，创建 `~/bioinformatics/` 目录以存储项目相关信息和偏好设置。

## 使用场景

当用户需要分析生物序列、运行基因组分析流程或解释测序数据时，可以使用该工具。该工具负责序列比对、变异检测、表达量分析以及数据格式转换等任务。

## 架构

所有数据存储在 `~/bioinformatics/` 目录下。具体存储结构请参考 `memory-template.md`。

```
~/bioinformatics/
├── memory.md         # Projects, preferences, reference genomes
├── pipelines/        # Saved pipeline configurations
└── results/          # Analysis outputs and logs
```

## 快速参考

| 主题 | 对应文件 |
|-------|------|
| 设置流程 | `setup.md` |
| 内存管理模板 | `memory-template.md` |
| 文件格式 | `formats.md` |
| 工具命令 | `tools.md` |
| RNA-seq 分析流程 | `rnaseq.md` |
| 变异检测 | `variants.md` |

## 核心规则

### 1. 首先验证输入数据质量
在进行任何分析之前，请检查输入数据的质量：
- **FASTQ 文件**：运行 FastQC 工具，检查每个碱基的质量及适配器的使用情况。
- **BAM 文件**：确认文件已排序并建立索引（使用 `samtools quickcheck`）。
- **VCF 文件**：验证文件格式（使用 `bcftools view -h`）。
**注意**：质量不佳的输入数据会导致分析结果错误。务必先进行质量控制。

### 2. 一致使用参考基因组
请记录每个项目使用的参考基因组版本：
- **人类基因组**：推荐使用 GRCh38/hg38 或 GRCh37/hg19。
- **小鼠基因组**：推荐使用 GRCm39/mm39 或 GRCm38/mm10。
- 使用不同的参考基因组会导致分析结果无效。
请将参考基因组信息存储在每个项目的 `~/bioinformatics/memory.md` 文件中。

### 3. 保留原始数据
**严禁** 修改原始的 FASTQ/BAM 文件：
- 在处理数据时请使用副本。
- 保持原始文件的只读属性。
- 记录所有数据转换的步骤。

### 4. 注意资源消耗
生物信息学分析工具可能会消耗大量系统资源：
- 在执行操作前请检查文件大小。
- 尽可能使用流式处理方式（例如 `samtools view | ...`）。
- 估算所需内存空间（例如，人类基因组分析可能需要约 6GB 内存）。
- 当操作耗时超过 10 分钟时请提前发出警告。

### 5. 确保分析结果的可重复性
所有分析过程都必须具备可重复性：
- 记录使用的工具版本（使用 `samtools --version`）。
- 保存所有命令参数。
- 对于关键分析，记录输入文件的校验和值。

## 常见问题及解决方法

- **染色体命名错误**：`chr1` 和 `1` 的区别可能导致分析失败。请使用 `sed 's/^chr/'` 命令进行修正。
- **BAM 文件未排序**：大多数工具要求输入文件已排序。错误表现可能为程序崩溃或分析结果错误，但通常没有提示信息。
- **缺少索引文件**：BAM 文件需要 `.bai` 文件，VCF 文件需要 `.tbi` 文件。缺少这些文件会导致工具运行失败。
- **内存不足**：处理大型 BAM 文件时可能导致系统崩溃。请合理使用多线程（`--threads` 参数）或采用流式处理方式。
- **索引文件损坏**：修改 BAM/VCF 文件后请重新生成索引。过时的索引可能导致数据读取错误。
- **坐标系统不一致**：BED 文件使用 0 为起始点，VCF/GFF 文件使用 1 为起始点。这种差异可能导致分析错误。

## 常见文件格式简介

| 文件格式 | 用途 | 主要使用的工具 |
|--------|---------|----------|
| FASTA | 参考序列 | `samtools faidx` |
| FASTQ | 原始读段及质量信息 | `seqtk`, `fastp` |
| SAM/BAM | 对齐后的读段 | `samtools` |
| VCF/BCF | 变异信息 | `bcftools` |
| BED | 基因组区间信息 | `bedtools` |
| GFF/GTF | 基因注释 | `gffread` |
| BigWig | 基因组覆盖信息 | `deepTools` |

## 必需掌握的命令

### 数据质量控制
```bash
# FASTQ quality report
fastqc sample.fastq.gz -o qc_reports/

# Trim adapters + low quality
fastp -i R1.fq.gz -I R2.fq.gz -o R1.clean.fq.gz -O R2.clean.fq.gz

# BAM statistics
samtools flagstat aligned.bam
samtools stats aligned.bam > stats.txt
```

### 序列比对
```bash
# Index reference (once)
bwa index reference.fa

# Align paired-end reads
bwa mem -t 8 reference.fa R1.fq.gz R2.fq.gz | \
  samtools sort -o aligned.bam -

# Index BAM
samtools index aligned.bam
```

### 变异检测
```bash
# Call variants
bcftools mpileup -Ou -f reference.fa aligned.bam | \
  bcftools call -mv -Oz -o variants.vcf.gz

# Index VCF
bcftools index variants.vcf.gz

# Filter variants
bcftools filter -s LowQual -e 'QUAL<20' variants.vcf.gz
```

### 数据处理
```bash
# Extract region
samtools view -b aligned.bam chr1:1000000-2000000 > region.bam

# Convert BAM to FASTQ
samtools fastq -1 R1.fq.gz -2 R2.fq.gz aligned.bam

# Merge BAMs
samtools merge merged.bam sample1.bam sample2.bam

# Subset VCF by region
bcftools view -r chr1:1000-2000 variants.vcf.gz
```

## 安全性与隐私保护

- **数据访问**：
  - 仅处理用户明确提供的输入文件。
  - 将输出结果保存在用户指定的目录中。
  - 将用户设置保存在 `~/bioinformatics/` 目录中（需用户同意）。

- **数据存储方式**：
  - 所有处理后的序列数据均保存在本地。
  - 不通过外部 API 进行数据分析。
  - 分析流程的配置文件存储在 `~/bioinformatics/` 目录中。

**本工具不执行以下操作：**
- 不会将序列数据上传到任何外部服务器。
- 未经用户明确授权，不会访问或修改任何文件。
- 不会推断或收集超出用户提供的输入数据。
- 分析过程中不会发起网络请求。

**注意**：安装工具（如 conda、brew）及下载参考基因组数据需要网络连接。这些操作均由用户自行完成。

## 相关技能
如果用户需要，可以使用 `clawhub install <slug>` 安装以下工具：
- `data-analysis`：用于数据统计分析。
- `statistics`：用于假设检验。
- `science`：用于研究方法论相关功能。

## 用户反馈
- 如果本工具对您有帮助，请使用 `clawhub star bioinformatics` 给予评价。
- 如需获取最新更新，请使用 `clawhub sync` 命令。