---
name: Forms
slug: forms
version: 1.0.0
description: 创建、验证并部署包含字段类型、验证规则、条件逻辑以及平台集成功能的表单。
metadata: {"clawdbot":{"emoji":"📝","requires":{"bins":[]},"os":["linux","darwin","win32"]}}
---
## 快速参考

| 主题 | 文件 |
|-------|------|
| 按使用场景分类的字段类型 | `types.md` |
| 平台比较 | `platforms.md` |
| 代码生成（React, Flutter） | `code.md` |
| 验证规则 | `validation.md` |
| 集成（Webhooks, CRM） | `integrations.md` |
| 自托管选项 | `selfhosted.md` |

## 用户资料

<!-- 请编辑此处以自定义表单建议内容 -->

### 偏好的技术栈
<!-- react | flutter | vue | html | 无代码开发 |

### 主要使用场景
<!-- 营销线索收集 | 调查问卷 | 应用程序 | 用户反馈 | 注册表单 |

### 默认使用的平台
<!-- google-forms | typeform | tally | heyform | 仅使用代码生成表单 |

## 数据存储

将表单定义和模板存储在 `~/forms/` 目录下：
- `templates`：可重用的表单定义（格式为 JSON/YAML）
- `submissions`：收集到的用户反馈数据（仅适用于自托管环境）
- `feedback`：记录哪些表单填写成功，哪些失败

## 核心规则

- 在建议添加字段之前，先了解具体的使用场景——营销线索表单与应用程序表单的需求可能不同
- 采用渐进式展示方式：初始时只显示最基本的信息，根据需要逐步展示更复杂的字段
- 优先考虑移动设备兼容性——所有表单都必须在手机上正常使用
- 每个营销线索收集表单的字段数量不超过 7 个——每个额外的字段都会降低表单转化率约 10%
- 对于包含 5 个以上字段的表单，建议采用多步骤填写方式而非长表格单
- 在用户输入内容时立即进行验证——提供即时反馈
- 必须包含以下元素：清晰的字段标签、错误提示以及成功提交后的确认信息
- 对于欧盟用户，必须设置 GDPR 同意框——并提供隐私政策链接
- 在可能的情况下，优先使用“蜜罐”（honeypot）验证机制而非 CAPTCHA——以减少用户使用障碍
- 文件上传需要设置文件类型和大小限制——防止滥用
- 条件逻辑语法：`IF field=value THEN show/hide field`（根据字段值显示/隐藏相关字段）
- 在真实设备上进行测试——模拟器无法完全模拟实际设备的键盘操作特性