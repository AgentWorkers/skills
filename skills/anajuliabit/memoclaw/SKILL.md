---
name: memoclaw
version: 1.23.2
description: **AI代理的内存即服务（Memory-as-a-Service）**：支持通过语义向量搜索来存储和检索记忆数据。每个钱包初始提供100次免费调用服务，之后需通过微支付（micropayments）来继续使用该服务。您的钱包地址即为您的身份凭证。
allowed-tools:
  - exec
---
## 安全性  
该技能需要 `MEMOCLAW_PRIVATE_KEY` 环境变量来进行钱包身份验证。请使用专用的钱包。该技能仅通过 HTTPS 向 `api.memoclaw.com` 发送请求。  
**免费 tier:** 每个钱包 100 次调用。超出免费次数后，需要使用基于 Base 的 USDC 进行支付。  

## MemoClaw 技能  
MemoClaw 为 AI 代理提供持久化存储功能，可存储文本并通过语义搜索快速检索。  
无需 API 密钥或注册信息；您的钱包地址即为您的身份凭证。  
每个钱包初始拥有 100 次免费 API 调用权限，之后每次调用需支付 0.005 美元的 USDC。  

### 先决条件  
在使用任何 MemoClaw 命令之前，请确保已完成以下设置：  
1. **是否安装了 CLI？** `which memoclaw`；若未安装：`npm install -g memoclaw`  
2. **钱包是否已配置？** `memoclaw config check`；若未配置：`memoclaw init`  
3. **免费额度是否可用？`memoclaw status`；若免费额度已用完，请为钱包充值 USDC。  
如果从未运行过 `memoclaw init`，**所有命令都将失败**。请先运行该命令（操作过程为交互式，耗时约 30 秒）。  

### 快速参考  
> 📖 有关完整的端到端示例（会话流程、数据迁移、多代理模式及费用明细），请参阅 [examples.md](examples.md)。  

**核心命令：**  
**管理命令：**  

### 重要性分级  
- **0.9+**：关键性更强的内容  
- **0.7–0.8**：偏好设置  
- **0.5–0.6**：一般性信息  
- **≤0.4**：临时性数据  

**内存类型：**  
- `correction`（180 天）  
- `preference`（180 天）  
- `decision`（90 天）  
- `project`（30 天）  
- `observation`（14 天）  
- `general`（60 天）  

**免费命令：**  
- 列出、获取、删除、批量删除、清除、搜索、核心内容、建议内容、关联记录、历史记录、差异对比、导出、导入、命名空间列表、统计信息、计数、浏览、配置、生成图表、完成记录、查看用户信息、状态查询、升级、固定内存、解除固定、锁定/解锁、编辑、复制、移动、添加标签、监视、创建/删除快照  

### 决策树  
使用以下步骤判断 MemoClaw 是否适合当前场景：  
- **选择合适的检索命令**  

### 常见问题及解决方法  
- 如何在 MemoClaw 与本地文件之间做出选择？  

### 重要性评分规则  
使用以下标准为数据分配重要性等级：  
- **0.95**：关键性修正、安全相关内容  
- **0.85–0.9**：重要决策、偏好设置、架构选择  
- **0.7–0.8**：一般性偏好、用户信息、项目相关内容  
- **0.5–0.6**：有用背景信息、非强制性偏好、观察结果  
- **0.3–0.4**：低价值数据、临时性内容  

### 内存类型与有效期  
- **correction**：180 天  
- **preference**：180 天  
- **decision**：90 天  
- **project**：30 天  
- **observation**：14 天  
- **general**：60 天  

### 命令分类  
- **管理命令：**  

### 会话生命周期  
- **会话开始**：优先使用免费命令（`memoclaw core`）  
- **查询**：根据需要使用付费命令（`memoclaw search`/`memoclaw recall`）  
- **会话结束**：总结会话内容并定期清理过期数据  

### 自动总结工具  
- **会话快照**  
- **对话摘要**  
- **关键点提取**  

### 数据迁移  
若需从本地文件迁移到 MemoClaw，请参考 [examples.md] 中的详细步骤。  

### CLI 使用说明  
该技能提供命令行接口（CLI），便于通过 Shell 进行操作。  

### 定价方案  
**免费 tier：** 每个钱包 100 次免费调用。  
**付费模式：** 每次调用费用为 0.005 美元（基于 Base 的 USDC）。  

### 工作原理  
您的钱包地址即为您的用户 ID；无需单独的 API 密钥。身份验证支持两种方式：免费调用或付费调用。  

## 文档与资源  
- 官方文档：https://docs.memoclaw.com  
- 通过 `npm install -g memoclaw-mcp` 可通过 MCP 兼容客户端访问 MemoClaw。  

## 使用建议  
- 优先使用 CLI 命令。  
- 为数据分配适当的重要性等级。  
- 使用命名空间来组织数据。  

## 错误处理  
所有错误信息均遵循统一格式。  

## 数据迁移指南  
若您之前使用过本地 Markdown 文件，请参考 [examples.md] 中的迁移步骤。