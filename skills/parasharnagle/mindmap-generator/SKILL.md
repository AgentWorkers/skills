---
name: mindmap-generator
description: 该工具能够根据对话内容、目标、决策以及每日优先事项生成可视化的思维导图图像，并以 PNG 格式输出，可以直接在 Telegram 中查看。适用于需要将主题、优先事项、决策、会议记录或每周工作进展进行可视化展示或分解的场景。
metadata: {"openclaw": {"emoji": "🗺️", "requires": {"bins": ["node", "npx"]}}}
---
# 思维导图生成技能

您是一名首席参谋代理，具备生成**可视化思维导图**的能力，并能将其以**PNG图像**的形式直接发送到Telegram消息中。

---

## 何时激活该技能

在以下任何一种情况下激活此技能：

- 用户明确请求生成思维导图、可视化概览，或表示需要“梳理信息”、“可视化展示”或“分解内容”；
- 用户希望以可视化形式查看自己的日程、周计划或优先事项；
- 用户分享了会议记录或语音转录内容，并请求对其进行结构化整理（注意：会议记录并非必需，不是每次交流都包含此环节）；
- 用户正在做决策，需要通过思维导图来了解各项任务的优缺点及潜在风险；
- 用户希望分解目标、项目或计划；
- 在晨间简报中，当用户需要同时处理3个以上的优先事项时；
- 用户表示“感到压力过大”或“事情太多”——此时可主动提供思维导图帮助用户理清思路。

**请勿在以下情况下激活该技能**：  
- 当用户只需列出1-2个简单项目时；  
- 或用户明确要求仅以文本形式获取信息时。

---

## 如何生成思维导图

### 第1步：提取层次结构

从用户的输入内容（语音笔记、文本消息、日历数据或记忆中的信息）中提取出树状结构：

- **根节点**：主要主题、日期或决策问题；
- **第1层**：主要类别（每个分支最多5-7个子节点）；
- **第2层**：详细信息、子任务或具体内容（每个分支最多3-5个子节点）；
- **第3层**：仅适用于复杂主题（每个分支最多2-3个子节点）。

为保证可读性，建议保持结构层次不超过3层。如果内容过于复杂，请在第3层进行总结。

### 第2步：编写Mermaid思维导图语法

使用Mermaid的思维导图语法来格式化提取出的结构。语法基于缩进规则：

```
mindmap
  root((Main Topic))
    Category A
      Detail 1
      Detail 2
    Category B
      Detail 3
      Detail 4
    Category C
      Detail 5
```

#### 节点形状说明（请根据需要使用，非所有节点都必须使用）：
- `((text))`：圆形——仅用于根节点；
- `(text)`：圆角矩形——用于类别；
- `[text]`：正方形——用于行动项；
- `)text(`：云形——用于想法或未解决的问题；
- `))text((`：爆炸形——用于紧急或待解决的问题；
- `{{text}}`：六边形——用于决策节点；
- 普通文本——用于详细信息和备注。

#### 状态标记：
- 前缀为✅表示已完成的任务；
- 前缀为⏳表示待处理/进行中的任务；
- 前缀为❌表示被阻塞的任务；
- 前缀为⚠️表示潜在风险或警告；
- 前缀为💡表示想法或建议。

### 第3步：渲染为PNG图像

运行渲染脚本，将Mermaid语法转换为PNG图像：

```bash
# Save the mermaid content to a temp .mmd file
echo "$MERMAID_CONTENT" > /tmp/mindmap_input.mmd

# Render to PNG using mermaid-cli
./scripts/render_mindmap.sh /tmp/mindmap_input.mmd /tmp/mindmap_output.png
```

该脚本使用`mmdc`（mermaid-cli）工具，并配置了适合在移动设备上查看的样式。

### 第4步：通过Telegram发送

渲染完成后，将PNG图像发送到用户的Telegram聊天窗口：

```bash
./scripts/send_telegram_photo.sh /tmp/mindmap_output.png "Here's your mindmap 🗺️" "$CHAT_ID"
```

图像将直接显示在聊天窗口中——无需下载、无需链接，也不会以HTML文件的形式发送。

---

## 格式规范

1. **根节点**：必须使用圆形 `((text))`；
2. 结构深度最多为4层（超过4层时需进行总结）；
- 根节点最多有7个子节点；如果节点过多，请进行分组；
- 节点标签应简短（每个节点不超过5-6个字），详细内容应放在子节点中；
- 节点文本中避免使用可能破坏Mermaid解析的特殊字符（如`(`、`)`、`[`、`]`、`{`、`}`）；
- 根据任务状态使用相应的标记（✅、⏳、❌等）；
- 节点内不要使用Markdown格式（Mermaid思维导图不支持加粗/斜体文字）。

---

## 输出方式

- 总是将思维导图以PNG图像的形式发送到Telegram；
- 图像前或后务必附上简短的文字说明（1-2句话）；
- 可选择性地询问用户是否需要根据实际情况调整思维导图内容；
- 如果渲染失败，可退而使用Unicode字符来表示任务结构（以文本形式呈现）：

```
📊 Today's Priorities
├── 🔴 Client Proposal (due 2pm)
│   ├── Review pricing section
│   └── Add case studies
├── 🟡 Team Standup (11am)
│   └── Prep sprint update
├── 🟢 Follow up with Rajesh
│   └── Send updated timeline
└── 📋 Admin
    ├── Expense report
    └── Update project tracker
```

---

## 示例场景

### 情景1：晨间简报

用户询问：“我的今天安排是怎样的？”

生成相应的思维导图：

```
mindmap
  root((Wednesday Feb 18))
    (Meetings)
      [10am - Team Standup]
      [2pm - Client Review]
      [4pm - 1:1 with Priya]
    (Tasks)
      ))⚠️ Proposal due today((
      [⏳ Review PR #342]
      [⏳ Update roadmap doc]
    (Follow-ups)
      [❌ Rajesh - SOW overdue 3 days]
      [⏳ Ankit - waiting on pricing]
    )Open Questions(
      )Timeline for Phase 2(
      )Budget approval status(
```

并发送消息：“早上好！这是您今天的工作安排。提案今天截止，Rajesh的工作计划已经逾期3天——这些是优先处理的事项。🗺️”

### 情景2：决策分析

用户询问：“我应该接手Acme咨询项目吗？”

```
mindmap
  root((Acme Consulting Decision))
    (Pros)
      ₹12L revenue over 3 months
      Expands fintech portfolio
      Rajesh intro to their CTO
    (Cons)
      40hrs/month commitment
      Overlaps with product launch
      Below usual rate by 15%
    ))Risks((
      Scope creep - no fixed SOW yet
      Payment terms NET-60
      Single point of contact leaving
    {{Past Precedent}}
      Similar deal with TechCorp
      Went 2x over timeline
      But led to 3 referrals
    )Decision Factors(
      Can you delegate product launch?
      Is the CTO intro worth the discount?
      What does your cash flow look like in Q2?
```

### 情景3：会议后行动规划（有会议记录时）

在会议记录可用后，生成相应的思维导图：

```
mindmap
  root((Meeting - Rajesh - Feb 18))
    {{Decisions Made}}
      Go with Vendor A
      Launch date March 15
      Budget approved at ₹8L
    (Action Items)
      [You - Send SOW by Friday]
      [Rajesh - Review pricing by Wed]
      [Priya - Set up staging env]
    )Open Questions(
      )Phase 2 timeline TBD(
      )Need legal review on clause 4.2(
    (Context)
      Rajesh seemed hesitant on timeline
      Budget was originally ₹6L - pushed up
```

### 情景4：每周回顾

```
mindmap
  root((Week 7 Review))
    (✅ Completed - 5)
      ✅ Client proposal submitted
      ✅ Sprint planning done
      ✅ Hired frontend dev
      ✅ Updated investor deck
      ✅ Fixed auth bug
    (⏳ Carried Forward - 2)
      ⏳ Blog post draft
      ⏳ Vendor evaluation
    (❌ Dropped - 1)
      ❌ Office space tour - deprioritized
    (Key Wins)
      Client signed 6-month extension
      New dev starts Monday
    ))Blockers((
      Legal review delayed 5 days
      AWS costs spiking - need investigation
```

---

## 所需依赖库/工具

- `@mermaid-js/mermaid-cli`（mmdc）：用于将Mermaid语法渲染为PNG/SVG格式；
- `curl`：用于调用Telegram Bot的API；
- Node.js v18+：作为mermaid-cli的运行环境。