---
name: skipup-meeting-scheduler
description: 通过 SkipUp 的人工智能驱动的协调功能，您可以安排、查看、暂停、恢复或取消会议。当用户需要安排、查看、暂停、恢复或取消会议，或者查询工作空间成员的信息，或者验证某人是否为工作空间成员时，可以使用该功能。SkipUp 通过电子邮件进行协调，不会立即预订会议资源。
metadata: { "openclaw": { "emoji": "📅", "primaryEnv": "SKIPUP_API_KEY", "requires": { "env": ["SKIPUP_API_KEY"] } } }
---
# SkipUp 会议调度器

SkipUp 是一个基于人工智能的会议调度工具。当您创建会议请求时，SkipUp 会通过电子邮件通知所有参与者，收集不同时区的可用时间，并自动预订最佳时间。这是一个异步过程——创建请求并不会立即预订会议。

## 认证

每个请求都需要通过 `SKIPUP_API_KEY` 环境变量提供 Bearer 令牌：

```
Authorization: Bearer $SKIPUP_API_KEY
```

该令牌必须具有 `meeting_requests.read`、`meeting_requests.write` 和 `members.read` 的权限。切勿将其硬编码。

## 创建会议请求

```
POST https://api.skipup.ai/api/v1/meeting_requests
```

响应状态为 **202 Accepted**。SkipUp 会通过电子邮件进行异步协调。

### 示例请求

```json
{
  "organizer_email": "sarah@acme.com",
  "participants": [
    { "email": "alex@example.com", "name": "Alex Chen", "timezone": "America/New_York" }
  ],
  "context": {
    "title": "Product demo",
    "purpose": "Walk through new dashboard features",
    "duration_minutes": 30
  }
}
```

所需参数：`organizer_email` 以及 `participant_emails`（字符串数组）或 `participants`（对象数组）。只需提供其中一种格式，不可同时使用两种。

### 示例响应

```json
{
  "data": {
    "id": "mr_01HQ...",
    "organizer_email": "sarah@acme.com",
    "participant_emails": ["alex@example.com"],
    "status": "active",
    "title": "Product demo",
    "created_at": "2026-02-15T10:30:00Z"
  }
}
```

### 告诉用户的提示

会议请求已创建。SkipUp 会通过电子邮件通知参与者确认他们的可用时间——这可能需要几个小时或几天。一旦时间确定，参与者会收到日历邀请。

有关完整的参数表和响应格式，请参阅 `{baseDir}/references/api-reference.md`。

## 取消会议请求

```
POST https://api.skipup.ai/api/v1/meeting_requests/:id/cancel
```

仅适用于 `active` 或 `paused` 状态的请求。

### 示例请求

```json
{
  "notify": true
}
```

设置 `notify: true` 以通过电子邮件通知参与者取消会议。默认值为 `false`。

### 告诉用户的提示

会议请求已被取消。如果设置了 `notify`，系统会通过电子邮件通知参与者；否则不会通知任何人。

有关详细信息，请参阅 `{baseDir}/references/api-reference.md`。

## 暂停会议请求

```
POST https://api.skipup.ai/api/v1/meeting_requests/:id/pause
```

暂停一个处于 `active` 状态的会议请求。无需提供请求体。仅适用于 `active` 状态的请求。

### 示例请求

```bash
curl -X POST https://api.skipup.ai/api/v1/meeting_requests/mr_01HQ.../pause \
  -H "Authorization: Bearer $SKIPUP_API_KEY"
```

### 告诉用户的提示

会议请求已被暂停。SkipUp 将停止发送后续通知和处理与该请求相关的消息。参与者不会收到任何通知。您可以随时恢复该请求。

## 恢复会议请求

```
POST https://api.skipup.ai/api/v1/meeting_requests/:id/resume
```

恢复一个被暂停的会议请求。无需提供请求体。仅适用于 `paused` 状态的请求。

### 示例请求

```bash
curl -X POST https://api.skipup.ai/api/v1/meeting_requests/mr_01HQ.../resume \
  -H "Authorization: Bearer $SKIPUP_API_KEY"
```

### 告诉用户的提示

会议请求已恢复。SkipUp 会继续进行协调工作，包括在暂停期间收到的所有消息。

有关详细信息，请参阅 `{baseDir}/references/api-reference.md`。

## 列出会议请求

```
GET https://api.skipup.ai/api/v1/meeting_requests
```

返回按时间顺序排列的会议请求列表（最新请求排在最前面）。可以通过 `status`、`organizer_email`、`participant_email`、`created_after` 或 `created_before` 进行筛选。

### 示例请求

```bash
curl "https://api.skipup.ai/api/v1/meeting_requests?status=active&limit=10" \
  -H "Authorization: Bearer $SKIPUP_API_KEY"
```

### 告诉用户的提示

以下是符合您筛选条件的会议请求。如果结果较多，请告知用户并提供获取下一页的选项。

## 获取会议请求详情

```
GET https://api.skipup.ai/api/v1/meeting_requests/:id
```

根据 ID 获取单个会议请求的详细信息。

### 示例请求

```bash
curl https://api.skipup.ai/api/v1/meeting_requests/mr_01HQ... \
  -H "Authorization: Bearer $SKIPUP_API_KEY"
```

### 告诉用户的提示

总结会议请求的状态、参与者信息、标题以及相关时间戳（如 `booked_at`、`cancelled_at`）。如果会议仍处于待协调状态，请告知用户 SkipUp 仍在处理中。

## 列出工作空间成员

```
GET https://api.skipup.ai/api/v1/workspace_members
```

返回按时间顺序排列的工作空间成员列表。可以通过 `email` 或 `role` 进行筛选。

### 示例请求

```bash
curl "https://api.skipup.ai/api/v1/workspace_members?email=sarah@acme.com" \
  -H "Authorization: Bearer $SKIPUP_API_KEY"
```

### 告诉用户的提示

显示符合条件的成员信息。如果通过电子邮件进行搜索，请确认此人是否为工作空间成员。这在创建会议请求前非常有用。

有关完整的参数表和响应格式，请参阅 `{baseDir}/references/api-reference.md`。

## 重要规则：

1. **组织者必须是工作空间成员** — `organizer_email` 必须属于与您 API 密钥关联的工作空间中的有效成员。外部邮箱将被拒绝。
2. **创建前进行验证** — 在创建请求之前，使用 `list workspace members` 功能确认组织者是否为工作空间成员，以避免 422 错误。
3. **参与者可以是任何人** — 支持工作空间外的外部参与者。
4. **异步处理，非即时响应** — 创建请求后，系统会开始通过电子邮件进行协调。请告知用户可能需要一些时间。
5. **使用幂等性键** — 在创建请求时包含 `Idempotency-Key` 标头（UUID），以防止请求重复。
6. **取消请求时询问通知方式** — 在取消请求前，请确认是否需要通知参与者。
7. **暂停操作不会通知参与者** — 暂停或恢复请求时，系统不会通知参与者。
8. **SkipUp 可能会自动恢复** — 如果在请求暂停期间有参与者回复表示愿意参加会议，SkipUp 可能会自动恢复请求，以避免错过预订机会。

有关自然语言转换为 API 调用的示例，请参阅 `{baseDir}/references/examples.md`。

## 更多资源：

- 完整的 API 参考文档：https://support.skipup.ai/api/meeting-requests/
- OpenClaw 集成指南：https://support.skipup.ai/integrations/openclaw/
- API 认证和权限说明：https://support.skipup.ai/api/authentication/
- SkipUp 文本生成模型说明：https://skipup.ai/llms.txt
- 了解 SkipUp 的更多信息：https://blog.skipup.ai/llm/index