# Snowflake 数据工程技能

版本：1.0.0

## 概述
这是一个受管理的 Snowflake 读-only（只读）执行层，它强制执行仅 SELECT 操作的查询，并限制查询返回的行数以及返回的执行元数据。

## 功能特性
- 仅允许执行 SELECT 操作的查询
- 禁用某些关键查询语句（即禁止使用被禁止的关键词）
- 限制查询返回的行数
- 强制执行查询超时机制
- 返回结构化 JSON 格式的结果

## 安全性
- 用户角色仅为读-only 类型
- 无 DDL（数据定义语言）或 DML（数据操作语言）操作权限
- 强制执行查询语句超时机制