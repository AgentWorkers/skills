---
name: department-manager
description: 管理由多个部门组成的AI子代理团队。当需要将任务分配给专门的代理、跟踪各部门的工作成果、分配角色以及协调多代理的工作流程时，此功能非常有用。对于独立运行业务或处理复杂项目的AI代理而言，这是必不可少的功能。
user-invocable: true
metadata: {"openclaw": {"emoji": "🏢", "os": ["darwin", "linux"], "requires": {"bins": ["python3"]}}}
---
# 部门经理

将您的人工智能员工分配到不同的部门中，分配任务，跟踪工作进度，并像首席执行官一样协调多团队之间的协作。

## 为什么需要这个功能

运营一个自主业务或复杂的项目意味着需要在多个领域（内容、研究、编码、营销、运营）之间同时处理多项任务。与其逐一完成这些任务，不如将员工组织成不同的部门并并行分配工作。

## 命令

### 创建一个部门
```bash
python3 {baseDir}/scripts/departments.py create --name "content" --description "SEO blog posts, marketing copy, newsletters" --model "arcee-ai/trinity-large-preview:free"
```

### 列出所有部门
```bash
python3 {baseDir}/scripts/departments.py list
```

### 为某个部门分配任务
```bash
python3 {baseDir}/scripts/departments.py assign --dept "content" --task "Write a blog post about OpenClaw memory system" --priority high
```

### 检查部门状态
```bash
python3 {baseDir}/scripts/departments.py status --dept "content"
```

### 查看所有部门的活跃任务
```bash
python3 {baseDir}/scripts/departments.py active
```

### 完成一项任务
```bash
python3 {baseDir}/scripts/departments.py complete --task-id 1 --output "Blog post written and saved to drafts/memory-post.md"
```

### 部门报告（所有部门的汇总）
```bash
python3 {baseDir}/scripts/departments.py report
```

### 删除一个部门
```bash
python3 {baseDir}/scripts/departments.py remove --name "content"
```

## 推荐的部门结构

| 部门 | 模型 | 负责范围 |
|------------|-------|-----------------|
| 内容 | arcee-ai/trinity-large-preview:free | 博文撰写、营销文案、新闻通讯 |
| 研究 | stepfun/step-3.5-flash:free | 市场研究、竞争对手分析、数据收集 |
| 工程 | openrouter/free | 代码生成、漏洞修复、工具开发 |
| 运营 | （手动操作/首席执行官） | 预算管理、战略规划、沟通协调 |
| 安全 | （手动操作/首席执行官） | 技能评估、威胁分析、安全审计 |

## 数据存储

部门数据存储在 `~/.openclaw/department-manager/departments.json` 文件中。

## 使用建议

- 根据实际的工作流程来创建部门，而不是基于假设的需求。
- 为每个部门分配一个默认的模型以确保一致性。
- 在发布内容之前先审核已完成的任务——质量把控是首席执行官的职责。
- 使用优先级来引导员工专注于当前最重要的任务。
- 每天结束时运行 `report` 命令以获取团队整体情况。