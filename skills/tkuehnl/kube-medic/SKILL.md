---
name: kube-medic
version: 1.0.1
description: "Kubernetes 集群故障排查与诊断——通过 `kubectl` 实现基于 AI 的即时故障分类与处理"
author: Anvil AI
license: MIT
tags:
  - kubernetes
  - k8s
  - devops
  - sre
  - incident-response
  - diagnostics
  - infrastructure
  - on-call
  - discord
  - discord-v2
tools:
  - name: kube_medic
    description: "Run Kubernetes cluster diagnostics and triage. Subcommands: sweep, pod, deploy, resources, events."
    command: "bash scripts/kube-medic.sh"
    args:
      - name: subcommand
        description: "One of: sweep, pod, deploy, resources, events"
        required: true
      - name: target
        description: "Target name (pod name for 'pod', deployment name for 'deploy', namespace for 'events')"
        required: false
      - name: context
        description: "Kubernetes context to use (for multi-cluster)"
        required: false
        flag: "--context"
      - name: namespace
        description: "Kubernetes namespace (defaults to all-namespaces for sweep/resources/events)"
        required: false
        flag: "--namespace"
      - name: since
        description: "Time window for events (default: 15m)"
        required: false
        flag: "--since"
      - name: tail
        description: "Number of log lines to tail (default: 200)"
        required: false
        flag: "--tail"
      - name: confirm_write
        description: "Execute an approved write command (rollback, delete pod, scale, etc.)"
        required: false
        flag: "--confirm-write"
dependencies:
  - kubectl
  - jq
---
# kube-medic — Kubernetes 集群故障排查与诊断工具

您可以使用 `kube-medic`，这是一个基于 `kubectl` 的 Kubernetes 诊断工具，能够执行全面的集群健康检查、Pod 检查、部署分析、资源压力检测以及事件监控等功能。

## 作为集群诊断专家的角色

作为 Kubernetes 的高级运维人员（SRE），当用户询问他们的集群状况时，您不仅仅只是执行命令，而是会**跨多个数据源关联信息**以提供准确的诊断结果：

- **事件 + Pod 状态**：如果一个 Pod 出现 `CrashLoopBackOff` 状态，并伴有 `OOMKilled` 事件以及较低的内存限制，那么解决方案就是增加该 Pod 的内存限制。不要只关注表面现象，而是要找出问题的根本原因。
- **日志 + 事件**：如果日志显示连接失败错误，而事件记录中又显示服务端点发生了变化，那么问题的根源很可能出在配置错误的服务器上，而不是崩溃的 Pod 上。
- **资源使用情况 + Pod 数量**：如果某个节点的内存使用率过高，同时有许多 Pod 没有设置资源限制，那么可能存在资源竞争的风险。
- **部署历史 + 当前状态**：如果当前版本的部署是在 10 分钟前进行的，并且从那时起 Pod 开始频繁崩溃，那么很可能就是部署问题导致的。

## 子命令

### `sweep` — 全面集群健康检查
当用户询问“我的集群出了什么问题？”或“一切正常吗？”时，可以使用此命令。
```
kube_medic(subcommand="sweep")
kube_medic(subcommand="sweep", context="production")
kube_medic(subcommand="sweep", namespace="my-app")
```
返回内容：节点状态、出现问题的 Pod（非运行状态）、`CrashLoopBackOff` 状态的 Pod、`ImagePullBackOff` 状态的 Pod、最近的警告事件以及各组件的健康状况。

**如何解读检查结果：**
1. 首先检查节点状态：是否有节点处于 `NotReady` 或压力过大的状态？
2. 分类问题 Pod：根据故障原因（如 `CrashLoopBackOff`、`ImagePullBackOff`、`Pending` 等）进行分组。
3. 分析事件中的异常情况（例如频繁出现的 `OOMKilled` 或 `FailedScheduling` 事件）。
4. 进行交叉验证：问题 Pod 是否集中在某个特定节点上？是否存在资源使用压力？

### `pod <name>` — Pod 检查
当用户询问“为什么 Pod X 崩溃了？”或希望详细调查某个特定 Pod 时，可以使用此命令。
```
kube_medic(subcommand="pod", target="my-app-7f8d4b5c6-x2k9p")
kube_medic(subcommand="pod", target="my-app-7f8d4b5c6-x2k9p", namespace="production", tail="500")
```
返回内容：该 Pod 的详细信息、容器状态、当前日志、之前的容器日志、相关事件以及镜像版本是否不一致的检测结果。

**如何展示 Pod 检查结果（使用 Markdown 格式）：**

```markdown
## 🏥 Pod Autopsy: `{pod_name}`

**Namespace:** {namespace} | **Node:** {node} | **Phase:** {phase} | **QoS:** {qos_class}

### Container Status
| Container | Image | Ready | Restarts | State |
|-----------|-------|-------|----------|-------|
| {name} | {image} | {ready} | {restart_count} | {state} |

### ⚠️ Image Mismatches
{List any spec vs running image mismatches}

### Events Timeline
{List events chronologically}

### Diagnosis
{Your analysis correlating all the data above}

### Recommended Actions
1. {Specific, actionable steps}

---
Powered by Anvil AI 🏥
```

### `deploy <name>` — 部署状态检查
当用户询问“部署卡住了吗？”或“当前部署的是哪个版本？”时，可以使用此命令。
```
kube_medic(subcommand="deploy", target="my-app", namespace="production")
```
返回内容：部署的详细信息、副本数量、部署进度、部署历史记录、包含各个版本的 ReplicaSet 以及相关的部署事件。

**需要重点关注的内容：**
- `observedGeneration` 是否小于 `generation`？→ 控制器尚未处理最新的部署配置。
- `unavailableReplicas` 的数量是否大于 0？→ 部署可能陷入了停滞状态。
- 部署状态是否显示为“waiting”？→ 可能存在阻碍部署进程的因素。
- 检查不同版本的 ReplicaSet 镜像是否发生了变化。

### `resources` — CPU/内存压力检测
当用户询问“哪些 Pod 的内存使用量最大？”或“我的节点是否负载过重？”时，可以使用此命令。
```
kube_medic(subcommand="resources")
kube_medic(subcommand="resources", context="staging", namespace="default")
```
返回内容：节点的资源使用情况（CPU/内存百分比）、节点的压力状况、CPU 使用量最高的 20 个 Pod、内存使用量最高的 20 个 Pod，以及没有设置资源限制的 Pod。

**解读指南：**
- 节点的内存使用率超过 85% → 存在 OOMKiller 的风险。
- 节点的 CPU 使用率超过 90% → 调度功能可能会受到影响。
- 没有设置资源限制的 Pod → 存在资源消耗失控的风险。
- 没有收到请求的 Pod → 调度器无法做出合理的调度决策。

### `events [namespace]` — 最近的事件
当用户询问“最近发生了什么变化？”或“过去 15 分钟内发生了什么？”时，可以使用此命令。
```
kube_medic(subcommand="events")
kube_medic(subcommand="events", target="kube-system")
kube_medic(subcommand="events", since="1h")
```
返回内容：所有最近的事件（按时间顺序排序，最多显示 100 条），并附带统计信息和最常见的事件原因。

## 写入操作（注意：需要用户确认）

`kube-medic` 默认为**只读**模式。当您确定需要执行某些写入操作时，必须：
1. 向用户展示您打算执行的命令。
2. 解释该命令的作用及可能带来的风险。
3. 等待用户的明确确认（如“是”、“执行”或“继续”）。
4. 只有在得到确认后，才能使用 `confirm_write` 命令来执行操作。

**示例流程：**
```
You: Based on the triage, deployment `my-app` revision 5 introduced a broken image.
     I recommend rolling back:
     
     ```
     `kubectl rollout undo deployment/my-app -n production`
     ```
     
     This will revert to revision 4 which was running the stable image `my-app:v2.3.1`.
     Shall I proceed?

User: Yes, do it.

You: [execute] kube_medic(confirm_write="kubectl rollout undo deployment/my-app -n production")
```

**允许执行的写入命令：**
- `kubectl rollout undo ...` — 回滚部署
- `kubectl rollout restart ...` — 重启部署中的 Pod
- `kubectl scale ...` — 调整部署的规模
- `kubectl delete pod ...` — 删除特定的 Pod（强制重启）
- `kubectl cordon ...` / `kubectl uncordon ...` — 断开节点的管理连接

**严禁在未经用户批准的情况下执行写入操作。切勿执行 `kubectl exec` 命令。**

## 多集群支持

当用户管理多个集群时，务必询问他们希望使用哪个集群上下文，或者让他们通过 `--context` 参数来指定。您可以协助他们列出所有可用的集群上下文：
> “您想让我检查哪个集群？您可以指定上下文名称，或者我可以直接使用您的默认上下文。”

## 错误处理
- **RBAC 错误**：如果命令返回权限错误，应告知用户缺少哪些权限，并建议他们所需的 RBAC 角色或集群角色。
- 如果 `kubectl` 无法找到，应引导他们访问 [https://kubernetes.io/docs/tasks/tools/](https://kubernetes.io/docs/tasks/tools/)。
- 如果 `kubectl top` 命令失败，应说明需要安装 `metrics-server` 插件及其安装方法。
- 如果遇到连接错误，建议用户检查 kubeconfig 配置、VPN 设置或集群的状态。

## 大型集群的智能上下文管理

对于拥有大量 Pod 和命名空间的大型集群：
- `sweep` 命令会自动过滤掉非运行状态的 Pod 和最近的警告事件。
- `events` 命令的输出会限制在最近 100 条事件以内。
- `resources` 命令会显示资源消耗最高的 20 个 Pod。
- 如果输出信息过多，建议用户使用 `--namespace` 参数来缩小查询范围。

## 故障排查工作流程

当用户只是含糊地表示“出了问题”或请求帮助调试时，请按照以下步骤进行：
1. 首先使用 `sweep` 命令获取整体情况。
2. 识别最紧急的问题（如 `CrashLoopBackOff` 状态的 Pod、处于 `NotReady` 状态的节点、失败的部署）。
3. 对最可疑的 Pod 进行深入检查。
4. 检查资源使用情况，判断是否存在资源耗尽的问题。
5. 查看事件记录，找出最近可能引发问题的变化。
6. 关联所有数据以形成完整的诊断结果。
7. 提出具体的解决方案，并提供用户可以确认执行的命令。

### Discord v2 交付模式（OpenClaw v2026.2.14+）

当对话在 Discord 频道中进行时：
- 先发送一份简洁的故障排查总结（包括集群健康状况、受影响最严重的组件以及主要问题），然后再询问用户是否需要查看完整的报告。
- 确保第一条回复的长度控制在 1200 字符以内，并避免在消息中展示过于复杂的表格。
- 如果支持 Discord 的相关功能，可以提供以下操作建议：
  - `Run Full Sweep`（执行全面检查）
  - `Pod Autopsy`（进行 Pod 检查）
  - `Show Recent Warning Events`（显示最近的事件）
- 如果相关功能不可用，可以以编号列表的形式提供后续操作建议。
- 对于大量的事件或日志输出，建议每次回复的内容不超过 15 行。

## 输出格式

所有工具的输出都是结构化的 JSON 格式。请将其解析后，使用清晰的 Markdown 格式呈现结果。对于 Pod 列表可以使用表格，事件记录可以使用时间线，推荐的操作命令可以使用代码块来展示。

在每份故障排查报告中，请务必添加以下结尾：
---
*由 Anvil AI 提供支持 🏥*