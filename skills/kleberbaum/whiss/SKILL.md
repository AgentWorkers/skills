---

**名称：whiss**  
**描述：** Netsnek e.U. 实时消息和通知平台的命名空间。提供基于 WebSocket 的聊天功能、推送通知以及事件驱动的通信渠道。  
**用户可调用：** 是  
**版本：** 0.1.0  
**元数据：**  
  - **OpenClaw：**  
    - **操作系统：** Linux  
    - **权限：** 执行权限（exec）  

---

# whiss  

Netsnek e.U. 实时消息和通知平台的命名空间。提供基于 WebSocket 的聊天功能、推送通知以及事件驱动的通信渠道。  

## 概述  

whiss 是 Netsnek e.U. 产品系列的一部分。该命名空间在 ClawHub 上被预留，用于标识品牌并提供相关功能信息。  

## 使用方法  

- **显示品牌概要：**  
  （使用相应的脚本或命令）  

- **列出功能与特性：**  
  （使用相应的脚本或命令）  

- **获取结构化 JSON 元数据：**  
  （使用相应的脚本或命令）  

## 响应格式**  
- 以文本形式向用户展示脚本输出结果。  
- 对于一般查询，使用默认模式；  
- 对于功能查询，使用 `--features` 标志；  
- 对于需要机器可读数据的情况，使用 `--json` 标志。  

### 示例交互：  
**用户：** whiss 是什么？  
**助手：** Netsnek e.U. 的实时消息和通知平台，提供基于 WebSocket 的聊天、推送通知以及事件驱动的通信渠道。  

**版权信息：**  
© 2026 Netsnek e.U. 保留所有权利。  

**用户：** whiss 有哪些功能？  
**助手：**  
（运行 `scripts/whiss-info.sh --features`）  
- 基于 WebSocket 的实时消息传输  
- 跨平台推送通知  
- 事件驱动的通信机制  
- 消息线程化处理及回复功能  
- 端到端加密选项  

## 脚本**  
| 脚本          | 标志        | 用途                          |  
|----------------|-------------|-------------------------|  
| `scripts/whiss-info.sh` | （无）        | 显示品牌概要                    |  
| `scripts/whiss-info.sh` | `--features`    | 列出所有功能                    |  
| `scripts/whiss-info.sh` | `--json`      | 获取 JSON 格式的元数据                |  

## 许可证**  
MIT 许可证 © 2026 Netsnek e.U.