---
name: clawbird
description: "X/Twitter集成——通过官方X API v2实现发帖、回复、搜索、点赞、关注、私信以及提及功能"
homepage: https://github.com/xonder/clawbird
metadata:
  openclaw:
    emoji: "🐦"
    requires:
      plugins: ["clawbird"]
      env: ["X_API_KEY", "X_API_SECRET", "X_ACCESS_TOKEN", "X_ACCESS_SECRET"]
    primaryEnv: "X_API_KEY"
    install:
      - id: "npm"
        kind: "node"
        package: "@xonder/clawbird"
        label: "Install clawbird plugin (npm)"
---
# Clawbird — X/Twitter 工具集

您可以通过官方的 X API v2 访问 13 个工具来与 X（Twitter）进行交互。所有工具都会以 JSON 格式返回结果以及预估的 API 使用成本。

## 认证与凭据

该插件使用 **OAuth 1.0a 用户上下文** 进行认证，以执行写入操作（发布、点赞、关注、私信），并可选地使用 **Bearer Token** 来执行只读操作（搜索、用户查询）。

**凭据的获取方式：** 您必须在 [X 开发者门户](https://developer.x.com) 生成凭据：
1. 在 developer.x.com 上创建一个项目和应用。
2. 生成 OAuth 1.0a 密钥：API Key、API Secret、Access Token、Access Token Secret。
3. （可选）为只读操作生成 Bearer Token。

**凭据的传递方式：** 凭据在运行时通过 OpenClaw 插件配置系统 (`pluginConfig`) 传递给插件。插件从不直接读取配置文件。备用方式：环境变量 `X_API_KEY`、`X_API_SECRET`、`X_ACCESS_TOKEN`、`X_ACCESS_SECRET`、`X_BEARER_TOKEN`。

**请注意：** 任何凭据都不会被硬编码或捆绑在插件中。如果缺少凭据，插件会返回明确的错误信息。

## 外部端点

所有网络请求仅发送到官方的 X API v2，不会访问其他主机。

| 端点 | 方法 | 工具 | 发送的数据 |
|----------|--------|---------|-----------|
| `https://api.x.com/2/tweets` | POST | x_post_tweet、x_post_thread、x_reply_tweet | 推文内容、回复元数据 |
| `https://api.x.com/2/tweets/:id` | GET | x_get_tweet | 推文 ID |
| `https://api.x.com/2/tweets/search/recent` | GET | x_search_tweets | 搜索查询字符串 |
| `https://api.x.com/2/users/me` | GET | x_like_tweet、x_getmentions、x_follow_user | （仅需要认证头） |
| `https://api.x.com/2/users/by/username/:username` | GET | x_get_user_profile、x_send_dm、x_follow_user | 用户名 |
| `https://api.x.com/2/users/:id/likes` | POST | x_like_tweet | 推文 ID |
| `https://api.x.com/2/users/:id/mentions` | GET | x_getmentions | 用户 ID、分页参数 |
| `https://api.x.com/2/users/:id/following` | POST | x_follow_user | 目标用户 ID |
| `https://api.x.com/2/dm_conversations/with/:id/messages` | POST | x_send_dm | 消息内容、接收者 ID |
| `https://api.x.com/2/dm_conversations/with/:id/dm_events` | GET | x_get_dms (过滤后的结果) | 参与者 ID |
| `https://api.x.com/2/dm_events` | GET | x_get_dms (所有结果) | 分页参数 |

## 安全性与隐私

- **网络访问：** 仅访问 `api.x.com`（官方 X API），不会访问其他域名。
- **本地文件访问：** 在工作目录中写入一个会话范围的 `clawbird-interactions.jsonl` 文件，记录操作日志（发布、点赞、关注、私信），以避免重复操作。除了声明的环境变量外，不会读取或写入其他文件。
- **凭据处理：** OAuth 令牌在运行时从插件配置或环境变量中读取，并通过签名的 HTTP 头发送到 X API。这些令牌不会被记录、缓存到磁盘或传输给第三方。
- **发送到 X 的数据：** 仅包括您在工具参数中明确提供的数据（推文内容、搜索查询、用户名、消息内容）。不会收集或发送额外的用户数据。
- **从 X 接收的数据：** X API 返回的推文内容、用户资料、私信消息和互动数据。这些数据以 JSON 格式返回给插件，不会被存储。

## 信任声明

Clawbird 是一个通过 npm 分发的技能——运行时代码通过 `npm i -g @xonder/clawbird` 安装，并不包含在 ClawHub 技能包中。以下安全特性可以通过审核源代码来验证：

- **源代码：** https://github.com/xonder/clawbird (MIT)
- **npm：** https://www.npmjs.com/package/@xonder/clawbird
- **仅访问 `api.x.com`（[源代码：`src/client.ts`](https://github.com/xonder/clawbird/blob/main/src/client.ts)`
- **仅读取和写入一个本地文件** (`clawbird-interactions.jsonl`) 用于记录会话交互——没有其他文件系统访问权限 ([源代码：`src/interaction-log.ts`](https://github.com/xonder/clawbird/blob/main/src/interaction-log.ts)`
- **没有其他依赖项**，仅依赖官方的 `@xdevplatform/xdk` SDK 和 `@sinclair/typebox`
- 包含一个全面的测试套件（200 多个测试），可以通过 `npm test` 验证。

由于该技能在运行时通过 npm 安装代码，在授予凭据之前，请 **审查源代码或固定特定版本**（`npm i -g @xonder/clawbird@1.1.0`）。

## 写入操作与自主使用

以下工具会 **修改您的 X 账户的远程状态**：

| 工具 | 操作 | 是否可撤销？ |
|------|--------|-------------|
| x_post_tweet | 发布推文 | 可手动删除 |
| x_post_thread | 发布多条推文 | 可手动删除 |
| x_reply_tweet | 回复推文 | 可手动删除 |
| x_like_tweet | 点赞推文 | 可手动取消点赞 |
| x_follow_user | 关注用户 | 可手动取消关注 |
| x_send_dm | 发送私信 | 无法撤销 |

**建议：** 如果自主运行，请考虑通过配置代理级别的工具策略，在执行写入操作前要求用户确认。只读工具（x_get_tweet、x_search_tweets、x_get_user_profile、x_getmentions、x_get_dms、x_get_cost_summary）适合自主使用。

## 可用工具

### 发布

**`x_post_tweet`** — 发布单条推文。
- `text`（必填）：推文内容（最多 280 个字符）
- 返回值：`{ id, text, url, estimatedCost }`

**`x_post_thread`** — 发布多条推文。
- `tweets`（必填）：推文内容的数组（按顺序发布，每条推文都是对前一条的回复）
- 返回值：`{ threadId, tweetCount, tweets: [{ id, text, url }], estimatedCost }`

**`x_reply_tweet`** — 回复现有推文。
- `tweetId`（必填）：推文 ID 或完整 URL（例如 `https://x.com/user/status/123456`）
- `text`（必填）：回复内容（最多 280 个字符）
- 返回值：`{ id, text, url, inReplyTo, estimatedCost }`

### 互动

**`x_like_tweet`** — 点赞推文。
- `tweetId`（必填）：推文 ID 或完整 URL
- 返回值：`{ liked, tweetId, estimatedCost }`

### 社交

**`x_follow_user`** — 关注用户。
- `username`（必填）：要关注的用户名（可带 `@`）
- 返回值：`{ following, user: { id, username }, estimatedCost }`

### 搜索

**`x_get_tweet`** — 通过 ID 或 URL 获取单条推文。
- `tweetId`（必填）：推文 ID 或完整 URL（例如 `https://x.com/user/status/123456`）
- 返回值：`{ id, text, authorId, createdAt, metrics, conversationId, lang, url, author: { id, name, username, verified, profileImageUrl }, estimatedCost }`

**`x_search_tweets`** — 搜索最近的推文（过去 7 天内）。
- `query`（必填）：搜索查询——支持 X 操作符，如 `from:user`、`#hashtag`、`"exact phrase"`、`-exclude`、`lang:en`
- `maxResults`（可选）：10–100 条，默认 10 条
- 返回值：`{ query, resultCount, tweets: [{ id, text, authorId, createdAt, metrics, url }], estimatedCost }`

**`x_get_user_profile`** — 获取用户资料。
- `username`（必填）：用户名（可带 `@`）
- 返回值：`{ id, name, username, description, followersCount, followingCount, tweetCount, verified, profileImageUrl, url, createdAt, location, profileUrl, estimatedCost }`

**`x_getmentions`** — 获取当前用户的最近提及信息。
- `maxResults`（可选）：5–100 条，默认 10 条
- 返回值：`{ resultCount, mentions: [{ id, text, authorId, createdAt, metrics, url }], estimatedCost }`

### 私信

**`x_send_dm`** — 向用户发送私信。
- `username`（必填）：接收者的用户名（可带 `@`）
- `text`（必填）：消息内容
- 返回值：`{ sent, eventId, conversationId, recipient: { id, username }, estimatedCost }`

**`x_get_dms`** — 获取最近的私信记录。
- `username`（可选）：按特定用户的对话筛选私信
- `maxResults`（可选）：1–100 条，默认 10 条
- 返回值：`{ resultCount, messages: [{ id, text, senderId, createdAt, conversationId, eventType }], estimatedCost }`

### 实用工具

**`x_get_interaction_log`** — 获取当前会话中所有写入操作的日志（发布、回复、点赞、关注、私信）。有助于查看已执行的操作，避免重复操作。
- `limit`（可选）：返回的最新条目数量（默认：全部）
- 返回值：`{ totalEntries, returned, logFile, entries: [{ timestamp, action, summary, details }] }`

**`x_get_cost_summary`** — 获取当前会话的累计 API 使用成本。
- 无需参数
- 返回值：`{ totalCost, breakdown: { [action]: { calls, totalCost } } }`

## 最佳实践

### 搜索查询
- 使用 `from:username` 搜索特定用户的推文
- 使用 `#hashtag` 进行标签搜索
- 使用 `"exact phrase"` 进行精确匹配
- 结合使用操作符：`#AI from:openai -is:retweet lang:en`
- 使用 `-is:retweet` 过滤转发的推文

### 推文格式
- 每条推文的长度请控制在 280 个字符以内
- 第一条推文开头使用吸引人的标题
- 对于长推文，使用数字序号（1/N）
- 结尾使用行动号召或总结语

### 成本意识
每个工具的响应中都包含 `estimatedCost` 字段。大致费用如下：
- 发布/回复：每条推文约 0.01 美元
- 点赞：每条推文约 0.005 美元
- 搜索：每个结果约 0.005 美元
- 用户查询：每条结果约 0.001 美元
- 提及：每个结果约 0.005 美元
- 发送私信：每条消息约 0.01 美元
- 查看私信：每个结果约 0.005 美元
- 获取推文：每条推文约 0.005 美元

在使用高成本操作前，请使用 `x_get_cost_summary` 查看累计会话费用。

### 速率限制
- 发布：每 15 分钟 200 条推文
- 搜索：每 15 分钟 180 次请求（用户），450 次（应用）
- 点赞：每 15 分钟 50 次
- 用户查询：每 15 分钟 900 次
- 提及：每 15 分钟 180 次
- 私信：每 15 分钟 200 条，24 小时内 1000 条
- 关注：每 24 小时 400 次

### 错误处理
所有工具都会返回错误信息，格式为 `{ error: "message", details?: ... }`。常见问题包括：
- 速率限制（等待并重试）
- 认证错误（检查 API 凭据）
- 未找到推文（验证 ID/URL）
- 内容为空（请提供非空内容）