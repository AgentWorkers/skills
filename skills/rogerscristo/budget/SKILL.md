---
name: budget-tracker
description: 跟踪代理的支出情况，设置预算和提醒机制，以避免意外的费用支出。适用于需要记录开支、查看剩余预算、设定支出限制或获取费用汇总的代理。对于涉及实际资金的自主代理来说，这一功能至关重要。
user-invocable: true
metadata: {"openclaw": {"emoji": "💰", "os": ["darwin", "linux"], "requires": {"bins": ["python3"]}}}
---
# 预算追踪器

实时追踪你的代理所花费的每一分钱。设置严格的预算限制，在超出预算前收到警报。

## 该功能的必要性

拥有API、域名和服务访问权限的自主代理可能会产生意外的费用。这个功能为你提供了财务保障——记录每一笔交易，执行支出限制，并随时了解你的财务状况。

## 命令

### 记录交易
```bash
python3 {baseDir}/scripts/budget.py log --amount 10.00 --merchant "Namecheap" --category "domain" --note "arcself.com registration"
```

### 查看余额
```bash
python3 {baseDir}/scripts/budget.py balance
```

### 查看支出汇总
```bash
python3 {baseDir}/scripts/budget.py summary
```

### 查看最近的交易
```bash
python3 {baseDir}/scripts/budget.py history --limit 10
```

### 设置预算限制
```bash
python3 {baseDir}/scripts/budget.py set-budget --total 200.00
```

### 设置警报阈值（当余额低于此值时发出警告）
```bash
python3 {baseDir}/scripts/budget.py set-alert --threshold 50.00
```

### 检查某笔购买是否安全
```bash
python3 {baseDir}/scripts/budget.py check --amount 25.00
```

### 导出到CSV
```bash
python3 {baseDir}/scripts/budget.py export --format csv
```

## 数据存储

预算数据默认存储在`~/.openclaw/budget-tracker/budget.json`文件中。你可以通过`--data-dir /path/to/dir`参数自定义存储路径。

JSON数据结构如下：
```json
{
  "budget": {"total": 200.00, "alert_threshold": 50.00},
  "transactions": [
    {
      "id": "txn_001",
      "timestamp": "2026-02-15T14:00:00Z",
      "amount": 10.00,
      "merchant": "Namecheap",
      "category": "domain",
      "note": "arcself.com"
    }
  ]
}
```

## 分类

请使用统一的分类方式：`domain`（域名）、`hosting`（托管服务）、`api`（API费用）、`tool`（工具费用）、`subscription`（订阅费用）、`marketing`（营销费用）、`other`（其他费用）。

## 警报机制

当余额低于设定的警报阈值时，系统会发出警告；如果某笔购买会超出剩余预算，系统会阻止该购买并给出警告。

## 使用建议

- 在支出后立即记录交易，不要批量处理。
- 在进行任何购买前使用`check`命令确认预算是否安全。
- 每天开始时运行`summary`命令以了解财务状况。
- 将`--alert-threshold`参数设置为总预算的25%。