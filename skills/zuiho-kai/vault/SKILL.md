---
name: vault
description: 一款采用 AES-256-GCM 加密技术的安全本地密码存储工具。支持通过 CLI 命令来存储、检索和管理密码。
version: 1.1.2
author: zuiho-kai
homepage: https://github.com/zuiho-kai/openclaw-vault
tags: [password, vault, security, credentials, password-manager, cli, encryption]
metadata:
  openclaw:
    requires:
      bins: [node, npm]
      env: [VAULT_MASTER_KEY]
---
# vault

**适用场景**：当您需要为密码、API密钥或凭证提供安全的本地存储时。

🔒 **AES-256-GCM加密**：该插件使用行业标准的AES-256-GCM加密算法对密码进行存储，并采用一个主密钥进行加密。

## 主要功能

- 🔒 所有存储的密码均采用AES-256-GCM加密
- 📝 简单的命令行接口
- 🗂️ 密钥管理和查询
- 💾 基于JSON的本地存储（已加密）
- 🕐 自动时间戳记录
- 🔑 主密钥保护

## 安装

```bash
clawhub install vault
```

## 使用方法

### 设置密码

```bash
vault gemini sk-abc123xyz
```

### 查看密码

```bash
vault gemini show
```

### 删除密码

```bash
vault gemini remove
```

### 列出所有密钥

```bash
vault list
```

## 配置

### 主密钥（必填）

您可以通过环境变量设置主加密密钥：

```bash
export VAULT_MASTER_KEY="your-secure-master-key-here"
```

或者通过OpenClaw的配置文件进行设置：

```json
{
  "plugins": {
    "vault": {
      "masterKey": "your-secure-master-key-here",
      "storageFile": ".vault/passwords.json"
    }
  }
}
```

**可选参数：**
- `masterKey` - 主加密密钥（也可以使用环境变量`VAULT_MASTER_KEY`）
- `storageFile`（默认值：`.vault/passwords.json`） - 相对于用户主目录的存储文件路径

**重要提示**：请确保主密钥的安全性！如果没有主密钥，您将无法解密存储的密码。

## 安全性

🔒 **加密细节**：
- **加密算法**：AES-256-GCM（Galois/Counter模式）
- **密钥生成**：为每个密码生成一个随机盐值，并使用scrypt算法进行密钥派生
- **IV（初始化向量）**：为每个密码生成一个12字节的随机初始化向量（GCM推荐的大小）
- **盐值**：为每个密码生成一个32字节的随机盐值，并与加密数据一起存储
- **认证**：使用GCM认证标签进行数据完整性验证

**安全最佳实践**：
- 使用强且唯一的密钥（建议至少32个字符）
- 保护好主密钥（通过环境变量或安全配置文件存储）
- 设置严格的文件权限：`chmod 600 ~/.vault/passwords.json`
- 将`.vault/`添加到`.gitignore`文件中，避免主密钥被提交到版本控制系统中
- 使用系统级别的磁盘加密来增加安全性
- 安全地备份主密钥——丢失密钥意味着密码也会丢失

**适用场景**：
- 开发/测试过程中的凭证
- API密钥和令牌
- 个人密码
- 团队共享的凭证（需确保密钥的安全分发）

## 示例

```bash
# Save API keys
vault openai sk-proj-abc123
vault anthropic sk-ant-xyz789

# View a key
vault openai show
# Output: Password for 'openai': sk-proj-abc123

# List all keys
vault list
# Output:
# Stored passwords:
# • openai (created: 2026-02-17T..., updated: 2026-02-17T...)
# • anthropic (created: 2026-02-17T..., updated: 2026-02-17T...)

# Remove a key
vault openai remove
```

## 链接

- GitHub仓库：https://github.com/zuiho-kai/openclaw-vault
- 问题报告：https://github.com/zuiho-kai/openclaw-vault/issues