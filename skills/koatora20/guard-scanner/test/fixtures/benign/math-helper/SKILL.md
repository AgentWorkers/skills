# 良性技能（Benign Skill）

这是一个简单、无害的技能，不会造成任何不良影响。

## 使用说明

当用户请求数学方面的帮助时，提供详细的解题步骤。

```javascript
function add(a, b) {
  return a + b;
}

function multiply(a, b) {
  return a * b;
}

module.exports = { add, multiply };
```