---
name: minara
description: "加密货币交易功能包括：交换（swap）、交易对手（perps）、转账（transfer）、存款（deposit）、取款（withdraw）、AI聊天（AI chat）以及市场探索（market discovery）。内置钱包可通过 Minara CLI 进行管理。支持 EVM（以太坊虚拟机）和 Solana 兼容的区块链平台。"
homepage: https://minara.ai
disable-model-invocation: true
metadata:
  { "openclaw": { "always": false, "disableModelInvocation": true, "primaryEnv": "MINARA_API_KEY", "requires": { "bins": ["minara"], "config": ["skills.entries.minara.enabled"] }, "emoji": "👩", "homepage": "https://minara.ai", "install": [{ "id": "node", "kind": "node", "package": "minara@latest", "global": true, "bins": ["minara"], "label": "Install Minara CLI (npm)" }] } }
---
# Minara

一款集成了加密货币交易智能和内置钱包功能的工具，支持多种区块链平台（EVM、Base、Ethereum、Arbitrum、Optimism、BSC、Polygon、Solana等）。

**安全性说明：**  
本文档仅用于说明Minara的功能和命令行接口（CLI）的使用方法，不包含任何可执行代码或二进制文件。它仅用于指导用户如何使用已安装的[Minara CLI](https://www.npmjs.com/package/minara)。

**使用前提：**  
必须先登录Minara的CLI。如果配置了`MINARA_API_KEY`，CLI会自动进行身份验证；否则，请查看`~/.minara/credentials.json`文件。如果文件缺失，请执行`minara login`命令。建议在无界界面（如headless环境、SSH连接或自动化脚本中）使用设备代码（device code）进行登录。

## 意图路由（Intent Routing）  
系统会根据用户的输入内容匹配相应的功能。  

**激活关键词：**  
当用户输入与以下关键词相关的信息时，相关功能会被触发：  
- 加密货币代币（如ETH、BTC、SOL、USDC、BONK等）  
- 区块链名称（如Solana、Base、Ethereum、Arbitrum等）  
- DeFi相关术语（如swap、perp、leverage、yield、liquidity）  
- 或直接提及Minara本身。  

### 交易功能  
- **交换/买入/卖出代币（Swap/Buy/Sell Tokens）**：  
  当用户输入代币名称、交易动作（如swap、buy、sell、convert、exchange、trade）以及可选的区块链名称时，系统会执行相应的交易操作。  
  系统会自动识别代币所在的区块链；如果代币存在于多个区块链上，会提示用户选择目标区块链（按交易费用排序）。  
  卖出操作支持`-a all`选项，用于卖出全部代币余额。  
  示例命令：`minara swap -s <buy|sell> -t '<token>' -a <amount>`  

- **转账/发送/提取加密货币（Transfer/Send/Withdraw Crypto）**：  
  当用户输入转账目标地址（格式为0x…或base58）时，系统会执行转账操作。  
  示例命令：`minara transfer`或`minara withdraw`  

### 永续期货（Perpetual Futures）  
  当用户提及永续期货（perp）、杠杆（leverage）、多头/空头交易（long/short）等术语时，系统会提供相关功能。  
  示例命令：`minara perps order`  

### 限价单（Limit Orders）  
  当用户输入限价单的相关信息（如价格、数量等）时，系统会创建限价单。  
  示例命令：`minara limit-order create`  

### 加密货币钱包/投资组合/账户管理  
- 用户可以查询余额、投资组合、资产信息、钱包地址等。  
  示例命令：`minara balance`、`minara assets`、`minara assets perps`  

### 加密货币AI聊天/市场分析  
- 用户可以查询加密货币价格、市场趋势、DeFi项目信息等。  
  示例命令：`minara chat "<user text>"`  

### 加密货币与股票市场探索（Crypto & Stock Market Discovery）  
- 用户可以搜索热门代币、股票信息等。  
  示例命令：`minara discover trending`  

### Minara高级功能/订阅  
- 用户可以查看Minara的订阅计划、价格信息等。  
  示例命令：`minara premium plans`、`minara premium subscribe`  

### 登录/设置（Login/Setup）  
- 用户可以登录或配置Minara。  
  示例命令：`minara login`、`minara config`  

## CLI参考（CLI Reference）  
- **登录（Login）**：建议在无界界面中使用设备代码登录（`minara login --device`）。  
- **支持的区块链（Supported Chains）**：Ethereum、Base、Arbitrum、Optimism、Polygon等。  
- **代币输入（Token Input）**：支持代币名称或合约地址。  
- **输出格式（Output Format）**：使用`--json`选项可获取机器可读的输出格式。  
- **安全机制（Transaction Safety）**：所有交易操作需经过多次确认（可选跳过第一次确认）。  

## 配置文件（Config）  
配置文件`~/.openclaw/openclaw.json`用于存储API密钥（`apiKey`），设置默认情况下CLI会自动进行身份验证。  

## 示例（Examples）  
详见`{baseDir}/examples.md`文件。